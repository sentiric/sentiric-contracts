# .github/workflows/release.yml

name: Release Contracts

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    name: Generate, Commit, and Publish Packages
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Get tag version
        id: get_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_ENV

      - name: Setup Buf
        uses: bufbuild/buf-setup-action@v1.35.1

      - name: Generate all language stubs
        run: buf generate

      - name: Configure Git user for bot
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "github-actions-bot@users.noreply.github.com"

      - name: Commit generated code
        run: |
          # --- KRİTİK DÜZELTME ---
          # .gitignore tarafından yok sayılan 'gen/' klasörünü ZORLA ekle.
          git add -f gen/
          
          if ! git diff --staged --quiet; then
            git commit -m "feat(gen): Generate code for v${{ env.VERSION }}"
          else
            echo "No changes in generated code to commit."
          fi

      - name: Push changes and update tag
        run: |
          # Commit atılıp atılmadığını kontrol et
          if git log -1 --pretty=%B | grep -q "Generate code for v${{ env.VERSION }}"; then
            echo "Pushing generated code and updating tag..."
            git push origin HEAD:main
            TAG_NAME="v${{ env.VERSION }}"
            # Mevcut etiketi silip, yeni commit'in üzerine yeniden oluşturuyoruz
            git tag -f $TAG_NAME
            git push -f origin refs/tags/$TAG_NAME
          else
            echo "No new commit to push. Tag is up to date."
          fi

      # --- Node.js Paketini Yayınla ---
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@sentiric'

      - name: Update package.json version
        run: npm version ${{ env.VERSION }} --no-git-tag-version --allow-same-version
      
      - name: Publish to GitHub Packages (Node.js)
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # --- Python Paketini Yayınla (İsteğe Bağlı) ---
      # ... (Python yayınlama adımları aynı kalabilir) ...