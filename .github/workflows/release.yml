# .github/workflows/release.yml

name: Release Contracts

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    name: Generate, Commit, and Publish Packages
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0 

      - name: Get tag version
        id: get_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_ENV

      - name: Setup Buf
        uses: bufbuild/buf-setup-action@v1.35.1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
      
      # YENİ: buf dep update adımını ekliyoruz
      - name: Update dependencies
        working-directory: proto # DÜZELTME
        run: buf dep update

      # DÜZELTME: Bu adımın çalışma dizinini de belirtiyoruz
      - name: Generate all language stubs
        working-directory: proto # DÜZELTME
        run: buf generate

      - name: Configure Git user for bot
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "github-actions-bot@users.noreply.github.com"

      - name: Commit generated code
        run: |
          # DÜZELTME: Ekleyeceğimiz dosyaların yolu artık bir üst dizinde
          git add -f ../gen/
          # DÜZELTME: Konfigürasyon dosyalarını da commite ekliyoruz
          git add -f buf.yaml buf.gen.yaml buf.lock
          if ! git diff --staged --quiet; then
            git commit -m "feat(gen): Generate code for v${{ env.VERSION }}"
          else
            echo "No changes in generated code to commit."
          fi
        working-directory: proto # DÜZELTME: Bu komutları da proto dizininden çalıştır

      # ... (geri kalan adımlar aynı kalabilir, ancak commit'i ana dizine dönerek yapmak daha güvenli olabilir. Şimdilik bu haliyle devam edelim.)
      # ...
      - name: Push changes and update tag
        run: |
          # ... (Bu adım şimdilik aynı kalabilir, bir sonraki seferde sorun çıkarsa iyileştiririz)
          if git log -1 --pretty=%B | grep -q "Generate code for v${{ env.VERSION }}"; then
            echo "Rebasing onto main and pushing generated code..."
            git pull origin main --rebase
            git push origin HEAD:main
            TAG_NAME="v${{ env.VERSION }}"
            git tag -f $TAG_NAME
            git push -f origin refs/tags/$TAG_NAME
          else
            echo "No new commit to push. Tag is up to date."
          fi
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@sentiric'

      - name: Update package.json version and publish
        run: |
          npm version ${{ env.VERSION }} --no-git-tag-version --allow-same-version
          npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # --- Python YAYINLAMA, GITHUB SORUNU NEDENİYLE GEÇİCİ OLARAK DEVRE DIŞI BIRAKILDI ---
      # - name: Setup Python
      #   uses: actions/setup-python@v4
      #   with:
      #     python-version: '3.10'
      # - name: Install Python build dependencies
      #   run: python -m pip install --upgrade build twine
      # - name: Update setup.py version
      #   run: sed -i "s/version=\"[0-9.]*\"/version=\"${{ env.VERSION }}\"/" setup.py
      # - name: Build Python package
      #   run: python -m build
      # - name: Create .pypirc file for GitHub Packages
      #   run: |
      #     echo "[distutils]" > ~/.pypirc
      #     echo "index-servers = github" >> ~/.pypirc
      #     echo "[github]" >> ~/.pypirc
      #     echo "repository = https://pypi.pkg.github.com/${{ github.repository_owner }}" >> ~/.pypirc
      #     echo "username = ${{ github.actor }}" >> ~/.pypirc
      #     echo "password = ${{ secrets.GH_PAT }}" >> ~/.pypirc
      # - name: Publish Python package to GitHub Packages
      #   run: twine upload --repository github dist/*