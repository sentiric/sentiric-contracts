// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/control/v1/task.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.control.v1";

/** ESKİ: TaskRequest */
export interface DispatchTaskRequest {
  taskType: string;
  payload: { [key: string]: string };
  dueDate?: string | undefined;
}

export interface DispatchTaskRequest_PayloadEntry {
  key: string;
  value: string;
}

/** ESKİ: TaskID */
export interface DispatchTaskResponse {
  /** İsim TaskID'den task_id'ye çevrildi */
  taskId: string;
}

function createBaseDispatchTaskRequest(): DispatchTaskRequest {
  return { taskType: "", payload: {}, dueDate: undefined };
}

export const DispatchTaskRequest: MessageFns<DispatchTaskRequest> = {
  encode(message: DispatchTaskRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskType !== "") {
      writer.uint32(10).string(message.taskType);
    }
    globalThis.Object.entries(message.payload).forEach(([key, value]: [string, string]) => {
      DispatchTaskRequest_PayloadEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    if (message.dueDate !== undefined) {
      writer.uint32(26).string(message.dueDate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DispatchTaskRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDispatchTaskRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskType = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = DispatchTaskRequest_PayloadEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.payload[entry2.key] = entry2.value;
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.dueDate = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DispatchTaskRequest {
    return {
      taskType: isSet(object.taskType)
        ? globalThis.String(object.taskType)
        : isSet(object.task_type)
        ? globalThis.String(object.task_type)
        : "",
      payload: isObject(object.payload)
        ? (globalThis.Object.entries(object.payload) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      dueDate: isSet(object.dueDate)
        ? globalThis.String(object.dueDate)
        : isSet(object.due_date)
        ? globalThis.String(object.due_date)
        : undefined,
    };
  },

  toJSON(message: DispatchTaskRequest): unknown {
    const obj: any = {};
    if (message.taskType !== "") {
      obj.taskType = message.taskType;
    }
    if (message.payload) {
      const entries = globalThis.Object.entries(message.payload) as [string, string][];
      if (entries.length > 0) {
        obj.payload = {};
        entries.forEach(([k, v]) => {
          obj.payload[k] = v;
        });
      }
    }
    if (message.dueDate !== undefined) {
      obj.dueDate = message.dueDate;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DispatchTaskRequest>, I>>(base?: I): DispatchTaskRequest {
    return DispatchTaskRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DispatchTaskRequest>, I>>(object: I): DispatchTaskRequest {
    const message = createBaseDispatchTaskRequest();
    message.taskType = object.taskType ?? "";
    message.payload = (globalThis.Object.entries(object.payload ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.dueDate = object.dueDate ?? undefined;
    return message;
  },
};

function createBaseDispatchTaskRequest_PayloadEntry(): DispatchTaskRequest_PayloadEntry {
  return { key: "", value: "" };
}

export const DispatchTaskRequest_PayloadEntry: MessageFns<DispatchTaskRequest_PayloadEntry> = {
  encode(message: DispatchTaskRequest_PayloadEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DispatchTaskRequest_PayloadEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDispatchTaskRequest_PayloadEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DispatchTaskRequest_PayloadEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: DispatchTaskRequest_PayloadEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DispatchTaskRequest_PayloadEntry>, I>>(
    base?: I,
  ): DispatchTaskRequest_PayloadEntry {
    return DispatchTaskRequest_PayloadEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DispatchTaskRequest_PayloadEntry>, I>>(
    object: I,
  ): DispatchTaskRequest_PayloadEntry {
    const message = createBaseDispatchTaskRequest_PayloadEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseDispatchTaskResponse(): DispatchTaskResponse {
  return { taskId: "" };
}

export const DispatchTaskResponse: MessageFns<DispatchTaskResponse> = {
  encode(message: DispatchTaskResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DispatchTaskResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDispatchTaskResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DispatchTaskResponse {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
    };
  },

  toJSON(message: DispatchTaskResponse): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DispatchTaskResponse>, I>>(base?: I): DispatchTaskResponse {
    return DispatchTaskResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DispatchTaskResponse>, I>>(object: I): DispatchTaskResponse {
    const message = createBaseDispatchTaskResponse();
    message.taskId = object.taskId ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
