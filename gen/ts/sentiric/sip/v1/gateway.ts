// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/sip/v1/gateway.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.sip.v1";

/** ESKİ: LoadRequest */
export interface GetLoadRequest {
  region: string;
}

/** ESKİ: LoadResponse */
export interface GetLoadResponse {
  currentCpuLoad: number;
  activeCalls: number;
}

function createBaseGetLoadRequest(): GetLoadRequest {
  return { region: "" };
}

export const GetLoadRequest: MessageFns<GetLoadRequest> = {
  encode(message: GetLoadRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.region !== "") {
      writer.uint32(10).string(message.region);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetLoadRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetLoadRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.region = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetLoadRequest {
    return { region: isSet(object.region) ? globalThis.String(object.region) : "" };
  },

  toJSON(message: GetLoadRequest): unknown {
    const obj: any = {};
    if (message.region !== "") {
      obj.region = message.region;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetLoadRequest>, I>>(base?: I): GetLoadRequest {
    return GetLoadRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetLoadRequest>, I>>(object: I): GetLoadRequest {
    const message = createBaseGetLoadRequest();
    message.region = object.region ?? "";
    return message;
  },
};

function createBaseGetLoadResponse(): GetLoadResponse {
  return { currentCpuLoad: 0, activeCalls: 0 };
}

export const GetLoadResponse: MessageFns<GetLoadResponse> = {
  encode(message: GetLoadResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.currentCpuLoad !== 0) {
      writer.uint32(9).double(message.currentCpuLoad);
    }
    if (message.activeCalls !== 0) {
      writer.uint32(16).int32(message.activeCalls);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetLoadResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetLoadResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.currentCpuLoad = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.activeCalls = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetLoadResponse {
    return {
      currentCpuLoad: isSet(object.currentCpuLoad)
        ? globalThis.Number(object.currentCpuLoad)
        : isSet(object.current_cpu_load)
        ? globalThis.Number(object.current_cpu_load)
        : 0,
      activeCalls: isSet(object.activeCalls)
        ? globalThis.Number(object.activeCalls)
        : isSet(object.active_calls)
        ? globalThis.Number(object.active_calls)
        : 0,
    };
  },

  toJSON(message: GetLoadResponse): unknown {
    const obj: any = {};
    if (message.currentCpuLoad !== 0) {
      obj.currentCpuLoad = message.currentCpuLoad;
    }
    if (message.activeCalls !== 0) {
      obj.activeCalls = Math.round(message.activeCalls);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetLoadResponse>, I>>(base?: I): GetLoadResponse {
    return GetLoadResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetLoadResponse>, I>>(object: I): GetLoadResponse {
    const message = createBaseGetLoadResponse();
    message.currentCpuLoad = object.currentCpuLoad ?? 0;
    message.activeCalls = object.activeCalls ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
