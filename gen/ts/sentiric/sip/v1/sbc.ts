// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/sip/v1/sbc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.sip.v1";

/** ESKİ: SipPacket */
export interface GetRouteRequest {
  rawSipMessage: string;
  sourceIp: string;
}

/** ESKİ: SbcDecision */
export interface GetRouteResponse {
  allow: boolean;
  nextHopUri?: string | undefined;
}

function createBaseGetRouteRequest(): GetRouteRequest {
  return { rawSipMessage: "", sourceIp: "" };
}

export const GetRouteRequest: MessageFns<GetRouteRequest> = {
  encode(message: GetRouteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rawSipMessage !== "") {
      writer.uint32(10).string(message.rawSipMessage);
    }
    if (message.sourceIp !== "") {
      writer.uint32(18).string(message.sourceIp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetRouteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetRouteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rawSipMessage = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sourceIp = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetRouteRequest {
    return {
      rawSipMessage: isSet(object.rawSipMessage)
        ? globalThis.String(object.rawSipMessage)
        : isSet(object.raw_sip_message)
        ? globalThis.String(object.raw_sip_message)
        : "",
      sourceIp: isSet(object.sourceIp)
        ? globalThis.String(object.sourceIp)
        : isSet(object.source_ip)
        ? globalThis.String(object.source_ip)
        : "",
    };
  },

  toJSON(message: GetRouteRequest): unknown {
    const obj: any = {};
    if (message.rawSipMessage !== "") {
      obj.rawSipMessage = message.rawSipMessage;
    }
    if (message.sourceIp !== "") {
      obj.sourceIp = message.sourceIp;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetRouteRequest>, I>>(base?: I): GetRouteRequest {
    return GetRouteRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetRouteRequest>, I>>(object: I): GetRouteRequest {
    const message = createBaseGetRouteRequest();
    message.rawSipMessage = object.rawSipMessage ?? "";
    message.sourceIp = object.sourceIp ?? "";
    return message;
  },
};

function createBaseGetRouteResponse(): GetRouteResponse {
  return { allow: false, nextHopUri: undefined };
}

export const GetRouteResponse: MessageFns<GetRouteResponse> = {
  encode(message: GetRouteResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.allow !== false) {
      writer.uint32(8).bool(message.allow);
    }
    if (message.nextHopUri !== undefined) {
      writer.uint32(18).string(message.nextHopUri);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetRouteResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetRouteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.allow = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nextHopUri = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetRouteResponse {
    return {
      allow: isSet(object.allow) ? globalThis.Boolean(object.allow) : false,
      nextHopUri: isSet(object.nextHopUri)
        ? globalThis.String(object.nextHopUri)
        : isSet(object.next_hop_uri)
        ? globalThis.String(object.next_hop_uri)
        : undefined,
    };
  },

  toJSON(message: GetRouteResponse): unknown {
    const obj: any = {};
    if (message.allow !== false) {
      obj.allow = message.allow;
    }
    if (message.nextHopUri !== undefined) {
      obj.nextHopUri = message.nextHopUri;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetRouteResponse>, I>>(base?: I): GetRouteResponse {
    return GetRouteResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetRouteResponse>, I>>(object: I): GetRouteResponse {
    const message = createBaseGetRouteResponse();
    message.allow = object.allow ?? false;
    message.nextHopUri = object.nextHopUri ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
