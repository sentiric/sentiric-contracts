// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/sip/v1/registrar.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.sip.v1";

export interface RegisterRequest {
  sipUri: string;
  contactUri: string;
  /** saniye cinsinden */
  expires: number;
}

export interface RegisterResponse {
  success: boolean;
}

export interface UnregisterRequest {
  sipUri: string;
}

export interface UnregisterResponse {
  success: boolean;
}

export interface LookupContactRequest {
  sipUri: string;
}

export interface LookupContactResponse {
  contactUris: string[];
}

function createBaseRegisterRequest(): RegisterRequest {
  return { sipUri: "", contactUri: "", expires: 0 };
}

export const RegisterRequest: MessageFns<RegisterRequest> = {
  encode(message: RegisterRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sipUri !== "") {
      writer.uint32(10).string(message.sipUri);
    }
    if (message.contactUri !== "") {
      writer.uint32(18).string(message.contactUri);
    }
    if (message.expires !== 0) {
      writer.uint32(24).int32(message.expires);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sipUri = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.contactUri = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.expires = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterRequest {
    return {
      sipUri: isSet(object.sipUri)
        ? globalThis.String(object.sipUri)
        : isSet(object.sip_uri)
        ? globalThis.String(object.sip_uri)
        : "",
      contactUri: isSet(object.contactUri)
        ? globalThis.String(object.contactUri)
        : isSet(object.contact_uri)
        ? globalThis.String(object.contact_uri)
        : "",
      expires: isSet(object.expires) ? globalThis.Number(object.expires) : 0,
    };
  },

  toJSON(message: RegisterRequest): unknown {
    const obj: any = {};
    if (message.sipUri !== "") {
      obj.sipUri = message.sipUri;
    }
    if (message.contactUri !== "") {
      obj.contactUri = message.contactUri;
    }
    if (message.expires !== 0) {
      obj.expires = Math.round(message.expires);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterRequest>, I>>(base?: I): RegisterRequest {
    return RegisterRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterRequest>, I>>(object: I): RegisterRequest {
    const message = createBaseRegisterRequest();
    message.sipUri = object.sipUri ?? "";
    message.contactUri = object.contactUri ?? "";
    message.expires = object.expires ?? 0;
    return message;
  },
};

function createBaseRegisterResponse(): RegisterResponse {
  return { success: false };
}

export const RegisterResponse: MessageFns<RegisterResponse> = {
  encode(message: RegisterResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: RegisterResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterResponse>, I>>(base?: I): RegisterResponse {
    return RegisterResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterResponse>, I>>(object: I): RegisterResponse {
    const message = createBaseRegisterResponse();
    message.success = object.success ?? false;
    return message;
  },
};

function createBaseUnregisterRequest(): UnregisterRequest {
  return { sipUri: "" };
}

export const UnregisterRequest: MessageFns<UnregisterRequest> = {
  encode(message: UnregisterRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sipUri !== "") {
      writer.uint32(10).string(message.sipUri);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UnregisterRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnregisterRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sipUri = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnregisterRequest {
    return {
      sipUri: isSet(object.sipUri)
        ? globalThis.String(object.sipUri)
        : isSet(object.sip_uri)
        ? globalThis.String(object.sip_uri)
        : "",
    };
  },

  toJSON(message: UnregisterRequest): unknown {
    const obj: any = {};
    if (message.sipUri !== "") {
      obj.sipUri = message.sipUri;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnregisterRequest>, I>>(base?: I): UnregisterRequest {
    return UnregisterRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnregisterRequest>, I>>(object: I): UnregisterRequest {
    const message = createBaseUnregisterRequest();
    message.sipUri = object.sipUri ?? "";
    return message;
  },
};

function createBaseUnregisterResponse(): UnregisterResponse {
  return { success: false };
}

export const UnregisterResponse: MessageFns<UnregisterResponse> = {
  encode(message: UnregisterResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UnregisterResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnregisterResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnregisterResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: UnregisterResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnregisterResponse>, I>>(base?: I): UnregisterResponse {
    return UnregisterResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnregisterResponse>, I>>(object: I): UnregisterResponse {
    const message = createBaseUnregisterResponse();
    message.success = object.success ?? false;
    return message;
  },
};

function createBaseLookupContactRequest(): LookupContactRequest {
  return { sipUri: "" };
}

export const LookupContactRequest: MessageFns<LookupContactRequest> = {
  encode(message: LookupContactRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sipUri !== "") {
      writer.uint32(10).string(message.sipUri);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LookupContactRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLookupContactRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sipUri = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LookupContactRequest {
    return {
      sipUri: isSet(object.sipUri)
        ? globalThis.String(object.sipUri)
        : isSet(object.sip_uri)
        ? globalThis.String(object.sip_uri)
        : "",
    };
  },

  toJSON(message: LookupContactRequest): unknown {
    const obj: any = {};
    if (message.sipUri !== "") {
      obj.sipUri = message.sipUri;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LookupContactRequest>, I>>(base?: I): LookupContactRequest {
    return LookupContactRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LookupContactRequest>, I>>(object: I): LookupContactRequest {
    const message = createBaseLookupContactRequest();
    message.sipUri = object.sipUri ?? "";
    return message;
  },
};

function createBaseLookupContactResponse(): LookupContactResponse {
  return { contactUris: [] };
}

export const LookupContactResponse: MessageFns<LookupContactResponse> = {
  encode(message: LookupContactResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.contactUris) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LookupContactResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLookupContactResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.contactUris.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LookupContactResponse {
    return {
      contactUris: globalThis.Array.isArray(object?.contactUris)
        ? object.contactUris.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.contact_uris)
        ? object.contact_uris.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: LookupContactResponse): unknown {
    const obj: any = {};
    if (message.contactUris?.length) {
      obj.contactUris = message.contactUris;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LookupContactResponse>, I>>(base?: I): LookupContactResponse {
    return LookupContactResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LookupContactResponse>, I>>(object: I): LookupContactResponse {
    const message = createBaseLookupContactResponse();
    message.contactUris = object.contactUris?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
