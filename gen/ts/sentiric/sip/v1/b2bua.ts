// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/sip/v1/b2bua.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.sip.v1";

export interface InitiateCallRequest {
  callId: string;
  fromUri: string;
  toUri: string;
}

export interface InitiateCallResponse {
  success: boolean;
  newCallId: string;
}

export interface TransferCallRequest {
  existingCallId: string;
  transferTargetUri: string;
}

export interface TransferCallResponse {
  success: boolean;
}

function createBaseInitiateCallRequest(): InitiateCallRequest {
  return { callId: "", fromUri: "", toUri: "" };
}

export const InitiateCallRequest: MessageFns<InitiateCallRequest> = {
  encode(message: InitiateCallRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.callId !== "") {
      writer.uint32(10).string(message.callId);
    }
    if (message.fromUri !== "") {
      writer.uint32(18).string(message.fromUri);
    }
    if (message.toUri !== "") {
      writer.uint32(26).string(message.toUri);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InitiateCallRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInitiateCallRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.callId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fromUri = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.toUri = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InitiateCallRequest {
    return {
      callId: isSet(object.callId)
        ? globalThis.String(object.callId)
        : isSet(object.call_id)
        ? globalThis.String(object.call_id)
        : "",
      fromUri: isSet(object.fromUri)
        ? globalThis.String(object.fromUri)
        : isSet(object.from_uri)
        ? globalThis.String(object.from_uri)
        : "",
      toUri: isSet(object.toUri)
        ? globalThis.String(object.toUri)
        : isSet(object.to_uri)
        ? globalThis.String(object.to_uri)
        : "",
    };
  },

  toJSON(message: InitiateCallRequest): unknown {
    const obj: any = {};
    if (message.callId !== "") {
      obj.callId = message.callId;
    }
    if (message.fromUri !== "") {
      obj.fromUri = message.fromUri;
    }
    if (message.toUri !== "") {
      obj.toUri = message.toUri;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InitiateCallRequest>, I>>(base?: I): InitiateCallRequest {
    return InitiateCallRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InitiateCallRequest>, I>>(object: I): InitiateCallRequest {
    const message = createBaseInitiateCallRequest();
    message.callId = object.callId ?? "";
    message.fromUri = object.fromUri ?? "";
    message.toUri = object.toUri ?? "";
    return message;
  },
};

function createBaseInitiateCallResponse(): InitiateCallResponse {
  return { success: false, newCallId: "" };
}

export const InitiateCallResponse: MessageFns<InitiateCallResponse> = {
  encode(message: InitiateCallResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.newCallId !== "") {
      writer.uint32(18).string(message.newCallId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InitiateCallResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInitiateCallResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.newCallId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InitiateCallResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      newCallId: isSet(object.newCallId)
        ? globalThis.String(object.newCallId)
        : isSet(object.new_call_id)
        ? globalThis.String(object.new_call_id)
        : "",
    };
  },

  toJSON(message: InitiateCallResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.newCallId !== "") {
      obj.newCallId = message.newCallId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InitiateCallResponse>, I>>(base?: I): InitiateCallResponse {
    return InitiateCallResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InitiateCallResponse>, I>>(object: I): InitiateCallResponse {
    const message = createBaseInitiateCallResponse();
    message.success = object.success ?? false;
    message.newCallId = object.newCallId ?? "";
    return message;
  },
};

function createBaseTransferCallRequest(): TransferCallRequest {
  return { existingCallId: "", transferTargetUri: "" };
}

export const TransferCallRequest: MessageFns<TransferCallRequest> = {
  encode(message: TransferCallRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.existingCallId !== "") {
      writer.uint32(10).string(message.existingCallId);
    }
    if (message.transferTargetUri !== "") {
      writer.uint32(18).string(message.transferTargetUri);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TransferCallRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTransferCallRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.existingCallId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.transferTargetUri = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TransferCallRequest {
    return {
      existingCallId: isSet(object.existingCallId)
        ? globalThis.String(object.existingCallId)
        : isSet(object.existing_call_id)
        ? globalThis.String(object.existing_call_id)
        : "",
      transferTargetUri: isSet(object.transferTargetUri)
        ? globalThis.String(object.transferTargetUri)
        : isSet(object.transfer_target_uri)
        ? globalThis.String(object.transfer_target_uri)
        : "",
    };
  },

  toJSON(message: TransferCallRequest): unknown {
    const obj: any = {};
    if (message.existingCallId !== "") {
      obj.existingCallId = message.existingCallId;
    }
    if (message.transferTargetUri !== "") {
      obj.transferTargetUri = message.transferTargetUri;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TransferCallRequest>, I>>(base?: I): TransferCallRequest {
    return TransferCallRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TransferCallRequest>, I>>(object: I): TransferCallRequest {
    const message = createBaseTransferCallRequest();
    message.existingCallId = object.existingCallId ?? "";
    message.transferTargetUri = object.transferTargetUri ?? "";
    return message;
  },
};

function createBaseTransferCallResponse(): TransferCallResponse {
  return { success: false };
}

export const TransferCallResponse: MessageFns<TransferCallResponse> = {
  encode(message: TransferCallResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TransferCallResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTransferCallResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TransferCallResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: TransferCallResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TransferCallResponse>, I>>(base?: I): TransferCallResponse {
    return TransferCallResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TransferCallResponse>, I>>(object: I): TransferCallResponse {
    const message = createBaseTransferCallResponse();
    message.success = object.success ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
