// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/llm/v1/llama.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Struct } from "../../../google/protobuf/struct";

export const protobufPackage = "sentiric.llm.v1";

/** Mesaj Adı: GenerateStreamRequest (RPC adı ile aynı olmalı) */
export interface GenerateStreamRequest {
  systemPrompt: string;
  userPrompt: string;
  ragContext?: string | undefined;
  history: ConversationTurn[];
  params?:
    | GenerationParams
    | undefined;
  /**
   * [YENİ] LoRA Adaptörünü belirtir. Sadece dosya adı, uzantısız.
   * Örn: "customer_service_finetune"
   */
  loraAdapterId?: string | undefined;
}

export interface GenerateStreamResponse {
  /**
   * [DEĞİŞİKLİK] String yerine bytes kullanıyoruz.
   * LLM tokenları bazen yarım UTF-8 karakteri içerebilir.
   * Protobuf string tipi UTF-8 doğrulaması yaptığı için hata veriyordu.
   */
  token?: Uint8Array | undefined;
  finishDetails?: FinishDetails | undefined;
}

/** --- Yardımcılar (Değişmedi) --- */
export interface ConversationTurn {
  role: string;
  content: string;
}

export interface GenerationParams {
  maxNewTokens?: number | undefined;
  temperature?: number | undefined;
  topK?: number | undefined;
  topP?: number | undefined;
  repetitionPenalty?: number | undefined;
  stopSequences: string[];
  seed?: string | undefined;
  engineSpecificParams?: { [key: string]: any } | undefined;
}

export interface FinishDetails {
  finishReason: string;
  promptTokens: number;
  completionTokens: number;
}

function createBaseGenerateStreamRequest(): GenerateStreamRequest {
  return {
    systemPrompt: "",
    userPrompt: "",
    ragContext: undefined,
    history: [],
    params: undefined,
    loraAdapterId: undefined,
  };
}

export const GenerateStreamRequest: MessageFns<GenerateStreamRequest> = {
  encode(message: GenerateStreamRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.systemPrompt !== "") {
      writer.uint32(10).string(message.systemPrompt);
    }
    if (message.userPrompt !== "") {
      writer.uint32(18).string(message.userPrompt);
    }
    if (message.ragContext !== undefined) {
      writer.uint32(26).string(message.ragContext);
    }
    for (const v of message.history) {
      ConversationTurn.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.params !== undefined) {
      GenerationParams.encode(message.params, writer.uint32(42).fork()).join();
    }
    if (message.loraAdapterId !== undefined) {
      writer.uint32(50).string(message.loraAdapterId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateStreamRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateStreamRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.systemPrompt = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userPrompt = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ragContext = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.history.push(ConversationTurn.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.params = GenerationParams.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.loraAdapterId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateStreamRequest {
    return {
      systemPrompt: isSet(object.systemPrompt)
        ? globalThis.String(object.systemPrompt)
        : isSet(object.system_prompt)
        ? globalThis.String(object.system_prompt)
        : "",
      userPrompt: isSet(object.userPrompt)
        ? globalThis.String(object.userPrompt)
        : isSet(object.user_prompt)
        ? globalThis.String(object.user_prompt)
        : "",
      ragContext: isSet(object.ragContext)
        ? globalThis.String(object.ragContext)
        : isSet(object.rag_context)
        ? globalThis.String(object.rag_context)
        : undefined,
      history: globalThis.Array.isArray(object?.history)
        ? object.history.map((e: any) => ConversationTurn.fromJSON(e))
        : [],
      params: isSet(object.params) ? GenerationParams.fromJSON(object.params) : undefined,
      loraAdapterId: isSet(object.loraAdapterId)
        ? globalThis.String(object.loraAdapterId)
        : isSet(object.lora_adapter_id)
        ? globalThis.String(object.lora_adapter_id)
        : undefined,
    };
  },

  toJSON(message: GenerateStreamRequest): unknown {
    const obj: any = {};
    if (message.systemPrompt !== "") {
      obj.systemPrompt = message.systemPrompt;
    }
    if (message.userPrompt !== "") {
      obj.userPrompt = message.userPrompt;
    }
    if (message.ragContext !== undefined) {
      obj.ragContext = message.ragContext;
    }
    if (message.history?.length) {
      obj.history = message.history.map((e) => ConversationTurn.toJSON(e));
    }
    if (message.params !== undefined) {
      obj.params = GenerationParams.toJSON(message.params);
    }
    if (message.loraAdapterId !== undefined) {
      obj.loraAdapterId = message.loraAdapterId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenerateStreamRequest>, I>>(base?: I): GenerateStreamRequest {
    return GenerateStreamRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenerateStreamRequest>, I>>(object: I): GenerateStreamRequest {
    const message = createBaseGenerateStreamRequest();
    message.systemPrompt = object.systemPrompt ?? "";
    message.userPrompt = object.userPrompt ?? "";
    message.ragContext = object.ragContext ?? undefined;
    message.history = object.history?.map((e) => ConversationTurn.fromPartial(e)) || [];
    message.params = (object.params !== undefined && object.params !== null)
      ? GenerationParams.fromPartial(object.params)
      : undefined;
    message.loraAdapterId = object.loraAdapterId ?? undefined;
    return message;
  },
};

function createBaseGenerateStreamResponse(): GenerateStreamResponse {
  return { token: undefined, finishDetails: undefined };
}

export const GenerateStreamResponse: MessageFns<GenerateStreamResponse> = {
  encode(message: GenerateStreamResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== undefined) {
      writer.uint32(10).bytes(message.token);
    }
    if (message.finishDetails !== undefined) {
      FinishDetails.encode(message.finishDetails, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateStreamResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateStreamResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.finishDetails = FinishDetails.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateStreamResponse {
    return {
      token: isSet(object.token) ? bytesFromBase64(object.token) : undefined,
      finishDetails: isSet(object.finishDetails)
        ? FinishDetails.fromJSON(object.finishDetails)
        : isSet(object.finish_details)
        ? FinishDetails.fromJSON(object.finish_details)
        : undefined,
    };
  },

  toJSON(message: GenerateStreamResponse): unknown {
    const obj: any = {};
    if (message.token !== undefined) {
      obj.token = base64FromBytes(message.token);
    }
    if (message.finishDetails !== undefined) {
      obj.finishDetails = FinishDetails.toJSON(message.finishDetails);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenerateStreamResponse>, I>>(base?: I): GenerateStreamResponse {
    return GenerateStreamResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenerateStreamResponse>, I>>(object: I): GenerateStreamResponse {
    const message = createBaseGenerateStreamResponse();
    message.token = object.token ?? undefined;
    message.finishDetails = (object.finishDetails !== undefined && object.finishDetails !== null)
      ? FinishDetails.fromPartial(object.finishDetails)
      : undefined;
    return message;
  },
};

function createBaseConversationTurn(): ConversationTurn {
  return { role: "", content: "" };
}

export const ConversationTurn: MessageFns<ConversationTurn> = {
  encode(message: ConversationTurn, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.role !== "") {
      writer.uint32(10).string(message.role);
    }
    if (message.content !== "") {
      writer.uint32(18).string(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConversationTurn {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConversationTurn();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.role = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConversationTurn {
    return {
      role: isSet(object.role) ? globalThis.String(object.role) : "",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
    };
  },

  toJSON(message: ConversationTurn): unknown {
    const obj: any = {};
    if (message.role !== "") {
      obj.role = message.role;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConversationTurn>, I>>(base?: I): ConversationTurn {
    return ConversationTurn.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConversationTurn>, I>>(object: I): ConversationTurn {
    const message = createBaseConversationTurn();
    message.role = object.role ?? "";
    message.content = object.content ?? "";
    return message;
  },
};

function createBaseGenerationParams(): GenerationParams {
  return {
    maxNewTokens: undefined,
    temperature: undefined,
    topK: undefined,
    topP: undefined,
    repetitionPenalty: undefined,
    stopSequences: [],
    seed: undefined,
    engineSpecificParams: undefined,
  };
}

export const GenerationParams: MessageFns<GenerationParams> = {
  encode(message: GenerationParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maxNewTokens !== undefined) {
      writer.uint32(8).int32(message.maxNewTokens);
    }
    if (message.temperature !== undefined) {
      writer.uint32(21).float(message.temperature);
    }
    if (message.topK !== undefined) {
      writer.uint32(24).int32(message.topK);
    }
    if (message.topP !== undefined) {
      writer.uint32(37).float(message.topP);
    }
    if (message.repetitionPenalty !== undefined) {
      writer.uint32(45).float(message.repetitionPenalty);
    }
    for (const v of message.stopSequences) {
      writer.uint32(50).string(v!);
    }
    if (message.seed !== undefined) {
      writer.uint32(56).int64(message.seed);
    }
    if (message.engineSpecificParams !== undefined) {
      Struct.encode(Struct.wrap(message.engineSpecificParams), writer.uint32(162).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerationParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerationParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.maxNewTokens = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.temperature = reader.float();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.topK = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 37) {
            break;
          }

          message.topP = reader.float();
          continue;
        }
        case 5: {
          if (tag !== 45) {
            break;
          }

          message.repetitionPenalty = reader.float();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.stopSequences.push(reader.string());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.seed = reader.int64().toString();
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          message.engineSpecificParams = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerationParams {
    return {
      maxNewTokens: isSet(object.maxNewTokens)
        ? globalThis.Number(object.maxNewTokens)
        : isSet(object.max_new_tokens)
        ? globalThis.Number(object.max_new_tokens)
        : undefined,
      temperature: isSet(object.temperature) ? globalThis.Number(object.temperature) : undefined,
      topK: isSet(object.topK)
        ? globalThis.Number(object.topK)
        : isSet(object.top_k)
        ? globalThis.Number(object.top_k)
        : undefined,
      topP: isSet(object.topP)
        ? globalThis.Number(object.topP)
        : isSet(object.top_p)
        ? globalThis.Number(object.top_p)
        : undefined,
      repetitionPenalty: isSet(object.repetitionPenalty)
        ? globalThis.Number(object.repetitionPenalty)
        : isSet(object.repetition_penalty)
        ? globalThis.Number(object.repetition_penalty)
        : undefined,
      stopSequences: globalThis.Array.isArray(object?.stopSequences)
        ? object.stopSequences.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.stop_sequences)
        ? object.stop_sequences.map((e: any) => globalThis.String(e))
        : [],
      seed: isSet(object.seed) ? globalThis.String(object.seed) : undefined,
      engineSpecificParams: isObject(object.engineSpecificParams)
        ? object.engineSpecificParams
        : isObject(object.engine_specific_params)
        ? object.engine_specific_params
        : undefined,
    };
  },

  toJSON(message: GenerationParams): unknown {
    const obj: any = {};
    if (message.maxNewTokens !== undefined) {
      obj.maxNewTokens = Math.round(message.maxNewTokens);
    }
    if (message.temperature !== undefined) {
      obj.temperature = message.temperature;
    }
    if (message.topK !== undefined) {
      obj.topK = Math.round(message.topK);
    }
    if (message.topP !== undefined) {
      obj.topP = message.topP;
    }
    if (message.repetitionPenalty !== undefined) {
      obj.repetitionPenalty = message.repetitionPenalty;
    }
    if (message.stopSequences?.length) {
      obj.stopSequences = message.stopSequences;
    }
    if (message.seed !== undefined) {
      obj.seed = message.seed;
    }
    if (message.engineSpecificParams !== undefined) {
      obj.engineSpecificParams = message.engineSpecificParams;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenerationParams>, I>>(base?: I): GenerationParams {
    return GenerationParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenerationParams>, I>>(object: I): GenerationParams {
    const message = createBaseGenerationParams();
    message.maxNewTokens = object.maxNewTokens ?? undefined;
    message.temperature = object.temperature ?? undefined;
    message.topK = object.topK ?? undefined;
    message.topP = object.topP ?? undefined;
    message.repetitionPenalty = object.repetitionPenalty ?? undefined;
    message.stopSequences = object.stopSequences?.map((e) => e) || [];
    message.seed = object.seed ?? undefined;
    message.engineSpecificParams = object.engineSpecificParams ?? undefined;
    return message;
  },
};

function createBaseFinishDetails(): FinishDetails {
  return { finishReason: "", promptTokens: 0, completionTokens: 0 };
}

export const FinishDetails: MessageFns<FinishDetails> = {
  encode(message: FinishDetails, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.finishReason !== "") {
      writer.uint32(10).string(message.finishReason);
    }
    if (message.promptTokens !== 0) {
      writer.uint32(16).int32(message.promptTokens);
    }
    if (message.completionTokens !== 0) {
      writer.uint32(24).int32(message.completionTokens);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FinishDetails {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFinishDetails();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.finishReason = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.promptTokens = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.completionTokens = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FinishDetails {
    return {
      finishReason: isSet(object.finishReason)
        ? globalThis.String(object.finishReason)
        : isSet(object.finish_reason)
        ? globalThis.String(object.finish_reason)
        : "",
      promptTokens: isSet(object.promptTokens)
        ? globalThis.Number(object.promptTokens)
        : isSet(object.prompt_tokens)
        ? globalThis.Number(object.prompt_tokens)
        : 0,
      completionTokens: isSet(object.completionTokens)
        ? globalThis.Number(object.completionTokens)
        : isSet(object.completion_tokens)
        ? globalThis.Number(object.completion_tokens)
        : 0,
    };
  },

  toJSON(message: FinishDetails): unknown {
    const obj: any = {};
    if (message.finishReason !== "") {
      obj.finishReason = message.finishReason;
    }
    if (message.promptTokens !== 0) {
      obj.promptTokens = Math.round(message.promptTokens);
    }
    if (message.completionTokens !== 0) {
      obj.completionTokens = Math.round(message.completionTokens);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FinishDetails>, I>>(base?: I): FinishDetails {
    return FinishDetails.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FinishDetails>, I>>(object: I): FinishDetails {
    const message = createBaseFinishDetails();
    message.finishReason = object.finishReason ?? "";
    message.promptTokens = object.promptTokens ?? 0;
    message.completionTokens = object.completionTokens ?? 0;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
