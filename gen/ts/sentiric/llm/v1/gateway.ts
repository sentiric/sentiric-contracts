// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/llm/v1/gateway.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { GenerateStreamRequest, GenerateStreamResponse } from "./llama";

export const protobufPackage = "sentiric.llm.v1";

export interface GenerateDialogStreamRequest {
  modelSelector: string;
  tenantId: string;
  /** DÜZELTME: Tip adı 'GenerateStreamRequest' olarak güncellendi (llama.proto ile uyumlu) */
  llamaRequest?: GenerateStreamRequest | undefined;
}

export interface GenerateDialogStreamResponse {
  /** DÜZELTME: Tip adı 'GenerateStreamResponse' olarak güncellendi */
  llamaResponse?: GenerateStreamResponse | undefined;
}

function createBaseGenerateDialogStreamRequest(): GenerateDialogStreamRequest {
  return { modelSelector: "", tenantId: "", llamaRequest: undefined };
}

export const GenerateDialogStreamRequest: MessageFns<GenerateDialogStreamRequest> = {
  encode(message: GenerateDialogStreamRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.modelSelector !== "") {
      writer.uint32(10).string(message.modelSelector);
    }
    if (message.tenantId !== "") {
      writer.uint32(18).string(message.tenantId);
    }
    if (message.llamaRequest !== undefined) {
      GenerateStreamRequest.encode(message.llamaRequest, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateDialogStreamRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateDialogStreamRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.modelSelector = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tenantId = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.llamaRequest = GenerateStreamRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateDialogStreamRequest {
    return {
      modelSelector: isSet(object.modelSelector)
        ? globalThis.String(object.modelSelector)
        : isSet(object.model_selector)
        ? globalThis.String(object.model_selector)
        : "",
      tenantId: isSet(object.tenantId)
        ? globalThis.String(object.tenantId)
        : isSet(object.tenant_id)
        ? globalThis.String(object.tenant_id)
        : "",
      llamaRequest: isSet(object.llamaRequest)
        ? GenerateStreamRequest.fromJSON(object.llamaRequest)
        : isSet(object.llama_request)
        ? GenerateStreamRequest.fromJSON(object.llama_request)
        : undefined,
    };
  },

  toJSON(message: GenerateDialogStreamRequest): unknown {
    const obj: any = {};
    if (message.modelSelector !== "") {
      obj.modelSelector = message.modelSelector;
    }
    if (message.tenantId !== "") {
      obj.tenantId = message.tenantId;
    }
    if (message.llamaRequest !== undefined) {
      obj.llamaRequest = GenerateStreamRequest.toJSON(message.llamaRequest);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenerateDialogStreamRequest>, I>>(base?: I): GenerateDialogStreamRequest {
    return GenerateDialogStreamRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenerateDialogStreamRequest>, I>>(object: I): GenerateDialogStreamRequest {
    const message = createBaseGenerateDialogStreamRequest();
    message.modelSelector = object.modelSelector ?? "";
    message.tenantId = object.tenantId ?? "";
    message.llamaRequest = (object.llamaRequest !== undefined && object.llamaRequest !== null)
      ? GenerateStreamRequest.fromPartial(object.llamaRequest)
      : undefined;
    return message;
  },
};

function createBaseGenerateDialogStreamResponse(): GenerateDialogStreamResponse {
  return { llamaResponse: undefined };
}

export const GenerateDialogStreamResponse: MessageFns<GenerateDialogStreamResponse> = {
  encode(message: GenerateDialogStreamResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.llamaResponse !== undefined) {
      GenerateStreamResponse.encode(message.llamaResponse, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateDialogStreamResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateDialogStreamResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.llamaResponse = GenerateStreamResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateDialogStreamResponse {
    return {
      llamaResponse: isSet(object.llamaResponse)
        ? GenerateStreamResponse.fromJSON(object.llamaResponse)
        : isSet(object.llama_response)
        ? GenerateStreamResponse.fromJSON(object.llama_response)
        : undefined,
    };
  },

  toJSON(message: GenerateDialogStreamResponse): unknown {
    const obj: any = {};
    if (message.llamaResponse !== undefined) {
      obj.llamaResponse = GenerateStreamResponse.toJSON(message.llamaResponse);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenerateDialogStreamResponse>, I>>(base?: I): GenerateDialogStreamResponse {
    return GenerateDialogStreamResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenerateDialogStreamResponse>, I>>(object: I): GenerateDialogStreamResponse {
    const message = createBaseGenerateDialogStreamResponse();
    message.llamaResponse = (object.llamaResponse !== undefined && object.llamaResponse !== null)
      ? GenerateStreamResponse.fromPartial(object.llamaResponse)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
