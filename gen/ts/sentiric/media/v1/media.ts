// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/media/v1/media.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.media.v1";

export interface StreamAudioToCallRequest {
  /** Hangi çağrıya ses basılacak? */
  callId: string;
  /** Ham PCM (16kHz Mono) veya Opus verisi. */
  audioChunk: Uint8Array;
}

export interface StreamAudioToCallResponse {
  success: boolean;
  /** Olası hata mesajları veya playback durumu (örn: "buffer_underrun") */
  errorMessage: string;
}

export interface AllocatePortRequest {
  callId: string;
}

export interface AllocatePortResponse {
  rtpPort: number;
}

export interface ReleasePortRequest {
  rtpPort: number;
}

export interface ReleasePortResponse {
  success: boolean;
}

export interface PlayAudioRequest {
  audioUri: string;
  serverRtpPort: number;
  rtpTargetAddr: string;
}

export interface PlayAudioResponse {
  success: boolean;
  message: string;
}

export interface RecordAudioRequest {
  serverRtpPort: number;
  targetSampleRate?: number | undefined;
}

export interface RecordAudioResponse {
  audioData: Uint8Array;
  mediaType: string;
}

export interface StartRecordingRequest {
  serverRtpPort: number;
  outputUri: string;
  sampleRate?: number | undefined;
  format?: string | undefined;
  callId: string;
  traceId: string;
}

export interface StartRecordingResponse {
  success: boolean;
}

export interface StopRecordingRequest {
  serverRtpPort: number;
}

export interface StopRecordingResponse {
  success: boolean;
}

function createBaseStreamAudioToCallRequest(): StreamAudioToCallRequest {
  return { callId: "", audioChunk: new Uint8Array(0) };
}

export const StreamAudioToCallRequest: MessageFns<StreamAudioToCallRequest> = {
  encode(message: StreamAudioToCallRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.callId !== "") {
      writer.uint32(10).string(message.callId);
    }
    if (message.audioChunk.length !== 0) {
      writer.uint32(18).bytes(message.audioChunk);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamAudioToCallRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamAudioToCallRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.callId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.audioChunk = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamAudioToCallRequest {
    return {
      callId: isSet(object.callId)
        ? globalThis.String(object.callId)
        : isSet(object.call_id)
        ? globalThis.String(object.call_id)
        : "",
      audioChunk: isSet(object.audioChunk)
        ? bytesFromBase64(object.audioChunk)
        : isSet(object.audio_chunk)
        ? bytesFromBase64(object.audio_chunk)
        : new Uint8Array(0),
    };
  },

  toJSON(message: StreamAudioToCallRequest): unknown {
    const obj: any = {};
    if (message.callId !== "") {
      obj.callId = message.callId;
    }
    if (message.audioChunk.length !== 0) {
      obj.audioChunk = base64FromBytes(message.audioChunk);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamAudioToCallRequest>, I>>(base?: I): StreamAudioToCallRequest {
    return StreamAudioToCallRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamAudioToCallRequest>, I>>(object: I): StreamAudioToCallRequest {
    const message = createBaseStreamAudioToCallRequest();
    message.callId = object.callId ?? "";
    message.audioChunk = object.audioChunk ?? new Uint8Array(0);
    return message;
  },
};

function createBaseStreamAudioToCallResponse(): StreamAudioToCallResponse {
  return { success: false, errorMessage: "" };
}

export const StreamAudioToCallResponse: MessageFns<StreamAudioToCallResponse> = {
  encode(message: StreamAudioToCallResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.errorMessage !== "") {
      writer.uint32(18).string(message.errorMessage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamAudioToCallResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamAudioToCallResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamAudioToCallResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : "",
    };
  },

  toJSON(message: StreamAudioToCallResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamAudioToCallResponse>, I>>(base?: I): StreamAudioToCallResponse {
    return StreamAudioToCallResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamAudioToCallResponse>, I>>(object: I): StreamAudioToCallResponse {
    const message = createBaseStreamAudioToCallResponse();
    message.success = object.success ?? false;
    message.errorMessage = object.errorMessage ?? "";
    return message;
  },
};

function createBaseAllocatePortRequest(): AllocatePortRequest {
  return { callId: "" };
}

export const AllocatePortRequest: MessageFns<AllocatePortRequest> = {
  encode(message: AllocatePortRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.callId !== "") {
      writer.uint32(10).string(message.callId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AllocatePortRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAllocatePortRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.callId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AllocatePortRequest {
    return {
      callId: isSet(object.callId)
        ? globalThis.String(object.callId)
        : isSet(object.call_id)
        ? globalThis.String(object.call_id)
        : "",
    };
  },

  toJSON(message: AllocatePortRequest): unknown {
    const obj: any = {};
    if (message.callId !== "") {
      obj.callId = message.callId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AllocatePortRequest>, I>>(base?: I): AllocatePortRequest {
    return AllocatePortRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AllocatePortRequest>, I>>(object: I): AllocatePortRequest {
    const message = createBaseAllocatePortRequest();
    message.callId = object.callId ?? "";
    return message;
  },
};

function createBaseAllocatePortResponse(): AllocatePortResponse {
  return { rtpPort: 0 };
}

export const AllocatePortResponse: MessageFns<AllocatePortResponse> = {
  encode(message: AllocatePortResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rtpPort !== 0) {
      writer.uint32(8).uint32(message.rtpPort);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AllocatePortResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAllocatePortResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.rtpPort = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AllocatePortResponse {
    return {
      rtpPort: isSet(object.rtpPort)
        ? globalThis.Number(object.rtpPort)
        : isSet(object.rtp_port)
        ? globalThis.Number(object.rtp_port)
        : 0,
    };
  },

  toJSON(message: AllocatePortResponse): unknown {
    const obj: any = {};
    if (message.rtpPort !== 0) {
      obj.rtpPort = Math.round(message.rtpPort);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AllocatePortResponse>, I>>(base?: I): AllocatePortResponse {
    return AllocatePortResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AllocatePortResponse>, I>>(object: I): AllocatePortResponse {
    const message = createBaseAllocatePortResponse();
    message.rtpPort = object.rtpPort ?? 0;
    return message;
  },
};

function createBaseReleasePortRequest(): ReleasePortRequest {
  return { rtpPort: 0 };
}

export const ReleasePortRequest: MessageFns<ReleasePortRequest> = {
  encode(message: ReleasePortRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rtpPort !== 0) {
      writer.uint32(8).uint32(message.rtpPort);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReleasePortRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReleasePortRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.rtpPort = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReleasePortRequest {
    return {
      rtpPort: isSet(object.rtpPort)
        ? globalThis.Number(object.rtpPort)
        : isSet(object.rtp_port)
        ? globalThis.Number(object.rtp_port)
        : 0,
    };
  },

  toJSON(message: ReleasePortRequest): unknown {
    const obj: any = {};
    if (message.rtpPort !== 0) {
      obj.rtpPort = Math.round(message.rtpPort);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReleasePortRequest>, I>>(base?: I): ReleasePortRequest {
    return ReleasePortRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReleasePortRequest>, I>>(object: I): ReleasePortRequest {
    const message = createBaseReleasePortRequest();
    message.rtpPort = object.rtpPort ?? 0;
    return message;
  },
};

function createBaseReleasePortResponse(): ReleasePortResponse {
  return { success: false };
}

export const ReleasePortResponse: MessageFns<ReleasePortResponse> = {
  encode(message: ReleasePortResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReleasePortResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReleasePortResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReleasePortResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: ReleasePortResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReleasePortResponse>, I>>(base?: I): ReleasePortResponse {
    return ReleasePortResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReleasePortResponse>, I>>(object: I): ReleasePortResponse {
    const message = createBaseReleasePortResponse();
    message.success = object.success ?? false;
    return message;
  },
};

function createBasePlayAudioRequest(): PlayAudioRequest {
  return { audioUri: "", serverRtpPort: 0, rtpTargetAddr: "" };
}

export const PlayAudioRequest: MessageFns<PlayAudioRequest> = {
  encode(message: PlayAudioRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.audioUri !== "") {
      writer.uint32(10).string(message.audioUri);
    }
    if (message.serverRtpPort !== 0) {
      writer.uint32(16).uint32(message.serverRtpPort);
    }
    if (message.rtpTargetAddr !== "") {
      writer.uint32(26).string(message.rtpTargetAddr);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayAudioRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayAudioRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.audioUri = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.serverRtpPort = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.rtpTargetAddr = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayAudioRequest {
    return {
      audioUri: isSet(object.audioUri)
        ? globalThis.String(object.audioUri)
        : isSet(object.audio_uri)
        ? globalThis.String(object.audio_uri)
        : "",
      serverRtpPort: isSet(object.serverRtpPort)
        ? globalThis.Number(object.serverRtpPort)
        : isSet(object.server_rtp_port)
        ? globalThis.Number(object.server_rtp_port)
        : 0,
      rtpTargetAddr: isSet(object.rtpTargetAddr)
        ? globalThis.String(object.rtpTargetAddr)
        : isSet(object.rtp_target_addr)
        ? globalThis.String(object.rtp_target_addr)
        : "",
    };
  },

  toJSON(message: PlayAudioRequest): unknown {
    const obj: any = {};
    if (message.audioUri !== "") {
      obj.audioUri = message.audioUri;
    }
    if (message.serverRtpPort !== 0) {
      obj.serverRtpPort = Math.round(message.serverRtpPort);
    }
    if (message.rtpTargetAddr !== "") {
      obj.rtpTargetAddr = message.rtpTargetAddr;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayAudioRequest>, I>>(base?: I): PlayAudioRequest {
    return PlayAudioRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayAudioRequest>, I>>(object: I): PlayAudioRequest {
    const message = createBasePlayAudioRequest();
    message.audioUri = object.audioUri ?? "";
    message.serverRtpPort = object.serverRtpPort ?? 0;
    message.rtpTargetAddr = object.rtpTargetAddr ?? "";
    return message;
  },
};

function createBasePlayAudioResponse(): PlayAudioResponse {
  return { success: false, message: "" };
}

export const PlayAudioResponse: MessageFns<PlayAudioResponse> = {
  encode(message: PlayAudioResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayAudioResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayAudioResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayAudioResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: PlayAudioResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayAudioResponse>, I>>(base?: I): PlayAudioResponse {
    return PlayAudioResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayAudioResponse>, I>>(object: I): PlayAudioResponse {
    const message = createBasePlayAudioResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseRecordAudioRequest(): RecordAudioRequest {
  return { serverRtpPort: 0, targetSampleRate: undefined };
}

export const RecordAudioRequest: MessageFns<RecordAudioRequest> = {
  encode(message: RecordAudioRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serverRtpPort !== 0) {
      writer.uint32(8).uint32(message.serverRtpPort);
    }
    if (message.targetSampleRate !== undefined) {
      writer.uint32(16).uint32(message.targetSampleRate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RecordAudioRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecordAudioRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.serverRtpPort = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.targetSampleRate = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RecordAudioRequest {
    return {
      serverRtpPort: isSet(object.serverRtpPort)
        ? globalThis.Number(object.serverRtpPort)
        : isSet(object.server_rtp_port)
        ? globalThis.Number(object.server_rtp_port)
        : 0,
      targetSampleRate: isSet(object.targetSampleRate)
        ? globalThis.Number(object.targetSampleRate)
        : isSet(object.target_sample_rate)
        ? globalThis.Number(object.target_sample_rate)
        : undefined,
    };
  },

  toJSON(message: RecordAudioRequest): unknown {
    const obj: any = {};
    if (message.serverRtpPort !== 0) {
      obj.serverRtpPort = Math.round(message.serverRtpPort);
    }
    if (message.targetSampleRate !== undefined) {
      obj.targetSampleRate = Math.round(message.targetSampleRate);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RecordAudioRequest>, I>>(base?: I): RecordAudioRequest {
    return RecordAudioRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RecordAudioRequest>, I>>(object: I): RecordAudioRequest {
    const message = createBaseRecordAudioRequest();
    message.serverRtpPort = object.serverRtpPort ?? 0;
    message.targetSampleRate = object.targetSampleRate ?? undefined;
    return message;
  },
};

function createBaseRecordAudioResponse(): RecordAudioResponse {
  return { audioData: new Uint8Array(0), mediaType: "" };
}

export const RecordAudioResponse: MessageFns<RecordAudioResponse> = {
  encode(message: RecordAudioResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.audioData.length !== 0) {
      writer.uint32(10).bytes(message.audioData);
    }
    if (message.mediaType !== "") {
      writer.uint32(18).string(message.mediaType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RecordAudioResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecordAudioResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.audioData = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.mediaType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RecordAudioResponse {
    return {
      audioData: isSet(object.audioData)
        ? bytesFromBase64(object.audioData)
        : isSet(object.audio_data)
        ? bytesFromBase64(object.audio_data)
        : new Uint8Array(0),
      mediaType: isSet(object.mediaType)
        ? globalThis.String(object.mediaType)
        : isSet(object.media_type)
        ? globalThis.String(object.media_type)
        : "",
    };
  },

  toJSON(message: RecordAudioResponse): unknown {
    const obj: any = {};
    if (message.audioData.length !== 0) {
      obj.audioData = base64FromBytes(message.audioData);
    }
    if (message.mediaType !== "") {
      obj.mediaType = message.mediaType;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RecordAudioResponse>, I>>(base?: I): RecordAudioResponse {
    return RecordAudioResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RecordAudioResponse>, I>>(object: I): RecordAudioResponse {
    const message = createBaseRecordAudioResponse();
    message.audioData = object.audioData ?? new Uint8Array(0);
    message.mediaType = object.mediaType ?? "";
    return message;
  },
};

function createBaseStartRecordingRequest(): StartRecordingRequest {
  return { serverRtpPort: 0, outputUri: "", sampleRate: undefined, format: undefined, callId: "", traceId: "" };
}

export const StartRecordingRequest: MessageFns<StartRecordingRequest> = {
  encode(message: StartRecordingRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serverRtpPort !== 0) {
      writer.uint32(8).uint32(message.serverRtpPort);
    }
    if (message.outputUri !== "") {
      writer.uint32(18).string(message.outputUri);
    }
    if (message.sampleRate !== undefined) {
      writer.uint32(24).uint32(message.sampleRate);
    }
    if (message.format !== undefined) {
      writer.uint32(34).string(message.format);
    }
    if (message.callId !== "") {
      writer.uint32(42).string(message.callId);
    }
    if (message.traceId !== "") {
      writer.uint32(50).string(message.traceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartRecordingRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartRecordingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.serverRtpPort = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.outputUri = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sampleRate = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.format = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.callId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.traceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartRecordingRequest {
    return {
      serverRtpPort: isSet(object.serverRtpPort)
        ? globalThis.Number(object.serverRtpPort)
        : isSet(object.server_rtp_port)
        ? globalThis.Number(object.server_rtp_port)
        : 0,
      outputUri: isSet(object.outputUri)
        ? globalThis.String(object.outputUri)
        : isSet(object.output_uri)
        ? globalThis.String(object.output_uri)
        : "",
      sampleRate: isSet(object.sampleRate)
        ? globalThis.Number(object.sampleRate)
        : isSet(object.sample_rate)
        ? globalThis.Number(object.sample_rate)
        : undefined,
      format: isSet(object.format) ? globalThis.String(object.format) : undefined,
      callId: isSet(object.callId)
        ? globalThis.String(object.callId)
        : isSet(object.call_id)
        ? globalThis.String(object.call_id)
        : "",
      traceId: isSet(object.traceId)
        ? globalThis.String(object.traceId)
        : isSet(object.trace_id)
        ? globalThis.String(object.trace_id)
        : "",
    };
  },

  toJSON(message: StartRecordingRequest): unknown {
    const obj: any = {};
    if (message.serverRtpPort !== 0) {
      obj.serverRtpPort = Math.round(message.serverRtpPort);
    }
    if (message.outputUri !== "") {
      obj.outputUri = message.outputUri;
    }
    if (message.sampleRate !== undefined) {
      obj.sampleRate = Math.round(message.sampleRate);
    }
    if (message.format !== undefined) {
      obj.format = message.format;
    }
    if (message.callId !== "") {
      obj.callId = message.callId;
    }
    if (message.traceId !== "") {
      obj.traceId = message.traceId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StartRecordingRequest>, I>>(base?: I): StartRecordingRequest {
    return StartRecordingRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartRecordingRequest>, I>>(object: I): StartRecordingRequest {
    const message = createBaseStartRecordingRequest();
    message.serverRtpPort = object.serverRtpPort ?? 0;
    message.outputUri = object.outputUri ?? "";
    message.sampleRate = object.sampleRate ?? undefined;
    message.format = object.format ?? undefined;
    message.callId = object.callId ?? "";
    message.traceId = object.traceId ?? "";
    return message;
  },
};

function createBaseStartRecordingResponse(): StartRecordingResponse {
  return { success: false };
}

export const StartRecordingResponse: MessageFns<StartRecordingResponse> = {
  encode(message: StartRecordingResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartRecordingResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartRecordingResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartRecordingResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: StartRecordingResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StartRecordingResponse>, I>>(base?: I): StartRecordingResponse {
    return StartRecordingResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartRecordingResponse>, I>>(object: I): StartRecordingResponse {
    const message = createBaseStartRecordingResponse();
    message.success = object.success ?? false;
    return message;
  },
};

function createBaseStopRecordingRequest(): StopRecordingRequest {
  return { serverRtpPort: 0 };
}

export const StopRecordingRequest: MessageFns<StopRecordingRequest> = {
  encode(message: StopRecordingRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serverRtpPort !== 0) {
      writer.uint32(8).uint32(message.serverRtpPort);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StopRecordingRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStopRecordingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.serverRtpPort = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StopRecordingRequest {
    return {
      serverRtpPort: isSet(object.serverRtpPort)
        ? globalThis.Number(object.serverRtpPort)
        : isSet(object.server_rtp_port)
        ? globalThis.Number(object.server_rtp_port)
        : 0,
    };
  },

  toJSON(message: StopRecordingRequest): unknown {
    const obj: any = {};
    if (message.serverRtpPort !== 0) {
      obj.serverRtpPort = Math.round(message.serverRtpPort);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StopRecordingRequest>, I>>(base?: I): StopRecordingRequest {
    return StopRecordingRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StopRecordingRequest>, I>>(object: I): StopRecordingRequest {
    const message = createBaseStopRecordingRequest();
    message.serverRtpPort = object.serverRtpPort ?? 0;
    return message;
  },
};

function createBaseStopRecordingResponse(): StopRecordingResponse {
  return { success: false };
}

export const StopRecordingResponse: MessageFns<StopRecordingResponse> = {
  encode(message: StopRecordingResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StopRecordingResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStopRecordingResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StopRecordingResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: StopRecordingResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StopRecordingResponse>, I>>(base?: I): StopRecordingResponse {
    return StopRecordingResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StopRecordingResponse>, I>>(object: I): StopRecordingResponse {
    const message = createBaseStopRecordingResponse();
    message.success = object.success ?? false;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
