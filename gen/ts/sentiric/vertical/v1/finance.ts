// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/vertical/v1/finance.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.vertical.v1";

/** RPC Kuralı */
export interface GetAccountBalanceRequest {
  accountId: string;
  userId: string;
}

/** RPC Kuralı */
export interface GetAccountBalanceResponse {
  balance: number;
  currency: string;
  lastTransactionDate: string;
}

function createBaseGetAccountBalanceRequest(): GetAccountBalanceRequest {
  return { accountId: "", userId: "" };
}

export const GetAccountBalanceRequest: MessageFns<GetAccountBalanceRequest> = {
  encode(message: GetAccountBalanceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accountId !== "") {
      writer.uint32(10).string(message.accountId);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAccountBalanceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAccountBalanceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accountId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetAccountBalanceRequest {
    return {
      accountId: isSet(object.accountId)
        ? globalThis.String(object.accountId)
        : isSet(object.account_id)
        ? globalThis.String(object.account_id)
        : "",
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
    };
  },

  toJSON(message: GetAccountBalanceRequest): unknown {
    const obj: any = {};
    if (message.accountId !== "") {
      obj.accountId = message.accountId;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetAccountBalanceRequest>, I>>(base?: I): GetAccountBalanceRequest {
    return GetAccountBalanceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetAccountBalanceRequest>, I>>(object: I): GetAccountBalanceRequest {
    const message = createBaseGetAccountBalanceRequest();
    message.accountId = object.accountId ?? "";
    message.userId = object.userId ?? "";
    return message;
  },
};

function createBaseGetAccountBalanceResponse(): GetAccountBalanceResponse {
  return { balance: 0, currency: "", lastTransactionDate: "" };
}

export const GetAccountBalanceResponse: MessageFns<GetAccountBalanceResponse> = {
  encode(message: GetAccountBalanceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.balance !== 0) {
      writer.uint32(9).double(message.balance);
    }
    if (message.currency !== "") {
      writer.uint32(18).string(message.currency);
    }
    if (message.lastTransactionDate !== "") {
      writer.uint32(26).string(message.lastTransactionDate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAccountBalanceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAccountBalanceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.balance = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.currency = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.lastTransactionDate = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetAccountBalanceResponse {
    return {
      balance: isSet(object.balance) ? globalThis.Number(object.balance) : 0,
      currency: isSet(object.currency) ? globalThis.String(object.currency) : "",
      lastTransactionDate: isSet(object.lastTransactionDate)
        ? globalThis.String(object.lastTransactionDate)
        : isSet(object.last_transaction_date)
        ? globalThis.String(object.last_transaction_date)
        : "",
    };
  },

  toJSON(message: GetAccountBalanceResponse): unknown {
    const obj: any = {};
    if (message.balance !== 0) {
      obj.balance = message.balance;
    }
    if (message.currency !== "") {
      obj.currency = message.currency;
    }
    if (message.lastTransactionDate !== "") {
      obj.lastTransactionDate = message.lastTransactionDate;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetAccountBalanceResponse>, I>>(base?: I): GetAccountBalanceResponse {
    return GetAccountBalanceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetAccountBalanceResponse>, I>>(object: I): GetAccountBalanceResponse {
    const message = createBaseGetAccountBalanceResponse();
    message.balance = object.balance ?? 0;
    message.currency = object.currency ?? "";
    message.lastTransactionDate = object.lastTransactionDate ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
