// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/vertical/v1/public.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.vertical.v1";

/** RPC Kuralı */
export interface SubmitApplicationRequest {
  applicationType: string;
  formData: { [key: string]: string };
  userId: string;
}

export interface SubmitApplicationRequest_FormDataEntry {
  key: string;
  value: string;
}

/** RPC Kuralı */
export interface SubmitApplicationResponse {
  trackingId: string;
  message: string;
}

function createBaseSubmitApplicationRequest(): SubmitApplicationRequest {
  return { applicationType: "", formData: {}, userId: "" };
}

export const SubmitApplicationRequest: MessageFns<SubmitApplicationRequest> = {
  encode(message: SubmitApplicationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.applicationType !== "") {
      writer.uint32(10).string(message.applicationType);
    }
    globalThis.Object.entries(message.formData).forEach(([key, value]: [string, string]) => {
      SubmitApplicationRequest_FormDataEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    if (message.userId !== "") {
      writer.uint32(26).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubmitApplicationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubmitApplicationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.applicationType = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = SubmitApplicationRequest_FormDataEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.formData[entry2.key] = entry2.value;
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubmitApplicationRequest {
    return {
      applicationType: isSet(object.applicationType)
        ? globalThis.String(object.applicationType)
        : isSet(object.application_type)
        ? globalThis.String(object.application_type)
        : "",
      formData: isObject(object.formData)
        ? (globalThis.Object.entries(object.formData) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : isObject(object.form_data)
        ? (globalThis.Object.entries(object.form_data) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
    };
  },

  toJSON(message: SubmitApplicationRequest): unknown {
    const obj: any = {};
    if (message.applicationType !== "") {
      obj.applicationType = message.applicationType;
    }
    if (message.formData) {
      const entries = globalThis.Object.entries(message.formData) as [string, string][];
      if (entries.length > 0) {
        obj.formData = {};
        entries.forEach(([k, v]) => {
          obj.formData[k] = v;
        });
      }
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubmitApplicationRequest>, I>>(base?: I): SubmitApplicationRequest {
    return SubmitApplicationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubmitApplicationRequest>, I>>(object: I): SubmitApplicationRequest {
    const message = createBaseSubmitApplicationRequest();
    message.applicationType = object.applicationType ?? "";
    message.formData = (globalThis.Object.entries(object.formData ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.userId = object.userId ?? "";
    return message;
  },
};

function createBaseSubmitApplicationRequest_FormDataEntry(): SubmitApplicationRequest_FormDataEntry {
  return { key: "", value: "" };
}

export const SubmitApplicationRequest_FormDataEntry: MessageFns<SubmitApplicationRequest_FormDataEntry> = {
  encode(message: SubmitApplicationRequest_FormDataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubmitApplicationRequest_FormDataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubmitApplicationRequest_FormDataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubmitApplicationRequest_FormDataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: SubmitApplicationRequest_FormDataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubmitApplicationRequest_FormDataEntry>, I>>(
    base?: I,
  ): SubmitApplicationRequest_FormDataEntry {
    return SubmitApplicationRequest_FormDataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubmitApplicationRequest_FormDataEntry>, I>>(
    object: I,
  ): SubmitApplicationRequest_FormDataEntry {
    const message = createBaseSubmitApplicationRequest_FormDataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseSubmitApplicationResponse(): SubmitApplicationResponse {
  return { trackingId: "", message: "" };
}

export const SubmitApplicationResponse: MessageFns<SubmitApplicationResponse> = {
  encode(message: SubmitApplicationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.trackingId !== "") {
      writer.uint32(10).string(message.trackingId);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubmitApplicationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubmitApplicationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.trackingId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubmitApplicationResponse {
    return {
      trackingId: isSet(object.trackingId)
        ? globalThis.String(object.trackingId)
        : isSet(object.tracking_id)
        ? globalThis.String(object.tracking_id)
        : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: SubmitApplicationResponse): unknown {
    const obj: any = {};
    if (message.trackingId !== "") {
      obj.trackingId = message.trackingId;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubmitApplicationResponse>, I>>(base?: I): SubmitApplicationResponse {
    return SubmitApplicationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubmitApplicationResponse>, I>>(object: I): SubmitApplicationResponse {
    const message = createBaseSubmitApplicationResponse();
    message.trackingId = object.trackingId ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
