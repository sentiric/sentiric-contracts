// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/vertical/v1/legal.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.vertical.v1";

/** RPC Kuralı */
export interface CheckCaseStatusRequest {
  caseNumber: string;
  clientId: string;
}

/** RPC Kuralı */
export interface CheckCaseStatusResponse {
  status: string;
  nextHearingDate?: string | undefined;
}

function createBaseCheckCaseStatusRequest(): CheckCaseStatusRequest {
  return { caseNumber: "", clientId: "" };
}

export const CheckCaseStatusRequest: MessageFns<CheckCaseStatusRequest> = {
  encode(message: CheckCaseStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.caseNumber !== "") {
      writer.uint32(10).string(message.caseNumber);
    }
    if (message.clientId !== "") {
      writer.uint32(18).string(message.clientId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CheckCaseStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCheckCaseStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.caseNumber = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.clientId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CheckCaseStatusRequest {
    return {
      caseNumber: isSet(object.caseNumber)
        ? globalThis.String(object.caseNumber)
        : isSet(object.case_number)
        ? globalThis.String(object.case_number)
        : "",
      clientId: isSet(object.clientId)
        ? globalThis.String(object.clientId)
        : isSet(object.client_id)
        ? globalThis.String(object.client_id)
        : "",
    };
  },

  toJSON(message: CheckCaseStatusRequest): unknown {
    const obj: any = {};
    if (message.caseNumber !== "") {
      obj.caseNumber = message.caseNumber;
    }
    if (message.clientId !== "") {
      obj.clientId = message.clientId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CheckCaseStatusRequest>, I>>(base?: I): CheckCaseStatusRequest {
    return CheckCaseStatusRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CheckCaseStatusRequest>, I>>(object: I): CheckCaseStatusRequest {
    const message = createBaseCheckCaseStatusRequest();
    message.caseNumber = object.caseNumber ?? "";
    message.clientId = object.clientId ?? "";
    return message;
  },
};

function createBaseCheckCaseStatusResponse(): CheckCaseStatusResponse {
  return { status: "", nextHearingDate: undefined };
}

export const CheckCaseStatusResponse: MessageFns<CheckCaseStatusResponse> = {
  encode(message: CheckCaseStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    if (message.nextHearingDate !== undefined) {
      writer.uint32(18).string(message.nextHearingDate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CheckCaseStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCheckCaseStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nextHearingDate = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CheckCaseStatusResponse {
    return {
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      nextHearingDate: isSet(object.nextHearingDate)
        ? globalThis.String(object.nextHearingDate)
        : isSet(object.next_hearing_date)
        ? globalThis.String(object.next_hearing_date)
        : undefined,
    };
  },

  toJSON(message: CheckCaseStatusResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.nextHearingDate !== undefined) {
      obj.nextHearingDate = message.nextHearingDate;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CheckCaseStatusResponse>, I>>(base?: I): CheckCaseStatusResponse {
    return CheckCaseStatusResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CheckCaseStatusResponse>, I>>(object: I): CheckCaseStatusResponse {
    const message = createBaseCheckCaseStatusResponse();
    message.status = object.status ?? "";
    message.nextHearingDate = object.nextHearingDate ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
