// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/vertical/v1/hospitality.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.vertical.v1";

/** RPC Kuralı */
export interface FindHotelsRequest {
  location: string;
  checkInDate: string;
}

/** RPC Kuralı */
export interface FindHotelsResponse {
  results: HotelResult[];
}

export interface HotelResult {
  name: string;
  rating: number;
}

/** RPC Kuralı */
export interface BookRoomRequest {
  hotelId: string;
  guestName: string;
  numGuests: number;
}

/** RPC Kuralı */
export interface BookRoomResponse {
  bookingId: string;
  success: boolean;
}

function createBaseFindHotelsRequest(): FindHotelsRequest {
  return { location: "", checkInDate: "" };
}

export const FindHotelsRequest: MessageFns<FindHotelsRequest> = {
  encode(message: FindHotelsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.location !== "") {
      writer.uint32(10).string(message.location);
    }
    if (message.checkInDate !== "") {
      writer.uint32(18).string(message.checkInDate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FindHotelsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFindHotelsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.location = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.checkInDate = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FindHotelsRequest {
    return {
      location: isSet(object.location) ? globalThis.String(object.location) : "",
      checkInDate: isSet(object.checkInDate)
        ? globalThis.String(object.checkInDate)
        : isSet(object.check_in_date)
        ? globalThis.String(object.check_in_date)
        : "",
    };
  },

  toJSON(message: FindHotelsRequest): unknown {
    const obj: any = {};
    if (message.location !== "") {
      obj.location = message.location;
    }
    if (message.checkInDate !== "") {
      obj.checkInDate = message.checkInDate;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FindHotelsRequest>, I>>(base?: I): FindHotelsRequest {
    return FindHotelsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FindHotelsRequest>, I>>(object: I): FindHotelsRequest {
    const message = createBaseFindHotelsRequest();
    message.location = object.location ?? "";
    message.checkInDate = object.checkInDate ?? "";
    return message;
  },
};

function createBaseFindHotelsResponse(): FindHotelsResponse {
  return { results: [] };
}

export const FindHotelsResponse: MessageFns<FindHotelsResponse> = {
  encode(message: FindHotelsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.results) {
      HotelResult.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FindHotelsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFindHotelsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.results.push(HotelResult.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FindHotelsResponse {
    return {
      results: globalThis.Array.isArray(object?.results) ? object.results.map((e: any) => HotelResult.fromJSON(e)) : [],
    };
  },

  toJSON(message: FindHotelsResponse): unknown {
    const obj: any = {};
    if (message.results?.length) {
      obj.results = message.results.map((e) => HotelResult.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FindHotelsResponse>, I>>(base?: I): FindHotelsResponse {
    return FindHotelsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FindHotelsResponse>, I>>(object: I): FindHotelsResponse {
    const message = createBaseFindHotelsResponse();
    message.results = object.results?.map((e) => HotelResult.fromPartial(e)) || [];
    return message;
  },
};

function createBaseHotelResult(): HotelResult {
  return { name: "", rating: 0 };
}

export const HotelResult: MessageFns<HotelResult> = {
  encode(message: HotelResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.rating !== 0) {
      writer.uint32(16).int32(message.rating);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HotelResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHotelResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.rating = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HotelResult {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      rating: isSet(object.rating) ? globalThis.Number(object.rating) : 0,
    };
  },

  toJSON(message: HotelResult): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.rating !== 0) {
      obj.rating = Math.round(message.rating);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HotelResult>, I>>(base?: I): HotelResult {
    return HotelResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HotelResult>, I>>(object: I): HotelResult {
    const message = createBaseHotelResult();
    message.name = object.name ?? "";
    message.rating = object.rating ?? 0;
    return message;
  },
};

function createBaseBookRoomRequest(): BookRoomRequest {
  return { hotelId: "", guestName: "", numGuests: 0 };
}

export const BookRoomRequest: MessageFns<BookRoomRequest> = {
  encode(message: BookRoomRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hotelId !== "") {
      writer.uint32(10).string(message.hotelId);
    }
    if (message.guestName !== "") {
      writer.uint32(18).string(message.guestName);
    }
    if (message.numGuests !== 0) {
      writer.uint32(24).int32(message.numGuests);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BookRoomRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBookRoomRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hotelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.guestName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.numGuests = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BookRoomRequest {
    return {
      hotelId: isSet(object.hotelId)
        ? globalThis.String(object.hotelId)
        : isSet(object.hotel_id)
        ? globalThis.String(object.hotel_id)
        : "",
      guestName: isSet(object.guestName)
        ? globalThis.String(object.guestName)
        : isSet(object.guest_name)
        ? globalThis.String(object.guest_name)
        : "",
      numGuests: isSet(object.numGuests)
        ? globalThis.Number(object.numGuests)
        : isSet(object.num_guests)
        ? globalThis.Number(object.num_guests)
        : 0,
    };
  },

  toJSON(message: BookRoomRequest): unknown {
    const obj: any = {};
    if (message.hotelId !== "") {
      obj.hotelId = message.hotelId;
    }
    if (message.guestName !== "") {
      obj.guestName = message.guestName;
    }
    if (message.numGuests !== 0) {
      obj.numGuests = Math.round(message.numGuests);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BookRoomRequest>, I>>(base?: I): BookRoomRequest {
    return BookRoomRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BookRoomRequest>, I>>(object: I): BookRoomRequest {
    const message = createBaseBookRoomRequest();
    message.hotelId = object.hotelId ?? "";
    message.guestName = object.guestName ?? "";
    message.numGuests = object.numGuests ?? 0;
    return message;
  },
};

function createBaseBookRoomResponse(): BookRoomResponse {
  return { bookingId: "", success: false };
}

export const BookRoomResponse: MessageFns<BookRoomResponse> = {
  encode(message: BookRoomResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.bookingId !== "") {
      writer.uint32(10).string(message.bookingId);
    }
    if (message.success !== false) {
      writer.uint32(16).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BookRoomResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBookRoomResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.bookingId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BookRoomResponse {
    return {
      bookingId: isSet(object.bookingId)
        ? globalThis.String(object.bookingId)
        : isSet(object.booking_id)
        ? globalThis.String(object.booking_id)
        : "",
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
    };
  },

  toJSON(message: BookRoomResponse): unknown {
    const obj: any = {};
    if (message.bookingId !== "") {
      obj.bookingId = message.bookingId;
    }
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BookRoomResponse>, I>>(base?: I): BookRoomResponse {
    return BookRoomResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BookRoomResponse>, I>>(object: I): BookRoomResponse {
    const message = createBaseBookRoomResponse();
    message.bookingId = object.bookingId ?? "";
    message.success = object.success ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
