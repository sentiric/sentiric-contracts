// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/vertical/v1/health.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.vertical.v1";

/** RPC Kuralı */
export interface FindDoctorRequest {
  specialty: string;
  location: string;
}

/** RPC Kuralı */
export interface FindDoctorResponse {
  doctors: DoctorInfo[];
}

export interface DoctorInfo {
  name: string;
  specialty: string;
}

/** RPC Kuralı */
export interface ScheduleAppointmentRequest {
  doctorId: string;
  patientId: string;
  preferredTimeIso: string;
}

/** RPC Kuralı */
export interface ScheduleAppointmentResponse {
  confirmationId: string;
  success: boolean;
}

function createBaseFindDoctorRequest(): FindDoctorRequest {
  return { specialty: "", location: "" };
}

export const FindDoctorRequest: MessageFns<FindDoctorRequest> = {
  encode(message: FindDoctorRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.specialty !== "") {
      writer.uint32(10).string(message.specialty);
    }
    if (message.location !== "") {
      writer.uint32(18).string(message.location);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FindDoctorRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFindDoctorRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.specialty = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.location = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FindDoctorRequest {
    return {
      specialty: isSet(object.specialty) ? globalThis.String(object.specialty) : "",
      location: isSet(object.location) ? globalThis.String(object.location) : "",
    };
  },

  toJSON(message: FindDoctorRequest): unknown {
    const obj: any = {};
    if (message.specialty !== "") {
      obj.specialty = message.specialty;
    }
    if (message.location !== "") {
      obj.location = message.location;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FindDoctorRequest>, I>>(base?: I): FindDoctorRequest {
    return FindDoctorRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FindDoctorRequest>, I>>(object: I): FindDoctorRequest {
    const message = createBaseFindDoctorRequest();
    message.specialty = object.specialty ?? "";
    message.location = object.location ?? "";
    return message;
  },
};

function createBaseFindDoctorResponse(): FindDoctorResponse {
  return { doctors: [] };
}

export const FindDoctorResponse: MessageFns<FindDoctorResponse> = {
  encode(message: FindDoctorResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.doctors) {
      DoctorInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FindDoctorResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFindDoctorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.doctors.push(DoctorInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FindDoctorResponse {
    return {
      doctors: globalThis.Array.isArray(object?.doctors) ? object.doctors.map((e: any) => DoctorInfo.fromJSON(e)) : [],
    };
  },

  toJSON(message: FindDoctorResponse): unknown {
    const obj: any = {};
    if (message.doctors?.length) {
      obj.doctors = message.doctors.map((e) => DoctorInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FindDoctorResponse>, I>>(base?: I): FindDoctorResponse {
    return FindDoctorResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FindDoctorResponse>, I>>(object: I): FindDoctorResponse {
    const message = createBaseFindDoctorResponse();
    message.doctors = object.doctors?.map((e) => DoctorInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDoctorInfo(): DoctorInfo {
  return { name: "", specialty: "" };
}

export const DoctorInfo: MessageFns<DoctorInfo> = {
  encode(message: DoctorInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.specialty !== "") {
      writer.uint32(18).string(message.specialty);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DoctorInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDoctorInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.specialty = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DoctorInfo {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      specialty: isSet(object.specialty) ? globalThis.String(object.specialty) : "",
    };
  },

  toJSON(message: DoctorInfo): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.specialty !== "") {
      obj.specialty = message.specialty;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DoctorInfo>, I>>(base?: I): DoctorInfo {
    return DoctorInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DoctorInfo>, I>>(object: I): DoctorInfo {
    const message = createBaseDoctorInfo();
    message.name = object.name ?? "";
    message.specialty = object.specialty ?? "";
    return message;
  },
};

function createBaseScheduleAppointmentRequest(): ScheduleAppointmentRequest {
  return { doctorId: "", patientId: "", preferredTimeIso: "" };
}

export const ScheduleAppointmentRequest: MessageFns<ScheduleAppointmentRequest> = {
  encode(message: ScheduleAppointmentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.doctorId !== "") {
      writer.uint32(10).string(message.doctorId);
    }
    if (message.patientId !== "") {
      writer.uint32(18).string(message.patientId);
    }
    if (message.preferredTimeIso !== "") {
      writer.uint32(26).string(message.preferredTimeIso);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScheduleAppointmentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScheduleAppointmentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.doctorId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.patientId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.preferredTimeIso = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScheduleAppointmentRequest {
    return {
      doctorId: isSet(object.doctorId)
        ? globalThis.String(object.doctorId)
        : isSet(object.doctor_id)
        ? globalThis.String(object.doctor_id)
        : "",
      patientId: isSet(object.patientId)
        ? globalThis.String(object.patientId)
        : isSet(object.patient_id)
        ? globalThis.String(object.patient_id)
        : "",
      preferredTimeIso: isSet(object.preferredTimeIso)
        ? globalThis.String(object.preferredTimeIso)
        : isSet(object.preferred_time_iso)
        ? globalThis.String(object.preferred_time_iso)
        : "",
    };
  },

  toJSON(message: ScheduleAppointmentRequest): unknown {
    const obj: any = {};
    if (message.doctorId !== "") {
      obj.doctorId = message.doctorId;
    }
    if (message.patientId !== "") {
      obj.patientId = message.patientId;
    }
    if (message.preferredTimeIso !== "") {
      obj.preferredTimeIso = message.preferredTimeIso;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ScheduleAppointmentRequest>, I>>(base?: I): ScheduleAppointmentRequest {
    return ScheduleAppointmentRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ScheduleAppointmentRequest>, I>>(object: I): ScheduleAppointmentRequest {
    const message = createBaseScheduleAppointmentRequest();
    message.doctorId = object.doctorId ?? "";
    message.patientId = object.patientId ?? "";
    message.preferredTimeIso = object.preferredTimeIso ?? "";
    return message;
  },
};

function createBaseScheduleAppointmentResponse(): ScheduleAppointmentResponse {
  return { confirmationId: "", success: false };
}

export const ScheduleAppointmentResponse: MessageFns<ScheduleAppointmentResponse> = {
  encode(message: ScheduleAppointmentResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.confirmationId !== "") {
      writer.uint32(10).string(message.confirmationId);
    }
    if (message.success !== false) {
      writer.uint32(16).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScheduleAppointmentResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScheduleAppointmentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.confirmationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScheduleAppointmentResponse {
    return {
      confirmationId: isSet(object.confirmationId)
        ? globalThis.String(object.confirmationId)
        : isSet(object.confirmation_id)
        ? globalThis.String(object.confirmation_id)
        : "",
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
    };
  },

  toJSON(message: ScheduleAppointmentResponse): unknown {
    const obj: any = {};
    if (message.confirmationId !== "") {
      obj.confirmationId = message.confirmationId;
    }
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ScheduleAppointmentResponse>, I>>(base?: I): ScheduleAppointmentResponse {
    return ScheduleAppointmentResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ScheduleAppointmentResponse>, I>>(object: I): ScheduleAppointmentResponse {
    const message = createBaseScheduleAppointmentResponse();
    message.confirmationId = object.confirmationId ?? "";
    message.success = object.success ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
