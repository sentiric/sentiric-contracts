// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/tts/v1/edge.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.tts.v1";

export interface EdgeSynthesizeRequest {
  text: string;
  voiceModelId: string;
}

export interface EdgeSynthesizeResponse {
  audioContent: Uint8Array;
}

function createBaseEdgeSynthesizeRequest(): EdgeSynthesizeRequest {
  return { text: "", voiceModelId: "" };
}

export const EdgeSynthesizeRequest: MessageFns<EdgeSynthesizeRequest> = {
  encode(message: EdgeSynthesizeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.voiceModelId !== "") {
      writer.uint32(18).string(message.voiceModelId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EdgeSynthesizeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEdgeSynthesizeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.voiceModelId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EdgeSynthesizeRequest {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      voiceModelId: isSet(object.voiceModelId)
        ? globalThis.String(object.voiceModelId)
        : isSet(object.voice_model_id)
        ? globalThis.String(object.voice_model_id)
        : "",
    };
  },

  toJSON(message: EdgeSynthesizeRequest): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.voiceModelId !== "") {
      obj.voiceModelId = message.voiceModelId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EdgeSynthesizeRequest>, I>>(base?: I): EdgeSynthesizeRequest {
    return EdgeSynthesizeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EdgeSynthesizeRequest>, I>>(object: I): EdgeSynthesizeRequest {
    const message = createBaseEdgeSynthesizeRequest();
    message.text = object.text ?? "";
    message.voiceModelId = object.voiceModelId ?? "";
    return message;
  },
};

function createBaseEdgeSynthesizeResponse(): EdgeSynthesizeResponse {
  return { audioContent: new Uint8Array(0) };
}

export const EdgeSynthesizeResponse: MessageFns<EdgeSynthesizeResponse> = {
  encode(message: EdgeSynthesizeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.audioContent.length !== 0) {
      writer.uint32(10).bytes(message.audioContent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EdgeSynthesizeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEdgeSynthesizeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.audioContent = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EdgeSynthesizeResponse {
    return {
      audioContent: isSet(object.audioContent)
        ? bytesFromBase64(object.audioContent)
        : isSet(object.audio_content)
        ? bytesFromBase64(object.audio_content)
        : new Uint8Array(0),
    };
  },

  toJSON(message: EdgeSynthesizeResponse): unknown {
    const obj: any = {};
    if (message.audioContent.length !== 0) {
      obj.audioContent = base64FromBytes(message.audioContent);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EdgeSynthesizeResponse>, I>>(base?: I): EdgeSynthesizeResponse {
    return EdgeSynthesizeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EdgeSynthesizeResponse>, I>>(object: I): EdgeSynthesizeResponse {
    const message = createBaseEdgeSynthesizeResponse();
    message.audioContent = object.audioContent ?? new Uint8Array(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
