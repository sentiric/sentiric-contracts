// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/tts/v1/omnivoice.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.tts.v1";

/** Dosya: proto/sentiric/tts/v1/omnivoice.proto */

/** --- Unary Messages --- */
export interface OmnivoiceSynthesizeRequest {
  text: string;
  languageCode: string;
  /** Modelin üretimi için referans ses (Klonlama) */
  referenceAudio?:
    | Uint8Array
    | undefined;
  /**
   * [OMNIVOICE ÖZEL YETENEK]: Metin ile ses tasarımı!
   * Örn: "female, low pitch, warm, british accent"
   */
  voiceGuidancePrompt?: string | undefined;
  temperature: number;
  speed: number;
  outputFormat: string;
  sampleRate: number;
}

export interface OmnivoiceSynthesizeResponse {
  audioContent: Uint8Array;
}

/** --- Stream Messages --- */
export interface OmnivoiceSynthesizeStreamRequest {
  text: string;
  languageCode: string;
  referenceAudio?: Uint8Array | undefined;
  voiceGuidancePrompt?: string | undefined;
  temperature: number;
  speed: number;
  outputFormat: string;
  sampleRate: number;
}

export interface OmnivoiceSynthesizeStreamResponse {
  audioChunk: Uint8Array;
  isFinal: boolean;
}

function createBaseOmnivoiceSynthesizeRequest(): OmnivoiceSynthesizeRequest {
  return {
    text: "",
    languageCode: "",
    referenceAudio: undefined,
    voiceGuidancePrompt: undefined,
    temperature: 0,
    speed: 0,
    outputFormat: "",
    sampleRate: 0,
  };
}

export const OmnivoiceSynthesizeRequest: MessageFns<OmnivoiceSynthesizeRequest> = {
  encode(message: OmnivoiceSynthesizeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.languageCode !== "") {
      writer.uint32(18).string(message.languageCode);
    }
    if (message.referenceAudio !== undefined) {
      writer.uint32(26).bytes(message.referenceAudio);
    }
    if (message.voiceGuidancePrompt !== undefined) {
      writer.uint32(34).string(message.voiceGuidancePrompt);
    }
    if (message.temperature !== 0) {
      writer.uint32(45).float(message.temperature);
    }
    if (message.speed !== 0) {
      writer.uint32(53).float(message.speed);
    }
    if (message.outputFormat !== "") {
      writer.uint32(58).string(message.outputFormat);
    }
    if (message.sampleRate !== 0) {
      writer.uint32(64).int32(message.sampleRate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OmnivoiceSynthesizeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOmnivoiceSynthesizeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.languageCode = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.referenceAudio = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.voiceGuidancePrompt = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 45) {
            break;
          }

          message.temperature = reader.float();
          continue;
        }
        case 6: {
          if (tag !== 53) {
            break;
          }

          message.speed = reader.float();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.outputFormat = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.sampleRate = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OmnivoiceSynthesizeRequest {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      languageCode: isSet(object.languageCode)
        ? globalThis.String(object.languageCode)
        : isSet(object.language_code)
        ? globalThis.String(object.language_code)
        : "",
      referenceAudio: isSet(object.referenceAudio)
        ? bytesFromBase64(object.referenceAudio)
        : isSet(object.reference_audio)
        ? bytesFromBase64(object.reference_audio)
        : undefined,
      voiceGuidancePrompt: isSet(object.voiceGuidancePrompt)
        ? globalThis.String(object.voiceGuidancePrompt)
        : isSet(object.voice_guidance_prompt)
        ? globalThis.String(object.voice_guidance_prompt)
        : undefined,
      temperature: isSet(object.temperature) ? globalThis.Number(object.temperature) : 0,
      speed: isSet(object.speed) ? globalThis.Number(object.speed) : 0,
      outputFormat: isSet(object.outputFormat)
        ? globalThis.String(object.outputFormat)
        : isSet(object.output_format)
        ? globalThis.String(object.output_format)
        : "",
      sampleRate: isSet(object.sampleRate)
        ? globalThis.Number(object.sampleRate)
        : isSet(object.sample_rate)
        ? globalThis.Number(object.sample_rate)
        : 0,
    };
  },

  toJSON(message: OmnivoiceSynthesizeRequest): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.languageCode !== "") {
      obj.languageCode = message.languageCode;
    }
    if (message.referenceAudio !== undefined) {
      obj.referenceAudio = base64FromBytes(message.referenceAudio);
    }
    if (message.voiceGuidancePrompt !== undefined) {
      obj.voiceGuidancePrompt = message.voiceGuidancePrompt;
    }
    if (message.temperature !== 0) {
      obj.temperature = message.temperature;
    }
    if (message.speed !== 0) {
      obj.speed = message.speed;
    }
    if (message.outputFormat !== "") {
      obj.outputFormat = message.outputFormat;
    }
    if (message.sampleRate !== 0) {
      obj.sampleRate = Math.round(message.sampleRate);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OmnivoiceSynthesizeRequest>, I>>(base?: I): OmnivoiceSynthesizeRequest {
    return OmnivoiceSynthesizeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OmnivoiceSynthesizeRequest>, I>>(object: I): OmnivoiceSynthesizeRequest {
    const message = createBaseOmnivoiceSynthesizeRequest();
    message.text = object.text ?? "";
    message.languageCode = object.languageCode ?? "";
    message.referenceAudio = object.referenceAudio ?? undefined;
    message.voiceGuidancePrompt = object.voiceGuidancePrompt ?? undefined;
    message.temperature = object.temperature ?? 0;
    message.speed = object.speed ?? 0;
    message.outputFormat = object.outputFormat ?? "";
    message.sampleRate = object.sampleRate ?? 0;
    return message;
  },
};

function createBaseOmnivoiceSynthesizeResponse(): OmnivoiceSynthesizeResponse {
  return { audioContent: new Uint8Array(0) };
}

export const OmnivoiceSynthesizeResponse: MessageFns<OmnivoiceSynthesizeResponse> = {
  encode(message: OmnivoiceSynthesizeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.audioContent.length !== 0) {
      writer.uint32(10).bytes(message.audioContent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OmnivoiceSynthesizeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOmnivoiceSynthesizeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.audioContent = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OmnivoiceSynthesizeResponse {
    return {
      audioContent: isSet(object.audioContent)
        ? bytesFromBase64(object.audioContent)
        : isSet(object.audio_content)
        ? bytesFromBase64(object.audio_content)
        : new Uint8Array(0),
    };
  },

  toJSON(message: OmnivoiceSynthesizeResponse): unknown {
    const obj: any = {};
    if (message.audioContent.length !== 0) {
      obj.audioContent = base64FromBytes(message.audioContent);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OmnivoiceSynthesizeResponse>, I>>(base?: I): OmnivoiceSynthesizeResponse {
    return OmnivoiceSynthesizeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OmnivoiceSynthesizeResponse>, I>>(object: I): OmnivoiceSynthesizeResponse {
    const message = createBaseOmnivoiceSynthesizeResponse();
    message.audioContent = object.audioContent ?? new Uint8Array(0);
    return message;
  },
};

function createBaseOmnivoiceSynthesizeStreamRequest(): OmnivoiceSynthesizeStreamRequest {
  return {
    text: "",
    languageCode: "",
    referenceAudio: undefined,
    voiceGuidancePrompt: undefined,
    temperature: 0,
    speed: 0,
    outputFormat: "",
    sampleRate: 0,
  };
}

export const OmnivoiceSynthesizeStreamRequest: MessageFns<OmnivoiceSynthesizeStreamRequest> = {
  encode(message: OmnivoiceSynthesizeStreamRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.languageCode !== "") {
      writer.uint32(18).string(message.languageCode);
    }
    if (message.referenceAudio !== undefined) {
      writer.uint32(26).bytes(message.referenceAudio);
    }
    if (message.voiceGuidancePrompt !== undefined) {
      writer.uint32(34).string(message.voiceGuidancePrompt);
    }
    if (message.temperature !== 0) {
      writer.uint32(45).float(message.temperature);
    }
    if (message.speed !== 0) {
      writer.uint32(53).float(message.speed);
    }
    if (message.outputFormat !== "") {
      writer.uint32(58).string(message.outputFormat);
    }
    if (message.sampleRate !== 0) {
      writer.uint32(64).int32(message.sampleRate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OmnivoiceSynthesizeStreamRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOmnivoiceSynthesizeStreamRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.languageCode = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.referenceAudio = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.voiceGuidancePrompt = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 45) {
            break;
          }

          message.temperature = reader.float();
          continue;
        }
        case 6: {
          if (tag !== 53) {
            break;
          }

          message.speed = reader.float();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.outputFormat = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.sampleRate = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OmnivoiceSynthesizeStreamRequest {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      languageCode: isSet(object.languageCode)
        ? globalThis.String(object.languageCode)
        : isSet(object.language_code)
        ? globalThis.String(object.language_code)
        : "",
      referenceAudio: isSet(object.referenceAudio)
        ? bytesFromBase64(object.referenceAudio)
        : isSet(object.reference_audio)
        ? bytesFromBase64(object.reference_audio)
        : undefined,
      voiceGuidancePrompt: isSet(object.voiceGuidancePrompt)
        ? globalThis.String(object.voiceGuidancePrompt)
        : isSet(object.voice_guidance_prompt)
        ? globalThis.String(object.voice_guidance_prompt)
        : undefined,
      temperature: isSet(object.temperature) ? globalThis.Number(object.temperature) : 0,
      speed: isSet(object.speed) ? globalThis.Number(object.speed) : 0,
      outputFormat: isSet(object.outputFormat)
        ? globalThis.String(object.outputFormat)
        : isSet(object.output_format)
        ? globalThis.String(object.output_format)
        : "",
      sampleRate: isSet(object.sampleRate)
        ? globalThis.Number(object.sampleRate)
        : isSet(object.sample_rate)
        ? globalThis.Number(object.sample_rate)
        : 0,
    };
  },

  toJSON(message: OmnivoiceSynthesizeStreamRequest): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.languageCode !== "") {
      obj.languageCode = message.languageCode;
    }
    if (message.referenceAudio !== undefined) {
      obj.referenceAudio = base64FromBytes(message.referenceAudio);
    }
    if (message.voiceGuidancePrompt !== undefined) {
      obj.voiceGuidancePrompt = message.voiceGuidancePrompt;
    }
    if (message.temperature !== 0) {
      obj.temperature = message.temperature;
    }
    if (message.speed !== 0) {
      obj.speed = message.speed;
    }
    if (message.outputFormat !== "") {
      obj.outputFormat = message.outputFormat;
    }
    if (message.sampleRate !== 0) {
      obj.sampleRate = Math.round(message.sampleRate);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OmnivoiceSynthesizeStreamRequest>, I>>(
    base?: I,
  ): OmnivoiceSynthesizeStreamRequest {
    return OmnivoiceSynthesizeStreamRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OmnivoiceSynthesizeStreamRequest>, I>>(
    object: I,
  ): OmnivoiceSynthesizeStreamRequest {
    const message = createBaseOmnivoiceSynthesizeStreamRequest();
    message.text = object.text ?? "";
    message.languageCode = object.languageCode ?? "";
    message.referenceAudio = object.referenceAudio ?? undefined;
    message.voiceGuidancePrompt = object.voiceGuidancePrompt ?? undefined;
    message.temperature = object.temperature ?? 0;
    message.speed = object.speed ?? 0;
    message.outputFormat = object.outputFormat ?? "";
    message.sampleRate = object.sampleRate ?? 0;
    return message;
  },
};

function createBaseOmnivoiceSynthesizeStreamResponse(): OmnivoiceSynthesizeStreamResponse {
  return { audioChunk: new Uint8Array(0), isFinal: false };
}

export const OmnivoiceSynthesizeStreamResponse: MessageFns<OmnivoiceSynthesizeStreamResponse> = {
  encode(message: OmnivoiceSynthesizeStreamResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.audioChunk.length !== 0) {
      writer.uint32(10).bytes(message.audioChunk);
    }
    if (message.isFinal !== false) {
      writer.uint32(16).bool(message.isFinal);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OmnivoiceSynthesizeStreamResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOmnivoiceSynthesizeStreamResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.audioChunk = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isFinal = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OmnivoiceSynthesizeStreamResponse {
    return {
      audioChunk: isSet(object.audioChunk)
        ? bytesFromBase64(object.audioChunk)
        : isSet(object.audio_chunk)
        ? bytesFromBase64(object.audio_chunk)
        : new Uint8Array(0),
      isFinal: isSet(object.isFinal)
        ? globalThis.Boolean(object.isFinal)
        : isSet(object.is_final)
        ? globalThis.Boolean(object.is_final)
        : false,
    };
  },

  toJSON(message: OmnivoiceSynthesizeStreamResponse): unknown {
    const obj: any = {};
    if (message.audioChunk.length !== 0) {
      obj.audioChunk = base64FromBytes(message.audioChunk);
    }
    if (message.isFinal !== false) {
      obj.isFinal = message.isFinal;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OmnivoiceSynthesizeStreamResponse>, I>>(
    base?: I,
  ): OmnivoiceSynthesizeStreamResponse {
    return OmnivoiceSynthesizeStreamResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OmnivoiceSynthesizeStreamResponse>, I>>(
    object: I,
  ): OmnivoiceSynthesizeStreamResponse {
    const message = createBaseOmnivoiceSynthesizeStreamResponse();
    message.audioChunk = object.audioChunk ?? new Uint8Array(0);
    message.isFinal = object.isFinal ?? false;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
