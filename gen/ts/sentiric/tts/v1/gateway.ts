// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/tts/v1/gateway.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.tts.v1";

export enum TextType {
  TEXT_TYPE_UNSPECIFIED = 0,
  TEXT_TYPE_TEXT = 1,
  TEXT_TYPE_SSML = 2,
  UNRECOGNIZED = -1,
}

export function textTypeFromJSON(object: any): TextType {
  switch (object) {
    case 0:
    case "TEXT_TYPE_UNSPECIFIED":
      return TextType.TEXT_TYPE_UNSPECIFIED;
    case 1:
    case "TEXT_TYPE_TEXT":
      return TextType.TEXT_TYPE_TEXT;
    case 2:
    case "TEXT_TYPE_SSML":
      return TextType.TEXT_TYPE_SSML;
    case -1:
    case "UNRECOGNIZED":
    default:
      return TextType.UNRECOGNIZED;
  }
}

export function textTypeToJSON(object: TextType): string {
  switch (object) {
    case TextType.TEXT_TYPE_UNSPECIFIED:
      return "TEXT_TYPE_UNSPECIFIED";
    case TextType.TEXT_TYPE_TEXT:
      return "TEXT_TYPE_TEXT";
    case TextType.TEXT_TYPE_SSML:
      return "TEXT_TYPE_SSML";
    case TextType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum AudioFormat {
  AUDIO_FORMAT_UNSPECIFIED = 0,
  AUDIO_FORMAT_PCM_S16LE = 1,
  AUDIO_FORMAT_WAV = 2,
  AUDIO_FORMAT_MP3 = 3,
  AUDIO_FORMAT_OGG_OPUS = 4,
  AUDIO_FORMAT_MULAW = 5,
  UNRECOGNIZED = -1,
}

export function audioFormatFromJSON(object: any): AudioFormat {
  switch (object) {
    case 0:
    case "AUDIO_FORMAT_UNSPECIFIED":
      return AudioFormat.AUDIO_FORMAT_UNSPECIFIED;
    case 1:
    case "AUDIO_FORMAT_PCM_S16LE":
      return AudioFormat.AUDIO_FORMAT_PCM_S16LE;
    case 2:
    case "AUDIO_FORMAT_WAV":
      return AudioFormat.AUDIO_FORMAT_WAV;
    case 3:
    case "AUDIO_FORMAT_MP3":
      return AudioFormat.AUDIO_FORMAT_MP3;
    case 4:
    case "AUDIO_FORMAT_OGG_OPUS":
      return AudioFormat.AUDIO_FORMAT_OGG_OPUS;
    case 5:
    case "AUDIO_FORMAT_MULAW":
      return AudioFormat.AUDIO_FORMAT_MULAW;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AudioFormat.UNRECOGNIZED;
  }
}

export function audioFormatToJSON(object: AudioFormat): string {
  switch (object) {
    case AudioFormat.AUDIO_FORMAT_UNSPECIFIED:
      return "AUDIO_FORMAT_UNSPECIFIED";
    case AudioFormat.AUDIO_FORMAT_PCM_S16LE:
      return "AUDIO_FORMAT_PCM_S16LE";
    case AudioFormat.AUDIO_FORMAT_WAV:
      return "AUDIO_FORMAT_WAV";
    case AudioFormat.AUDIO_FORMAT_MP3:
      return "AUDIO_FORMAT_MP3";
    case AudioFormat.AUDIO_FORMAT_OGG_OPUS:
      return "AUDIO_FORMAT_OGG_OPUS";
    case AudioFormat.AUDIO_FORMAT_MULAW:
      return "AUDIO_FORMAT_MULAW";
    case AudioFormat.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface SynthesizeRequest {
  text: string;
  textType: TextType;
  voiceId: string;
  /** [MİMARİ KARAR]: AudioConfig zorunludur. Her istek format ve sample rate belirtmelidir. */
  audioConfig?: AudioConfig | undefined;
  preferredProvider: string;
  /** [MİMARİ KARAR]: İnce ayar parametreleri opsiyonel bir alt mesaj olarak eklendi. */
  tuning?: TuningParams | undefined;
  cloningAudioData?: Uint8Array | undefined;
}

export interface SynthesizeResponse {
  audioContent: Uint8Array;
  contentType: string;
  providerUsed: string;
}

export interface SynthesizeStreamRequest {
  text: string;
  textType: TextType;
  voiceId: string;
  /** [MİMARİ KARAR]: AudioConfig zorunludur. */
  audioConfig?: AudioConfig | undefined;
  preferredProvider: string;
  /** [MİMARİ KARAR]: İnce ayar parametreleri opsiyonel bir alt mesaj olarak eklendi. */
  tuning?: TuningParams | undefined;
  cloningAudioData?: Uint8Array | undefined;
}

export interface SynthesizeStreamResponse {
  audioContent: Uint8Array;
  contentType: string;
  providerUsed: string;
}

export interface ListVoicesRequest {
  languageCode: string;
}

export interface ListVoicesResponse {
  voices: VoiceInfo[];
}

/** [YENİ MESAJ]: Ses üretiminin ince ayarlarını kontrol eder. */
export interface TuningParams {
  /** Rastgelelik: Yüksek değerler daha yaratıcı, düşük değerler daha deterministik. (0.0-2.0) */
  temperature: number;
  /** Nucleus sampling: Olasılık eşiği. (0.0-1.0) */
  topP: number;
  /** En olası K token arasından seçim yap. */
  topK: number;
  /** Tekrar eden kelimeleri cezalandır. (1.0+) */
  repetitionPenalty: number;
  /** Konuşma hızı. (0.25-4.0) */
  speed: number;
}

export interface AudioConfig {
  audioFormat: AudioFormat;
  sampleRateHertz: number;
  volumeGainDb: number;
}

/**
 * ProsodyConfig eski versiyonlarla uyumluluk için tutuluyor,
 * ancak yeni sistemler TuningParams kullanmalı.
 */
export interface ProsodyConfig {
  rate: number;
  pitch: number;
  emotion: string;
}

export interface VoiceInfo {
  id: string;
  name: string;
  language: string;
  gender: string;
  styles: string[];
}

function createBaseSynthesizeRequest(): SynthesizeRequest {
  return {
    text: "",
    textType: 0,
    voiceId: "",
    audioConfig: undefined,
    preferredProvider: "",
    tuning: undefined,
    cloningAudioData: undefined,
  };
}

export const SynthesizeRequest: MessageFns<SynthesizeRequest> = {
  encode(message: SynthesizeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.textType !== 0) {
      writer.uint32(16).int32(message.textType);
    }
    if (message.voiceId !== "") {
      writer.uint32(26).string(message.voiceId);
    }
    if (message.audioConfig !== undefined) {
      AudioConfig.encode(message.audioConfig, writer.uint32(34).fork()).join();
    }
    if (message.preferredProvider !== "") {
      writer.uint32(42).string(message.preferredProvider);
    }
    if (message.tuning !== undefined) {
      TuningParams.encode(message.tuning, writer.uint32(50).fork()).join();
    }
    if (message.cloningAudioData !== undefined) {
      writer.uint32(58).bytes(message.cloningAudioData);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SynthesizeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSynthesizeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.textType = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.voiceId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.audioConfig = AudioConfig.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.preferredProvider = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.tuning = TuningParams.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.cloningAudioData = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SynthesizeRequest {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      textType: isSet(object.textType)
        ? textTypeFromJSON(object.textType)
        : isSet(object.text_type)
        ? textTypeFromJSON(object.text_type)
        : 0,
      voiceId: isSet(object.voiceId)
        ? globalThis.String(object.voiceId)
        : isSet(object.voice_id)
        ? globalThis.String(object.voice_id)
        : "",
      audioConfig: isSet(object.audioConfig)
        ? AudioConfig.fromJSON(object.audioConfig)
        : isSet(object.audio_config)
        ? AudioConfig.fromJSON(object.audio_config)
        : undefined,
      preferredProvider: isSet(object.preferredProvider)
        ? globalThis.String(object.preferredProvider)
        : isSet(object.preferred_provider)
        ? globalThis.String(object.preferred_provider)
        : "",
      tuning: isSet(object.tuning) ? TuningParams.fromJSON(object.tuning) : undefined,
      cloningAudioData: isSet(object.cloningAudioData)
        ? bytesFromBase64(object.cloningAudioData)
        : isSet(object.cloning_audio_data)
        ? bytesFromBase64(object.cloning_audio_data)
        : undefined,
    };
  },

  toJSON(message: SynthesizeRequest): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.textType !== 0) {
      obj.textType = textTypeToJSON(message.textType);
    }
    if (message.voiceId !== "") {
      obj.voiceId = message.voiceId;
    }
    if (message.audioConfig !== undefined) {
      obj.audioConfig = AudioConfig.toJSON(message.audioConfig);
    }
    if (message.preferredProvider !== "") {
      obj.preferredProvider = message.preferredProvider;
    }
    if (message.tuning !== undefined) {
      obj.tuning = TuningParams.toJSON(message.tuning);
    }
    if (message.cloningAudioData !== undefined) {
      obj.cloningAudioData = base64FromBytes(message.cloningAudioData);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SynthesizeRequest>, I>>(base?: I): SynthesizeRequest {
    return SynthesizeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SynthesizeRequest>, I>>(object: I): SynthesizeRequest {
    const message = createBaseSynthesizeRequest();
    message.text = object.text ?? "";
    message.textType = object.textType ?? 0;
    message.voiceId = object.voiceId ?? "";
    message.audioConfig = (object.audioConfig !== undefined && object.audioConfig !== null)
      ? AudioConfig.fromPartial(object.audioConfig)
      : undefined;
    message.preferredProvider = object.preferredProvider ?? "";
    message.tuning = (object.tuning !== undefined && object.tuning !== null)
      ? TuningParams.fromPartial(object.tuning)
      : undefined;
    message.cloningAudioData = object.cloningAudioData ?? undefined;
    return message;
  },
};

function createBaseSynthesizeResponse(): SynthesizeResponse {
  return { audioContent: new Uint8Array(0), contentType: "", providerUsed: "" };
}

export const SynthesizeResponse: MessageFns<SynthesizeResponse> = {
  encode(message: SynthesizeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.audioContent.length !== 0) {
      writer.uint32(10).bytes(message.audioContent);
    }
    if (message.contentType !== "") {
      writer.uint32(18).string(message.contentType);
    }
    if (message.providerUsed !== "") {
      writer.uint32(26).string(message.providerUsed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SynthesizeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSynthesizeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.audioContent = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.contentType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.providerUsed = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SynthesizeResponse {
    return {
      audioContent: isSet(object.audioContent)
        ? bytesFromBase64(object.audioContent)
        : isSet(object.audio_content)
        ? bytesFromBase64(object.audio_content)
        : new Uint8Array(0),
      contentType: isSet(object.contentType)
        ? globalThis.String(object.contentType)
        : isSet(object.content_type)
        ? globalThis.String(object.content_type)
        : "",
      providerUsed: isSet(object.providerUsed)
        ? globalThis.String(object.providerUsed)
        : isSet(object.provider_used)
        ? globalThis.String(object.provider_used)
        : "",
    };
  },

  toJSON(message: SynthesizeResponse): unknown {
    const obj: any = {};
    if (message.audioContent.length !== 0) {
      obj.audioContent = base64FromBytes(message.audioContent);
    }
    if (message.contentType !== "") {
      obj.contentType = message.contentType;
    }
    if (message.providerUsed !== "") {
      obj.providerUsed = message.providerUsed;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SynthesizeResponse>, I>>(base?: I): SynthesizeResponse {
    return SynthesizeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SynthesizeResponse>, I>>(object: I): SynthesizeResponse {
    const message = createBaseSynthesizeResponse();
    message.audioContent = object.audioContent ?? new Uint8Array(0);
    message.contentType = object.contentType ?? "";
    message.providerUsed = object.providerUsed ?? "";
    return message;
  },
};

function createBaseSynthesizeStreamRequest(): SynthesizeStreamRequest {
  return {
    text: "",
    textType: 0,
    voiceId: "",
    audioConfig: undefined,
    preferredProvider: "",
    tuning: undefined,
    cloningAudioData: undefined,
  };
}

export const SynthesizeStreamRequest: MessageFns<SynthesizeStreamRequest> = {
  encode(message: SynthesizeStreamRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.textType !== 0) {
      writer.uint32(16).int32(message.textType);
    }
    if (message.voiceId !== "") {
      writer.uint32(26).string(message.voiceId);
    }
    if (message.audioConfig !== undefined) {
      AudioConfig.encode(message.audioConfig, writer.uint32(34).fork()).join();
    }
    if (message.preferredProvider !== "") {
      writer.uint32(42).string(message.preferredProvider);
    }
    if (message.tuning !== undefined) {
      TuningParams.encode(message.tuning, writer.uint32(50).fork()).join();
    }
    if (message.cloningAudioData !== undefined) {
      writer.uint32(58).bytes(message.cloningAudioData);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SynthesizeStreamRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSynthesizeStreamRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.textType = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.voiceId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.audioConfig = AudioConfig.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.preferredProvider = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.tuning = TuningParams.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.cloningAudioData = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SynthesizeStreamRequest {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      textType: isSet(object.textType)
        ? textTypeFromJSON(object.textType)
        : isSet(object.text_type)
        ? textTypeFromJSON(object.text_type)
        : 0,
      voiceId: isSet(object.voiceId)
        ? globalThis.String(object.voiceId)
        : isSet(object.voice_id)
        ? globalThis.String(object.voice_id)
        : "",
      audioConfig: isSet(object.audioConfig)
        ? AudioConfig.fromJSON(object.audioConfig)
        : isSet(object.audio_config)
        ? AudioConfig.fromJSON(object.audio_config)
        : undefined,
      preferredProvider: isSet(object.preferredProvider)
        ? globalThis.String(object.preferredProvider)
        : isSet(object.preferred_provider)
        ? globalThis.String(object.preferred_provider)
        : "",
      tuning: isSet(object.tuning) ? TuningParams.fromJSON(object.tuning) : undefined,
      cloningAudioData: isSet(object.cloningAudioData)
        ? bytesFromBase64(object.cloningAudioData)
        : isSet(object.cloning_audio_data)
        ? bytesFromBase64(object.cloning_audio_data)
        : undefined,
    };
  },

  toJSON(message: SynthesizeStreamRequest): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.textType !== 0) {
      obj.textType = textTypeToJSON(message.textType);
    }
    if (message.voiceId !== "") {
      obj.voiceId = message.voiceId;
    }
    if (message.audioConfig !== undefined) {
      obj.audioConfig = AudioConfig.toJSON(message.audioConfig);
    }
    if (message.preferredProvider !== "") {
      obj.preferredProvider = message.preferredProvider;
    }
    if (message.tuning !== undefined) {
      obj.tuning = TuningParams.toJSON(message.tuning);
    }
    if (message.cloningAudioData !== undefined) {
      obj.cloningAudioData = base64FromBytes(message.cloningAudioData);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SynthesizeStreamRequest>, I>>(base?: I): SynthesizeStreamRequest {
    return SynthesizeStreamRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SynthesizeStreamRequest>, I>>(object: I): SynthesizeStreamRequest {
    const message = createBaseSynthesizeStreamRequest();
    message.text = object.text ?? "";
    message.textType = object.textType ?? 0;
    message.voiceId = object.voiceId ?? "";
    message.audioConfig = (object.audioConfig !== undefined && object.audioConfig !== null)
      ? AudioConfig.fromPartial(object.audioConfig)
      : undefined;
    message.preferredProvider = object.preferredProvider ?? "";
    message.tuning = (object.tuning !== undefined && object.tuning !== null)
      ? TuningParams.fromPartial(object.tuning)
      : undefined;
    message.cloningAudioData = object.cloningAudioData ?? undefined;
    return message;
  },
};

function createBaseSynthesizeStreamResponse(): SynthesizeStreamResponse {
  return { audioContent: new Uint8Array(0), contentType: "", providerUsed: "" };
}

export const SynthesizeStreamResponse: MessageFns<SynthesizeStreamResponse> = {
  encode(message: SynthesizeStreamResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.audioContent.length !== 0) {
      writer.uint32(10).bytes(message.audioContent);
    }
    if (message.contentType !== "") {
      writer.uint32(18).string(message.contentType);
    }
    if (message.providerUsed !== "") {
      writer.uint32(26).string(message.providerUsed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SynthesizeStreamResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSynthesizeStreamResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.audioContent = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.contentType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.providerUsed = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SynthesizeStreamResponse {
    return {
      audioContent: isSet(object.audioContent)
        ? bytesFromBase64(object.audioContent)
        : isSet(object.audio_content)
        ? bytesFromBase64(object.audio_content)
        : new Uint8Array(0),
      contentType: isSet(object.contentType)
        ? globalThis.String(object.contentType)
        : isSet(object.content_type)
        ? globalThis.String(object.content_type)
        : "",
      providerUsed: isSet(object.providerUsed)
        ? globalThis.String(object.providerUsed)
        : isSet(object.provider_used)
        ? globalThis.String(object.provider_used)
        : "",
    };
  },

  toJSON(message: SynthesizeStreamResponse): unknown {
    const obj: any = {};
    if (message.audioContent.length !== 0) {
      obj.audioContent = base64FromBytes(message.audioContent);
    }
    if (message.contentType !== "") {
      obj.contentType = message.contentType;
    }
    if (message.providerUsed !== "") {
      obj.providerUsed = message.providerUsed;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SynthesizeStreamResponse>, I>>(base?: I): SynthesizeStreamResponse {
    return SynthesizeStreamResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SynthesizeStreamResponse>, I>>(object: I): SynthesizeStreamResponse {
    const message = createBaseSynthesizeStreamResponse();
    message.audioContent = object.audioContent ?? new Uint8Array(0);
    message.contentType = object.contentType ?? "";
    message.providerUsed = object.providerUsed ?? "";
    return message;
  },
};

function createBaseListVoicesRequest(): ListVoicesRequest {
  return { languageCode: "" };
}

export const ListVoicesRequest: MessageFns<ListVoicesRequest> = {
  encode(message: ListVoicesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.languageCode !== "") {
      writer.uint32(10).string(message.languageCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListVoicesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListVoicesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.languageCode = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListVoicesRequest {
    return {
      languageCode: isSet(object.languageCode)
        ? globalThis.String(object.languageCode)
        : isSet(object.language_code)
        ? globalThis.String(object.language_code)
        : "",
    };
  },

  toJSON(message: ListVoicesRequest): unknown {
    const obj: any = {};
    if (message.languageCode !== "") {
      obj.languageCode = message.languageCode;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListVoicesRequest>, I>>(base?: I): ListVoicesRequest {
    return ListVoicesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListVoicesRequest>, I>>(object: I): ListVoicesRequest {
    const message = createBaseListVoicesRequest();
    message.languageCode = object.languageCode ?? "";
    return message;
  },
};

function createBaseListVoicesResponse(): ListVoicesResponse {
  return { voices: [] };
}

export const ListVoicesResponse: MessageFns<ListVoicesResponse> = {
  encode(message: ListVoicesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.voices) {
      VoiceInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListVoicesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListVoicesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.voices.push(VoiceInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListVoicesResponse {
    return {
      voices: globalThis.Array.isArray(object?.voices) ? object.voices.map((e: any) => VoiceInfo.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListVoicesResponse): unknown {
    const obj: any = {};
    if (message.voices?.length) {
      obj.voices = message.voices.map((e) => VoiceInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListVoicesResponse>, I>>(base?: I): ListVoicesResponse {
    return ListVoicesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListVoicesResponse>, I>>(object: I): ListVoicesResponse {
    const message = createBaseListVoicesResponse();
    message.voices = object.voices?.map((e) => VoiceInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseTuningParams(): TuningParams {
  return { temperature: 0, topP: 0, topK: 0, repetitionPenalty: 0, speed: 0 };
}

export const TuningParams: MessageFns<TuningParams> = {
  encode(message: TuningParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.temperature !== 0) {
      writer.uint32(13).float(message.temperature);
    }
    if (message.topP !== 0) {
      writer.uint32(21).float(message.topP);
    }
    if (message.topK !== 0) {
      writer.uint32(24).int32(message.topK);
    }
    if (message.repetitionPenalty !== 0) {
      writer.uint32(37).float(message.repetitionPenalty);
    }
    if (message.speed !== 0) {
      writer.uint32(45).float(message.speed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TuningParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTuningParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 13) {
            break;
          }

          message.temperature = reader.float();
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.topP = reader.float();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.topK = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 37) {
            break;
          }

          message.repetitionPenalty = reader.float();
          continue;
        }
        case 5: {
          if (tag !== 45) {
            break;
          }

          message.speed = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TuningParams {
    return {
      temperature: isSet(object.temperature) ? globalThis.Number(object.temperature) : 0,
      topP: isSet(object.topP)
        ? globalThis.Number(object.topP)
        : isSet(object.top_p)
        ? globalThis.Number(object.top_p)
        : 0,
      topK: isSet(object.topK)
        ? globalThis.Number(object.topK)
        : isSet(object.top_k)
        ? globalThis.Number(object.top_k)
        : 0,
      repetitionPenalty: isSet(object.repetitionPenalty)
        ? globalThis.Number(object.repetitionPenalty)
        : isSet(object.repetition_penalty)
        ? globalThis.Number(object.repetition_penalty)
        : 0,
      speed: isSet(object.speed) ? globalThis.Number(object.speed) : 0,
    };
  },

  toJSON(message: TuningParams): unknown {
    const obj: any = {};
    if (message.temperature !== 0) {
      obj.temperature = message.temperature;
    }
    if (message.topP !== 0) {
      obj.topP = message.topP;
    }
    if (message.topK !== 0) {
      obj.topK = Math.round(message.topK);
    }
    if (message.repetitionPenalty !== 0) {
      obj.repetitionPenalty = message.repetitionPenalty;
    }
    if (message.speed !== 0) {
      obj.speed = message.speed;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TuningParams>, I>>(base?: I): TuningParams {
    return TuningParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TuningParams>, I>>(object: I): TuningParams {
    const message = createBaseTuningParams();
    message.temperature = object.temperature ?? 0;
    message.topP = object.topP ?? 0;
    message.topK = object.topK ?? 0;
    message.repetitionPenalty = object.repetitionPenalty ?? 0;
    message.speed = object.speed ?? 0;
    return message;
  },
};

function createBaseAudioConfig(): AudioConfig {
  return { audioFormat: 0, sampleRateHertz: 0, volumeGainDb: 0 };
}

export const AudioConfig: MessageFns<AudioConfig> = {
  encode(message: AudioConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.audioFormat !== 0) {
      writer.uint32(8).int32(message.audioFormat);
    }
    if (message.sampleRateHertz !== 0) {
      writer.uint32(16).int32(message.sampleRateHertz);
    }
    if (message.volumeGainDb !== 0) {
      writer.uint32(25).double(message.volumeGainDb);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AudioConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAudioConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.audioFormat = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sampleRateHertz = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.volumeGainDb = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AudioConfig {
    return {
      audioFormat: isSet(object.audioFormat)
        ? audioFormatFromJSON(object.audioFormat)
        : isSet(object.audio_format)
        ? audioFormatFromJSON(object.audio_format)
        : 0,
      sampleRateHertz: isSet(object.sampleRateHertz)
        ? globalThis.Number(object.sampleRateHertz)
        : isSet(object.sample_rate_hertz)
        ? globalThis.Number(object.sample_rate_hertz)
        : 0,
      volumeGainDb: isSet(object.volumeGainDb)
        ? globalThis.Number(object.volumeGainDb)
        : isSet(object.volume_gain_db)
        ? globalThis.Number(object.volume_gain_db)
        : 0,
    };
  },

  toJSON(message: AudioConfig): unknown {
    const obj: any = {};
    if (message.audioFormat !== 0) {
      obj.audioFormat = audioFormatToJSON(message.audioFormat);
    }
    if (message.sampleRateHertz !== 0) {
      obj.sampleRateHertz = Math.round(message.sampleRateHertz);
    }
    if (message.volumeGainDb !== 0) {
      obj.volumeGainDb = message.volumeGainDb;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AudioConfig>, I>>(base?: I): AudioConfig {
    return AudioConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AudioConfig>, I>>(object: I): AudioConfig {
    const message = createBaseAudioConfig();
    message.audioFormat = object.audioFormat ?? 0;
    message.sampleRateHertz = object.sampleRateHertz ?? 0;
    message.volumeGainDb = object.volumeGainDb ?? 0;
    return message;
  },
};

function createBaseProsodyConfig(): ProsodyConfig {
  return { rate: 0, pitch: 0, emotion: "" };
}

export const ProsodyConfig: MessageFns<ProsodyConfig> = {
  encode(message: ProsodyConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rate !== 0) {
      writer.uint32(9).double(message.rate);
    }
    if (message.pitch !== 0) {
      writer.uint32(17).double(message.pitch);
    }
    if (message.emotion !== "") {
      writer.uint32(26).string(message.emotion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProsodyConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProsodyConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.rate = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.pitch = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.emotion = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProsodyConfig {
    return {
      rate: isSet(object.rate) ? globalThis.Number(object.rate) : 0,
      pitch: isSet(object.pitch) ? globalThis.Number(object.pitch) : 0,
      emotion: isSet(object.emotion) ? globalThis.String(object.emotion) : "",
    };
  },

  toJSON(message: ProsodyConfig): unknown {
    const obj: any = {};
    if (message.rate !== 0) {
      obj.rate = message.rate;
    }
    if (message.pitch !== 0) {
      obj.pitch = message.pitch;
    }
    if (message.emotion !== "") {
      obj.emotion = message.emotion;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProsodyConfig>, I>>(base?: I): ProsodyConfig {
    return ProsodyConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProsodyConfig>, I>>(object: I): ProsodyConfig {
    const message = createBaseProsodyConfig();
    message.rate = object.rate ?? 0;
    message.pitch = object.pitch ?? 0;
    message.emotion = object.emotion ?? "";
    return message;
  },
};

function createBaseVoiceInfo(): VoiceInfo {
  return { id: "", name: "", language: "", gender: "", styles: [] };
}

export const VoiceInfo: MessageFns<VoiceInfo> = {
  encode(message: VoiceInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.language !== "") {
      writer.uint32(26).string(message.language);
    }
    if (message.gender !== "") {
      writer.uint32(34).string(message.gender);
    }
    for (const v of message.styles) {
      writer.uint32(42).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VoiceInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVoiceInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.language = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.gender = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.styles.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VoiceInfo {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      language: isSet(object.language) ? globalThis.String(object.language) : "",
      gender: isSet(object.gender) ? globalThis.String(object.gender) : "",
      styles: globalThis.Array.isArray(object?.styles) ? object.styles.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: VoiceInfo): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.language !== "") {
      obj.language = message.language;
    }
    if (message.gender !== "") {
      obj.gender = message.gender;
    }
    if (message.styles?.length) {
      obj.styles = message.styles;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VoiceInfo>, I>>(base?: I): VoiceInfo {
    return VoiceInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VoiceInfo>, I>>(object: I): VoiceInfo {
    const message = createBaseVoiceInfo();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.language = object.language ?? "";
    message.gender = object.gender ?? "";
    message.styles = object.styles?.map((e) => e) || [];
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
