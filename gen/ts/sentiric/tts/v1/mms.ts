// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/tts/v1/mms.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.tts.v1";

export interface MmsSynthesizeRequest {
  /** Sentezlenecek ham metin veya SSML. */
  text: string;
  /**
   * ISO dil kodu (örn: "tur" veya "tr").
   * Model bu kodu desteklenen diller listesinde doğrular.
   */
  languageCode: string;
  /**
   * Konuşma hızı çarpanı (Varsayılan: 1.0).
   * MMS VITS mimarisi süre manipülasyonuna izin verir.
   */
  speed: number;
  /** Örnekleme hızı (Varsayılan: Modelin native hızı, genellikle 24000Hz veya 16000Hz). */
  sampleRate: number;
}

export interface MmsSynthesizeResponse {
  /** Üretilen ses verisi (WAV header dahil veya hariç, yapılandırmaya bağlı). */
  audioContent: Uint8Array;
  /** Kullanılan örnekleme hızı. */
  sampleRate: number;
}

export interface MmsSynthesizeStreamRequest {
  text: string;
  languageCode: string;
  speed: number;
  sampleRate: number;
}

export interface MmsSynthesizeStreamResponse {
  /** Ses verisinin bir parçası (Chunk). */
  audioChunk: Uint8Array;
  /** Akışın sonlanıp sonlanmadığını belirtir. */
  isFinal: boolean;
}

function createBaseMmsSynthesizeRequest(): MmsSynthesizeRequest {
  return { text: "", languageCode: "", speed: 0, sampleRate: 0 };
}

export const MmsSynthesizeRequest: MessageFns<MmsSynthesizeRequest> = {
  encode(message: MmsSynthesizeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.languageCode !== "") {
      writer.uint32(18).string(message.languageCode);
    }
    if (message.speed !== 0) {
      writer.uint32(29).float(message.speed);
    }
    if (message.sampleRate !== 0) {
      writer.uint32(32).int32(message.sampleRate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MmsSynthesizeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMmsSynthesizeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.languageCode = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.speed = reader.float();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.sampleRate = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MmsSynthesizeRequest {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      languageCode: isSet(object.languageCode)
        ? globalThis.String(object.languageCode)
        : isSet(object.language_code)
        ? globalThis.String(object.language_code)
        : "",
      speed: isSet(object.speed) ? globalThis.Number(object.speed) : 0,
      sampleRate: isSet(object.sampleRate)
        ? globalThis.Number(object.sampleRate)
        : isSet(object.sample_rate)
        ? globalThis.Number(object.sample_rate)
        : 0,
    };
  },

  toJSON(message: MmsSynthesizeRequest): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.languageCode !== "") {
      obj.languageCode = message.languageCode;
    }
    if (message.speed !== 0) {
      obj.speed = message.speed;
    }
    if (message.sampleRate !== 0) {
      obj.sampleRate = Math.round(message.sampleRate);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MmsSynthesizeRequest>, I>>(base?: I): MmsSynthesizeRequest {
    return MmsSynthesizeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MmsSynthesizeRequest>, I>>(object: I): MmsSynthesizeRequest {
    const message = createBaseMmsSynthesizeRequest();
    message.text = object.text ?? "";
    message.languageCode = object.languageCode ?? "";
    message.speed = object.speed ?? 0;
    message.sampleRate = object.sampleRate ?? 0;
    return message;
  },
};

function createBaseMmsSynthesizeResponse(): MmsSynthesizeResponse {
  return { audioContent: new Uint8Array(0), sampleRate: 0 };
}

export const MmsSynthesizeResponse: MessageFns<MmsSynthesizeResponse> = {
  encode(message: MmsSynthesizeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.audioContent.length !== 0) {
      writer.uint32(10).bytes(message.audioContent);
    }
    if (message.sampleRate !== 0) {
      writer.uint32(16).int32(message.sampleRate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MmsSynthesizeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMmsSynthesizeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.audioContent = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sampleRate = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MmsSynthesizeResponse {
    return {
      audioContent: isSet(object.audioContent)
        ? bytesFromBase64(object.audioContent)
        : isSet(object.audio_content)
        ? bytesFromBase64(object.audio_content)
        : new Uint8Array(0),
      sampleRate: isSet(object.sampleRate)
        ? globalThis.Number(object.sampleRate)
        : isSet(object.sample_rate)
        ? globalThis.Number(object.sample_rate)
        : 0,
    };
  },

  toJSON(message: MmsSynthesizeResponse): unknown {
    const obj: any = {};
    if (message.audioContent.length !== 0) {
      obj.audioContent = base64FromBytes(message.audioContent);
    }
    if (message.sampleRate !== 0) {
      obj.sampleRate = Math.round(message.sampleRate);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MmsSynthesizeResponse>, I>>(base?: I): MmsSynthesizeResponse {
    return MmsSynthesizeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MmsSynthesizeResponse>, I>>(object: I): MmsSynthesizeResponse {
    const message = createBaseMmsSynthesizeResponse();
    message.audioContent = object.audioContent ?? new Uint8Array(0);
    message.sampleRate = object.sampleRate ?? 0;
    return message;
  },
};

function createBaseMmsSynthesizeStreamRequest(): MmsSynthesizeStreamRequest {
  return { text: "", languageCode: "", speed: 0, sampleRate: 0 };
}

export const MmsSynthesizeStreamRequest: MessageFns<MmsSynthesizeStreamRequest> = {
  encode(message: MmsSynthesizeStreamRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.languageCode !== "") {
      writer.uint32(18).string(message.languageCode);
    }
    if (message.speed !== 0) {
      writer.uint32(29).float(message.speed);
    }
    if (message.sampleRate !== 0) {
      writer.uint32(32).int32(message.sampleRate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MmsSynthesizeStreamRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMmsSynthesizeStreamRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.languageCode = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.speed = reader.float();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.sampleRate = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MmsSynthesizeStreamRequest {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      languageCode: isSet(object.languageCode)
        ? globalThis.String(object.languageCode)
        : isSet(object.language_code)
        ? globalThis.String(object.language_code)
        : "",
      speed: isSet(object.speed) ? globalThis.Number(object.speed) : 0,
      sampleRate: isSet(object.sampleRate)
        ? globalThis.Number(object.sampleRate)
        : isSet(object.sample_rate)
        ? globalThis.Number(object.sample_rate)
        : 0,
    };
  },

  toJSON(message: MmsSynthesizeStreamRequest): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.languageCode !== "") {
      obj.languageCode = message.languageCode;
    }
    if (message.speed !== 0) {
      obj.speed = message.speed;
    }
    if (message.sampleRate !== 0) {
      obj.sampleRate = Math.round(message.sampleRate);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MmsSynthesizeStreamRequest>, I>>(base?: I): MmsSynthesizeStreamRequest {
    return MmsSynthesizeStreamRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MmsSynthesizeStreamRequest>, I>>(object: I): MmsSynthesizeStreamRequest {
    const message = createBaseMmsSynthesizeStreamRequest();
    message.text = object.text ?? "";
    message.languageCode = object.languageCode ?? "";
    message.speed = object.speed ?? 0;
    message.sampleRate = object.sampleRate ?? 0;
    return message;
  },
};

function createBaseMmsSynthesizeStreamResponse(): MmsSynthesizeStreamResponse {
  return { audioChunk: new Uint8Array(0), isFinal: false };
}

export const MmsSynthesizeStreamResponse: MessageFns<MmsSynthesizeStreamResponse> = {
  encode(message: MmsSynthesizeStreamResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.audioChunk.length !== 0) {
      writer.uint32(10).bytes(message.audioChunk);
    }
    if (message.isFinal !== false) {
      writer.uint32(16).bool(message.isFinal);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MmsSynthesizeStreamResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMmsSynthesizeStreamResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.audioChunk = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isFinal = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MmsSynthesizeStreamResponse {
    return {
      audioChunk: isSet(object.audioChunk)
        ? bytesFromBase64(object.audioChunk)
        : isSet(object.audio_chunk)
        ? bytesFromBase64(object.audio_chunk)
        : new Uint8Array(0),
      isFinal: isSet(object.isFinal)
        ? globalThis.Boolean(object.isFinal)
        : isSet(object.is_final)
        ? globalThis.Boolean(object.is_final)
        : false,
    };
  },

  toJSON(message: MmsSynthesizeStreamResponse): unknown {
    const obj: any = {};
    if (message.audioChunk.length !== 0) {
      obj.audioChunk = base64FromBytes(message.audioChunk);
    }
    if (message.isFinal !== false) {
      obj.isFinal = message.isFinal;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MmsSynthesizeStreamResponse>, I>>(base?: I): MmsSynthesizeStreamResponse {
    return MmsSynthesizeStreamResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MmsSynthesizeStreamResponse>, I>>(object: I): MmsSynthesizeStreamResponse {
    const message = createBaseMmsSynthesizeStreamResponse();
    message.audioChunk = object.audioChunk ?? new Uint8Array(0);
    message.isFinal = object.isFinal ?? false;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
