// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/tts/v1/coqui.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.tts.v1";

/** --- Unary Messages --- */
export interface CoquiSynthesizeRequest {
  text: string;
  languageCode: string;
  /**
   * [MİMARİ GÜNCELLEME]: Klonlama için referans ses.
   * Bu, speaker_idx'i geçersiz kılar.
   */
  speakerWav?: Uint8Array | undefined;
  temperature: number;
  speed: number;
  topP: number;
  topK: number;
  repetitionPenalty: number;
  outputFormat: string;
  /**
   * [DEEP VOICE FIX]: İstenen örnekleme hızını belirtmek için yeni alan.
   * Eğer 0 ise, motorun doğal hızı (24000Hz) kullanılır.
   */
  sampleRate: number;
}

export interface CoquiSynthesizeResponse {
  audioContent: Uint8Array;
}

/** --- Stream Messages --- */
export interface CoquiSynthesizeStreamRequest {
  text: string;
  languageCode: string;
  /** [MİMARİ GÜNCELLEME]: Klonlama için referans ses. */
  speakerWav?: Uint8Array | undefined;
  temperature: number;
  speed: number;
  topP: number;
  topK: number;
  repetitionPenalty: number;
  outputFormat: string;
  /** [DEEP VOICE FIX]: İstenen örnekleme hızını belirtmek için yeni alan. */
  sampleRate: number;
}

export interface CoquiSynthesizeStreamResponse {
  audioChunk: Uint8Array;
  isFinal: boolean;
}

function createBaseCoquiSynthesizeRequest(): CoquiSynthesizeRequest {
  return {
    text: "",
    languageCode: "",
    speakerWav: undefined,
    temperature: 0,
    speed: 0,
    topP: 0,
    topK: 0,
    repetitionPenalty: 0,
    outputFormat: "",
    sampleRate: 0,
  };
}

export const CoquiSynthesizeRequest: MessageFns<CoquiSynthesizeRequest> = {
  encode(message: CoquiSynthesizeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.languageCode !== "") {
      writer.uint32(18).string(message.languageCode);
    }
    if (message.speakerWav !== undefined) {
      writer.uint32(26).bytes(message.speakerWav);
    }
    if (message.temperature !== 0) {
      writer.uint32(37).float(message.temperature);
    }
    if (message.speed !== 0) {
      writer.uint32(45).float(message.speed);
    }
    if (message.topP !== 0) {
      writer.uint32(53).float(message.topP);
    }
    if (message.topK !== 0) {
      writer.uint32(61).float(message.topK);
    }
    if (message.repetitionPenalty !== 0) {
      writer.uint32(69).float(message.repetitionPenalty);
    }
    if (message.outputFormat !== "") {
      writer.uint32(74).string(message.outputFormat);
    }
    if (message.sampleRate !== 0) {
      writer.uint32(80).int32(message.sampleRate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CoquiSynthesizeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCoquiSynthesizeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.languageCode = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.speakerWav = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 37) {
            break;
          }

          message.temperature = reader.float();
          continue;
        }
        case 5: {
          if (tag !== 45) {
            break;
          }

          message.speed = reader.float();
          continue;
        }
        case 6: {
          if (tag !== 53) {
            break;
          }

          message.topP = reader.float();
          continue;
        }
        case 7: {
          if (tag !== 61) {
            break;
          }

          message.topK = reader.float();
          continue;
        }
        case 8: {
          if (tag !== 69) {
            break;
          }

          message.repetitionPenalty = reader.float();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.outputFormat = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.sampleRate = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CoquiSynthesizeRequest {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      languageCode: isSet(object.languageCode)
        ? globalThis.String(object.languageCode)
        : isSet(object.language_code)
        ? globalThis.String(object.language_code)
        : "",
      speakerWav: isSet(object.speakerWav)
        ? bytesFromBase64(object.speakerWav)
        : isSet(object.speaker_wav)
        ? bytesFromBase64(object.speaker_wav)
        : undefined,
      temperature: isSet(object.temperature) ? globalThis.Number(object.temperature) : 0,
      speed: isSet(object.speed) ? globalThis.Number(object.speed) : 0,
      topP: isSet(object.topP)
        ? globalThis.Number(object.topP)
        : isSet(object.top_p)
        ? globalThis.Number(object.top_p)
        : 0,
      topK: isSet(object.topK)
        ? globalThis.Number(object.topK)
        : isSet(object.top_k)
        ? globalThis.Number(object.top_k)
        : 0,
      repetitionPenalty: isSet(object.repetitionPenalty)
        ? globalThis.Number(object.repetitionPenalty)
        : isSet(object.repetition_penalty)
        ? globalThis.Number(object.repetition_penalty)
        : 0,
      outputFormat: isSet(object.outputFormat)
        ? globalThis.String(object.outputFormat)
        : isSet(object.output_format)
        ? globalThis.String(object.output_format)
        : "",
      sampleRate: isSet(object.sampleRate)
        ? globalThis.Number(object.sampleRate)
        : isSet(object.sample_rate)
        ? globalThis.Number(object.sample_rate)
        : 0,
    };
  },

  toJSON(message: CoquiSynthesizeRequest): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.languageCode !== "") {
      obj.languageCode = message.languageCode;
    }
    if (message.speakerWav !== undefined) {
      obj.speakerWav = base64FromBytes(message.speakerWav);
    }
    if (message.temperature !== 0) {
      obj.temperature = message.temperature;
    }
    if (message.speed !== 0) {
      obj.speed = message.speed;
    }
    if (message.topP !== 0) {
      obj.topP = message.topP;
    }
    if (message.topK !== 0) {
      obj.topK = message.topK;
    }
    if (message.repetitionPenalty !== 0) {
      obj.repetitionPenalty = message.repetitionPenalty;
    }
    if (message.outputFormat !== "") {
      obj.outputFormat = message.outputFormat;
    }
    if (message.sampleRate !== 0) {
      obj.sampleRate = Math.round(message.sampleRate);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CoquiSynthesizeRequest>, I>>(base?: I): CoquiSynthesizeRequest {
    return CoquiSynthesizeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CoquiSynthesizeRequest>, I>>(object: I): CoquiSynthesizeRequest {
    const message = createBaseCoquiSynthesizeRequest();
    message.text = object.text ?? "";
    message.languageCode = object.languageCode ?? "";
    message.speakerWav = object.speakerWav ?? undefined;
    message.temperature = object.temperature ?? 0;
    message.speed = object.speed ?? 0;
    message.topP = object.topP ?? 0;
    message.topK = object.topK ?? 0;
    message.repetitionPenalty = object.repetitionPenalty ?? 0;
    message.outputFormat = object.outputFormat ?? "";
    message.sampleRate = object.sampleRate ?? 0;
    return message;
  },
};

function createBaseCoquiSynthesizeResponse(): CoquiSynthesizeResponse {
  return { audioContent: new Uint8Array(0) };
}

export const CoquiSynthesizeResponse: MessageFns<CoquiSynthesizeResponse> = {
  encode(message: CoquiSynthesizeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.audioContent.length !== 0) {
      writer.uint32(10).bytes(message.audioContent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CoquiSynthesizeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCoquiSynthesizeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.audioContent = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CoquiSynthesizeResponse {
    return {
      audioContent: isSet(object.audioContent)
        ? bytesFromBase64(object.audioContent)
        : isSet(object.audio_content)
        ? bytesFromBase64(object.audio_content)
        : new Uint8Array(0),
    };
  },

  toJSON(message: CoquiSynthesizeResponse): unknown {
    const obj: any = {};
    if (message.audioContent.length !== 0) {
      obj.audioContent = base64FromBytes(message.audioContent);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CoquiSynthesizeResponse>, I>>(base?: I): CoquiSynthesizeResponse {
    return CoquiSynthesizeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CoquiSynthesizeResponse>, I>>(object: I): CoquiSynthesizeResponse {
    const message = createBaseCoquiSynthesizeResponse();
    message.audioContent = object.audioContent ?? new Uint8Array(0);
    return message;
  },
};

function createBaseCoquiSynthesizeStreamRequest(): CoquiSynthesizeStreamRequest {
  return {
    text: "",
    languageCode: "",
    speakerWav: undefined,
    temperature: 0,
    speed: 0,
    topP: 0,
    topK: 0,
    repetitionPenalty: 0,
    outputFormat: "",
    sampleRate: 0,
  };
}

export const CoquiSynthesizeStreamRequest: MessageFns<CoquiSynthesizeStreamRequest> = {
  encode(message: CoquiSynthesizeStreamRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.languageCode !== "") {
      writer.uint32(18).string(message.languageCode);
    }
    if (message.speakerWav !== undefined) {
      writer.uint32(26).bytes(message.speakerWav);
    }
    if (message.temperature !== 0) {
      writer.uint32(37).float(message.temperature);
    }
    if (message.speed !== 0) {
      writer.uint32(45).float(message.speed);
    }
    if (message.topP !== 0) {
      writer.uint32(53).float(message.topP);
    }
    if (message.topK !== 0) {
      writer.uint32(61).float(message.topK);
    }
    if (message.repetitionPenalty !== 0) {
      writer.uint32(69).float(message.repetitionPenalty);
    }
    if (message.outputFormat !== "") {
      writer.uint32(74).string(message.outputFormat);
    }
    if (message.sampleRate !== 0) {
      writer.uint32(80).int32(message.sampleRate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CoquiSynthesizeStreamRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCoquiSynthesizeStreamRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.languageCode = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.speakerWav = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 37) {
            break;
          }

          message.temperature = reader.float();
          continue;
        }
        case 5: {
          if (tag !== 45) {
            break;
          }

          message.speed = reader.float();
          continue;
        }
        case 6: {
          if (tag !== 53) {
            break;
          }

          message.topP = reader.float();
          continue;
        }
        case 7: {
          if (tag !== 61) {
            break;
          }

          message.topK = reader.float();
          continue;
        }
        case 8: {
          if (tag !== 69) {
            break;
          }

          message.repetitionPenalty = reader.float();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.outputFormat = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.sampleRate = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CoquiSynthesizeStreamRequest {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      languageCode: isSet(object.languageCode)
        ? globalThis.String(object.languageCode)
        : isSet(object.language_code)
        ? globalThis.String(object.language_code)
        : "",
      speakerWav: isSet(object.speakerWav)
        ? bytesFromBase64(object.speakerWav)
        : isSet(object.speaker_wav)
        ? bytesFromBase64(object.speaker_wav)
        : undefined,
      temperature: isSet(object.temperature) ? globalThis.Number(object.temperature) : 0,
      speed: isSet(object.speed) ? globalThis.Number(object.speed) : 0,
      topP: isSet(object.topP)
        ? globalThis.Number(object.topP)
        : isSet(object.top_p)
        ? globalThis.Number(object.top_p)
        : 0,
      topK: isSet(object.topK)
        ? globalThis.Number(object.topK)
        : isSet(object.top_k)
        ? globalThis.Number(object.top_k)
        : 0,
      repetitionPenalty: isSet(object.repetitionPenalty)
        ? globalThis.Number(object.repetitionPenalty)
        : isSet(object.repetition_penalty)
        ? globalThis.Number(object.repetition_penalty)
        : 0,
      outputFormat: isSet(object.outputFormat)
        ? globalThis.String(object.outputFormat)
        : isSet(object.output_format)
        ? globalThis.String(object.output_format)
        : "",
      sampleRate: isSet(object.sampleRate)
        ? globalThis.Number(object.sampleRate)
        : isSet(object.sample_rate)
        ? globalThis.Number(object.sample_rate)
        : 0,
    };
  },

  toJSON(message: CoquiSynthesizeStreamRequest): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.languageCode !== "") {
      obj.languageCode = message.languageCode;
    }
    if (message.speakerWav !== undefined) {
      obj.speakerWav = base64FromBytes(message.speakerWav);
    }
    if (message.temperature !== 0) {
      obj.temperature = message.temperature;
    }
    if (message.speed !== 0) {
      obj.speed = message.speed;
    }
    if (message.topP !== 0) {
      obj.topP = message.topP;
    }
    if (message.topK !== 0) {
      obj.topK = message.topK;
    }
    if (message.repetitionPenalty !== 0) {
      obj.repetitionPenalty = message.repetitionPenalty;
    }
    if (message.outputFormat !== "") {
      obj.outputFormat = message.outputFormat;
    }
    if (message.sampleRate !== 0) {
      obj.sampleRate = Math.round(message.sampleRate);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CoquiSynthesizeStreamRequest>, I>>(base?: I): CoquiSynthesizeStreamRequest {
    return CoquiSynthesizeStreamRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CoquiSynthesizeStreamRequest>, I>>(object: I): CoquiSynthesizeStreamRequest {
    const message = createBaseCoquiSynthesizeStreamRequest();
    message.text = object.text ?? "";
    message.languageCode = object.languageCode ?? "";
    message.speakerWav = object.speakerWav ?? undefined;
    message.temperature = object.temperature ?? 0;
    message.speed = object.speed ?? 0;
    message.topP = object.topP ?? 0;
    message.topK = object.topK ?? 0;
    message.repetitionPenalty = object.repetitionPenalty ?? 0;
    message.outputFormat = object.outputFormat ?? "";
    message.sampleRate = object.sampleRate ?? 0;
    return message;
  },
};

function createBaseCoquiSynthesizeStreamResponse(): CoquiSynthesizeStreamResponse {
  return { audioChunk: new Uint8Array(0), isFinal: false };
}

export const CoquiSynthesizeStreamResponse: MessageFns<CoquiSynthesizeStreamResponse> = {
  encode(message: CoquiSynthesizeStreamResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.audioChunk.length !== 0) {
      writer.uint32(10).bytes(message.audioChunk);
    }
    if (message.isFinal !== false) {
      writer.uint32(16).bool(message.isFinal);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CoquiSynthesizeStreamResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCoquiSynthesizeStreamResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.audioChunk = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isFinal = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CoquiSynthesizeStreamResponse {
    return {
      audioChunk: isSet(object.audioChunk)
        ? bytesFromBase64(object.audioChunk)
        : isSet(object.audio_chunk)
        ? bytesFromBase64(object.audio_chunk)
        : new Uint8Array(0),
      isFinal: isSet(object.isFinal)
        ? globalThis.Boolean(object.isFinal)
        : isSet(object.is_final)
        ? globalThis.Boolean(object.is_final)
        : false,
    };
  },

  toJSON(message: CoquiSynthesizeStreamResponse): unknown {
    const obj: any = {};
    if (message.audioChunk.length !== 0) {
      obj.audioChunk = base64FromBytes(message.audioChunk);
    }
    if (message.isFinal !== false) {
      obj.isFinal = message.isFinal;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CoquiSynthesizeStreamResponse>, I>>(base?: I): CoquiSynthesizeStreamResponse {
    return CoquiSynthesizeStreamResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CoquiSynthesizeStreamResponse>, I>>(
    object: I,
  ): CoquiSynthesizeStreamResponse {
    const message = createBaseCoquiSynthesizeStreamResponse();
    message.audioChunk = object.audioChunk ?? new Uint8Array(0);
    message.isFinal = object.isFinal ?? false;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
