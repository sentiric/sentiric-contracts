// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: sentiric/audio_gen/v1/gateway.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.audio_gen.v1";

export interface SubmitAudioJobRequest {
  tenantId: string;
  traceId: string;
  prompt: string;
  /** "music" (ACE-Step) veya "sfx" (AudioGen) */
  audioType: string;
  durationSeconds: number;
}

export interface SubmitAudioJobResponse {
  accepted: boolean;
  jobId: string;
}

function createBaseSubmitAudioJobRequest(): SubmitAudioJobRequest {
  return { tenantId: "", traceId: "", prompt: "", audioType: "", durationSeconds: 0 };
}

export const SubmitAudioJobRequest: MessageFns<SubmitAudioJobRequest> = {
  encode(message: SubmitAudioJobRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tenantId !== "") {
      writer.uint32(10).string(message.tenantId);
    }
    if (message.traceId !== "") {
      writer.uint32(18).string(message.traceId);
    }
    if (message.prompt !== "") {
      writer.uint32(26).string(message.prompt);
    }
    if (message.audioType !== "") {
      writer.uint32(34).string(message.audioType);
    }
    if (message.durationSeconds !== 0) {
      writer.uint32(40).int32(message.durationSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubmitAudioJobRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubmitAudioJobRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tenantId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.traceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.prompt = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.audioType = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.durationSeconds = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubmitAudioJobRequest {
    return {
      tenantId: isSet(object.tenantId)
        ? globalThis.String(object.tenantId)
        : isSet(object.tenant_id)
        ? globalThis.String(object.tenant_id)
        : "",
      traceId: isSet(object.traceId)
        ? globalThis.String(object.traceId)
        : isSet(object.trace_id)
        ? globalThis.String(object.trace_id)
        : "",
      prompt: isSet(object.prompt) ? globalThis.String(object.prompt) : "",
      audioType: isSet(object.audioType)
        ? globalThis.String(object.audioType)
        : isSet(object.audio_type)
        ? globalThis.String(object.audio_type)
        : "",
      durationSeconds: isSet(object.durationSeconds)
        ? globalThis.Number(object.durationSeconds)
        : isSet(object.duration_seconds)
        ? globalThis.Number(object.duration_seconds)
        : 0,
    };
  },

  toJSON(message: SubmitAudioJobRequest): unknown {
    const obj: any = {};
    if (message.tenantId !== "") {
      obj.tenantId = message.tenantId;
    }
    if (message.traceId !== "") {
      obj.traceId = message.traceId;
    }
    if (message.prompt !== "") {
      obj.prompt = message.prompt;
    }
    if (message.audioType !== "") {
      obj.audioType = message.audioType;
    }
    if (message.durationSeconds !== 0) {
      obj.durationSeconds = Math.round(message.durationSeconds);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubmitAudioJobRequest>, I>>(base?: I): SubmitAudioJobRequest {
    return SubmitAudioJobRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubmitAudioJobRequest>, I>>(object: I): SubmitAudioJobRequest {
    const message = createBaseSubmitAudioJobRequest();
    message.tenantId = object.tenantId ?? "";
    message.traceId = object.traceId ?? "";
    message.prompt = object.prompt ?? "";
    message.audioType = object.audioType ?? "";
    message.durationSeconds = object.durationSeconds ?? 0;
    return message;
  },
};

function createBaseSubmitAudioJobResponse(): SubmitAudioJobResponse {
  return { accepted: false, jobId: "" };
}

export const SubmitAudioJobResponse: MessageFns<SubmitAudioJobResponse> = {
  encode(message: SubmitAudioJobResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accepted !== false) {
      writer.uint32(8).bool(message.accepted);
    }
    if (message.jobId !== "") {
      writer.uint32(18).string(message.jobId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubmitAudioJobResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubmitAudioJobResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.accepted = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubmitAudioJobResponse {
    return {
      accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false,
      jobId: isSet(object.jobId)
        ? globalThis.String(object.jobId)
        : isSet(object.job_id)
        ? globalThis.String(object.job_id)
        : "",
    };
  },

  toJSON(message: SubmitAudioJobResponse): unknown {
    const obj: any = {};
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    if (message.jobId !== "") {
      obj.jobId = message.jobId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubmitAudioJobResponse>, I>>(base?: I): SubmitAudioJobResponse {
    return SubmitAudioJobResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubmitAudioJobResponse>, I>>(object: I): SubmitAudioJobResponse {
    const message = createBaseSubmitAudioJobResponse();
    message.accepted = object.accepted ?? false;
    message.jobId = object.jobId ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
