// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/event/v1/event.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../../google/protobuf/timestamp";
import { ResolveDialplanResponse } from "../../dialplan/v1/dialplan";
import { Contact, User } from "../../user/v1/user";

export const protobufPackage = "sentiric.event.v1";

/**
 * MediaInfo, bir çağrının medya (RTP) oturumuyla ilgili temel bilgileri içerir.
 * Bu yapı, `map<string, string>` gibi belirsiz ve hataya açık yapıların
 * yerine geçerek tip güvenliği sağlar.
 */
export interface MediaInfo {
  /** Arayan tarafın RTP paketlerini gönderdiği IP adresi ve portu (örn: "1.2.3.4:12345"). */
  callerRtpAddr: string;
  /** `media-service` tarafından bu çağrı için tahsis edilen sunucu tarafı RTP portu. */
  serverRtpPort: number;
}

/**
 * CallStartedEvent, bir SIP çağrısının `sip-signaling` tarafından başarıyla
 * kurulduğunu ve platformun iç mantığı tarafından işlenmeye hazır olduğunu bildirir.
 * Bu olay, genellikle `agent-service` tarafından tüketilir.
 */
export interface CallStartedEvent {
  /** Olayın türü, standart olarak "call.started". */
  eventType: string;
  /** Tüm çağrı yaşam döngüsü boyunca işlemleri takip etmek için kullanılan benzersiz kimlik. */
  traceId: string;
  /** Çağrının SIP Call-ID'si. */
  callId: string;
  /** Arayanın SIP URI'si. */
  fromUri: string;
  /** Arananın SIP URI'si. */
  toUri: string;
  /** Olayın UTC olarak oluşturulma zamanı. */
  timestamp?:
    | Date
    | undefined;
  /** `dialplan-service`'ten dönen, çağrının nasıl işleneceğine dair tüm zenginleştirilmiş bilgi. */
  dialplanResolution?:
    | ResolveDialplanResponse
    | undefined;
  /**
   * DEĞİŞİKLİK: `map<string, string>` yerine tip-güvenli `MediaInfo` mesajı kullanılıyor.
   * Bu, tüketen servislerde (örn: agent-service) anahtar adı hatalarını ve tip dönüşüm
   * problemlerini ortadan kaldırır.
   */
  mediaInfo?: MediaInfo | undefined;
}

/**
 * UserIdentifiedForCallEvent, bir çağrı sırasında arayanın kimliğinin
 * `user-service` tarafından belirlendiğini veya yeni bir kullanıcı olarak
 * oluşturulduğunu bildirir.
 */
export interface UserIdentifiedForCallEvent {
  /** "user.identified.for_call" veya "user.created.for_call" */
  eventType: string;
  traceId: string;
  callId: string;
  timestamp?: Date | undefined;
  user?: User | undefined;
  contact?: Contact | undefined;
}

/**
 * CallRecordingAvailableEvent, bir çağrının ses kaydının işlenip
 * depolama alanına (örn: S3) yüklendiğini bildirir.
 */
export interface CallRecordingAvailableEvent {
  /** "call.recording.available" */
  eventType: string;
  traceId: string;
  callId: string;
  timestamp?:
    | Date
    | undefined;
  /** Kaydın depolama alanındaki URI'si (örn: "s3://sentiric/recordings/call123.wav"). */
  recordingUri: string;
  /** Kayda erişilebilecek genel URL (eğer varsa). */
  publicUrl: string;
}

/** CallEndedEvent, bir çağrının sonlandığını bildirir. */
export interface CallEndedEvent {
  /** "call.ended" */
  eventType: string;
  traceId: string;
  callId: string;
  timestamp?:
    | Date
    | undefined;
  /** Çağrının sonlanma nedeni (örn: "normal_clearing", "terminated_by_request"). */
  reason: string;
}

/**
 * GenericEvent, platform genelinde diğer (çağrı dışı) asenkron olayları kapsar.
 * Örn: UserCreated, DialplanUpdated, RecipeInstalled.
 */
export interface GenericEvent {
  /** Örn: "user.created", "dialplan.updated" */
  eventType: string;
  traceId: string;
  timestamp?: Date | undefined;
  tenantId: string;
  /** Olayla ilgili seri hale getirilmiş (Protobuf Any veya JSON dize) payload. */
  payloadJson: string;
}

function createBaseMediaInfo(): MediaInfo {
  return { callerRtpAddr: "", serverRtpPort: 0 };
}

export const MediaInfo: MessageFns<MediaInfo> = {
  encode(message: MediaInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.callerRtpAddr !== "") {
      writer.uint32(10).string(message.callerRtpAddr);
    }
    if (message.serverRtpPort !== 0) {
      writer.uint32(16).uint32(message.serverRtpPort);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MediaInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMediaInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.callerRtpAddr = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.serverRtpPort = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MediaInfo {
    return {
      callerRtpAddr: isSet(object.callerRtpAddr)
        ? globalThis.String(object.callerRtpAddr)
        : isSet(object.caller_rtp_addr)
        ? globalThis.String(object.caller_rtp_addr)
        : "",
      serverRtpPort: isSet(object.serverRtpPort)
        ? globalThis.Number(object.serverRtpPort)
        : isSet(object.server_rtp_port)
        ? globalThis.Number(object.server_rtp_port)
        : 0,
    };
  },

  toJSON(message: MediaInfo): unknown {
    const obj: any = {};
    if (message.callerRtpAddr !== "") {
      obj.callerRtpAddr = message.callerRtpAddr;
    }
    if (message.serverRtpPort !== 0) {
      obj.serverRtpPort = Math.round(message.serverRtpPort);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MediaInfo>, I>>(base?: I): MediaInfo {
    return MediaInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MediaInfo>, I>>(object: I): MediaInfo {
    const message = createBaseMediaInfo();
    message.callerRtpAddr = object.callerRtpAddr ?? "";
    message.serverRtpPort = object.serverRtpPort ?? 0;
    return message;
  },
};

function createBaseCallStartedEvent(): CallStartedEvent {
  return {
    eventType: "",
    traceId: "",
    callId: "",
    fromUri: "",
    toUri: "",
    timestamp: undefined,
    dialplanResolution: undefined,
    mediaInfo: undefined,
  };
}

export const CallStartedEvent: MessageFns<CallStartedEvent> = {
  encode(message: CallStartedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventType !== "") {
      writer.uint32(10).string(message.eventType);
    }
    if (message.traceId !== "") {
      writer.uint32(18).string(message.traceId);
    }
    if (message.callId !== "") {
      writer.uint32(26).string(message.callId);
    }
    if (message.fromUri !== "") {
      writer.uint32(34).string(message.fromUri);
    }
    if (message.toUri !== "") {
      writer.uint32(42).string(message.toUri);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(50).fork()).join();
    }
    if (message.dialplanResolution !== undefined) {
      ResolveDialplanResponse.encode(message.dialplanResolution, writer.uint32(58).fork()).join();
    }
    if (message.mediaInfo !== undefined) {
      MediaInfo.encode(message.mediaInfo, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CallStartedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCallStartedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventType = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.traceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.callId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.fromUri = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.toUri = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.dialplanResolution = ResolveDialplanResponse.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.mediaInfo = MediaInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CallStartedEvent {
    return {
      eventType: isSet(object.eventType)
        ? globalThis.String(object.eventType)
        : isSet(object.event_type)
        ? globalThis.String(object.event_type)
        : "",
      traceId: isSet(object.traceId)
        ? globalThis.String(object.traceId)
        : isSet(object.trace_id)
        ? globalThis.String(object.trace_id)
        : "",
      callId: isSet(object.callId)
        ? globalThis.String(object.callId)
        : isSet(object.call_id)
        ? globalThis.String(object.call_id)
        : "",
      fromUri: isSet(object.fromUri)
        ? globalThis.String(object.fromUri)
        : isSet(object.from_uri)
        ? globalThis.String(object.from_uri)
        : "",
      toUri: isSet(object.toUri)
        ? globalThis.String(object.toUri)
        : isSet(object.to_uri)
        ? globalThis.String(object.to_uri)
        : "",
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      dialplanResolution: isSet(object.dialplanResolution)
        ? ResolveDialplanResponse.fromJSON(object.dialplanResolution)
        : isSet(object.dialplan_resolution)
        ? ResolveDialplanResponse.fromJSON(object.dialplan_resolution)
        : undefined,
      mediaInfo: isSet(object.mediaInfo)
        ? MediaInfo.fromJSON(object.mediaInfo)
        : isSet(object.media_info)
        ? MediaInfo.fromJSON(object.media_info)
        : undefined,
    };
  },

  toJSON(message: CallStartedEvent): unknown {
    const obj: any = {};
    if (message.eventType !== "") {
      obj.eventType = message.eventType;
    }
    if (message.traceId !== "") {
      obj.traceId = message.traceId;
    }
    if (message.callId !== "") {
      obj.callId = message.callId;
    }
    if (message.fromUri !== "") {
      obj.fromUri = message.fromUri;
    }
    if (message.toUri !== "") {
      obj.toUri = message.toUri;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.dialplanResolution !== undefined) {
      obj.dialplanResolution = ResolveDialplanResponse.toJSON(message.dialplanResolution);
    }
    if (message.mediaInfo !== undefined) {
      obj.mediaInfo = MediaInfo.toJSON(message.mediaInfo);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CallStartedEvent>, I>>(base?: I): CallStartedEvent {
    return CallStartedEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CallStartedEvent>, I>>(object: I): CallStartedEvent {
    const message = createBaseCallStartedEvent();
    message.eventType = object.eventType ?? "";
    message.traceId = object.traceId ?? "";
    message.callId = object.callId ?? "";
    message.fromUri = object.fromUri ?? "";
    message.toUri = object.toUri ?? "";
    message.timestamp = object.timestamp ?? undefined;
    message.dialplanResolution = (object.dialplanResolution !== undefined && object.dialplanResolution !== null)
      ? ResolveDialplanResponse.fromPartial(object.dialplanResolution)
      : undefined;
    message.mediaInfo = (object.mediaInfo !== undefined && object.mediaInfo !== null)
      ? MediaInfo.fromPartial(object.mediaInfo)
      : undefined;
    return message;
  },
};

function createBaseUserIdentifiedForCallEvent(): UserIdentifiedForCallEvent {
  return { eventType: "", traceId: "", callId: "", timestamp: undefined, user: undefined, contact: undefined };
}

export const UserIdentifiedForCallEvent: MessageFns<UserIdentifiedForCallEvent> = {
  encode(message: UserIdentifiedForCallEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventType !== "") {
      writer.uint32(10).string(message.eventType);
    }
    if (message.traceId !== "") {
      writer.uint32(18).string(message.traceId);
    }
    if (message.callId !== "") {
      writer.uint32(26).string(message.callId);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(34).fork()).join();
    }
    if (message.user !== undefined) {
      User.encode(message.user, writer.uint32(42).fork()).join();
    }
    if (message.contact !== undefined) {
      Contact.encode(message.contact, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserIdentifiedForCallEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserIdentifiedForCallEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventType = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.traceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.callId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.user = User.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.contact = Contact.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserIdentifiedForCallEvent {
    return {
      eventType: isSet(object.eventType)
        ? globalThis.String(object.eventType)
        : isSet(object.event_type)
        ? globalThis.String(object.event_type)
        : "",
      traceId: isSet(object.traceId)
        ? globalThis.String(object.traceId)
        : isSet(object.trace_id)
        ? globalThis.String(object.trace_id)
        : "",
      callId: isSet(object.callId)
        ? globalThis.String(object.callId)
        : isSet(object.call_id)
        ? globalThis.String(object.call_id)
        : "",
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      user: isSet(object.user) ? User.fromJSON(object.user) : undefined,
      contact: isSet(object.contact) ? Contact.fromJSON(object.contact) : undefined,
    };
  },

  toJSON(message: UserIdentifiedForCallEvent): unknown {
    const obj: any = {};
    if (message.eventType !== "") {
      obj.eventType = message.eventType;
    }
    if (message.traceId !== "") {
      obj.traceId = message.traceId;
    }
    if (message.callId !== "") {
      obj.callId = message.callId;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.user !== undefined) {
      obj.user = User.toJSON(message.user);
    }
    if (message.contact !== undefined) {
      obj.contact = Contact.toJSON(message.contact);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserIdentifiedForCallEvent>, I>>(base?: I): UserIdentifiedForCallEvent {
    return UserIdentifiedForCallEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserIdentifiedForCallEvent>, I>>(object: I): UserIdentifiedForCallEvent {
    const message = createBaseUserIdentifiedForCallEvent();
    message.eventType = object.eventType ?? "";
    message.traceId = object.traceId ?? "";
    message.callId = object.callId ?? "";
    message.timestamp = object.timestamp ?? undefined;
    message.user = (object.user !== undefined && object.user !== null) ? User.fromPartial(object.user) : undefined;
    message.contact = (object.contact !== undefined && object.contact !== null)
      ? Contact.fromPartial(object.contact)
      : undefined;
    return message;
  },
};

function createBaseCallRecordingAvailableEvent(): CallRecordingAvailableEvent {
  return { eventType: "", traceId: "", callId: "", timestamp: undefined, recordingUri: "", publicUrl: "" };
}

export const CallRecordingAvailableEvent: MessageFns<CallRecordingAvailableEvent> = {
  encode(message: CallRecordingAvailableEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventType !== "") {
      writer.uint32(10).string(message.eventType);
    }
    if (message.traceId !== "") {
      writer.uint32(18).string(message.traceId);
    }
    if (message.callId !== "") {
      writer.uint32(26).string(message.callId);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(34).fork()).join();
    }
    if (message.recordingUri !== "") {
      writer.uint32(42).string(message.recordingUri);
    }
    if (message.publicUrl !== "") {
      writer.uint32(50).string(message.publicUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CallRecordingAvailableEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCallRecordingAvailableEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventType = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.traceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.callId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.recordingUri = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.publicUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CallRecordingAvailableEvent {
    return {
      eventType: isSet(object.eventType)
        ? globalThis.String(object.eventType)
        : isSet(object.event_type)
        ? globalThis.String(object.event_type)
        : "",
      traceId: isSet(object.traceId)
        ? globalThis.String(object.traceId)
        : isSet(object.trace_id)
        ? globalThis.String(object.trace_id)
        : "",
      callId: isSet(object.callId)
        ? globalThis.String(object.callId)
        : isSet(object.call_id)
        ? globalThis.String(object.call_id)
        : "",
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      recordingUri: isSet(object.recordingUri)
        ? globalThis.String(object.recordingUri)
        : isSet(object.recording_uri)
        ? globalThis.String(object.recording_uri)
        : "",
      publicUrl: isSet(object.publicUrl)
        ? globalThis.String(object.publicUrl)
        : isSet(object.public_url)
        ? globalThis.String(object.public_url)
        : "",
    };
  },

  toJSON(message: CallRecordingAvailableEvent): unknown {
    const obj: any = {};
    if (message.eventType !== "") {
      obj.eventType = message.eventType;
    }
    if (message.traceId !== "") {
      obj.traceId = message.traceId;
    }
    if (message.callId !== "") {
      obj.callId = message.callId;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.recordingUri !== "") {
      obj.recordingUri = message.recordingUri;
    }
    if (message.publicUrl !== "") {
      obj.publicUrl = message.publicUrl;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CallRecordingAvailableEvent>, I>>(base?: I): CallRecordingAvailableEvent {
    return CallRecordingAvailableEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CallRecordingAvailableEvent>, I>>(object: I): CallRecordingAvailableEvent {
    const message = createBaseCallRecordingAvailableEvent();
    message.eventType = object.eventType ?? "";
    message.traceId = object.traceId ?? "";
    message.callId = object.callId ?? "";
    message.timestamp = object.timestamp ?? undefined;
    message.recordingUri = object.recordingUri ?? "";
    message.publicUrl = object.publicUrl ?? "";
    return message;
  },
};

function createBaseCallEndedEvent(): CallEndedEvent {
  return { eventType: "", traceId: "", callId: "", timestamp: undefined, reason: "" };
}

export const CallEndedEvent: MessageFns<CallEndedEvent> = {
  encode(message: CallEndedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventType !== "") {
      writer.uint32(10).string(message.eventType);
    }
    if (message.traceId !== "") {
      writer.uint32(18).string(message.traceId);
    }
    if (message.callId !== "") {
      writer.uint32(26).string(message.callId);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(34).fork()).join();
    }
    if (message.reason !== "") {
      writer.uint32(42).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CallEndedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCallEndedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventType = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.traceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.callId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CallEndedEvent {
    return {
      eventType: isSet(object.eventType)
        ? globalThis.String(object.eventType)
        : isSet(object.event_type)
        ? globalThis.String(object.event_type)
        : "",
      traceId: isSet(object.traceId)
        ? globalThis.String(object.traceId)
        : isSet(object.trace_id)
        ? globalThis.String(object.trace_id)
        : "",
      callId: isSet(object.callId)
        ? globalThis.String(object.callId)
        : isSet(object.call_id)
        ? globalThis.String(object.call_id)
        : "",
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: CallEndedEvent): unknown {
    const obj: any = {};
    if (message.eventType !== "") {
      obj.eventType = message.eventType;
    }
    if (message.traceId !== "") {
      obj.traceId = message.traceId;
    }
    if (message.callId !== "") {
      obj.callId = message.callId;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CallEndedEvent>, I>>(base?: I): CallEndedEvent {
    return CallEndedEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CallEndedEvent>, I>>(object: I): CallEndedEvent {
    const message = createBaseCallEndedEvent();
    message.eventType = object.eventType ?? "";
    message.traceId = object.traceId ?? "";
    message.callId = object.callId ?? "";
    message.timestamp = object.timestamp ?? undefined;
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseGenericEvent(): GenericEvent {
  return { eventType: "", traceId: "", timestamp: undefined, tenantId: "", payloadJson: "" };
}

export const GenericEvent: MessageFns<GenericEvent> = {
  encode(message: GenericEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventType !== "") {
      writer.uint32(10).string(message.eventType);
    }
    if (message.traceId !== "") {
      writer.uint32(18).string(message.traceId);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(26).fork()).join();
    }
    if (message.tenantId !== "") {
      writer.uint32(34).string(message.tenantId);
    }
    if (message.payloadJson !== "") {
      writer.uint32(42).string(message.payloadJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenericEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenericEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventType = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.traceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.tenantId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.payloadJson = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenericEvent {
    return {
      eventType: isSet(object.eventType)
        ? globalThis.String(object.eventType)
        : isSet(object.event_type)
        ? globalThis.String(object.event_type)
        : "",
      traceId: isSet(object.traceId)
        ? globalThis.String(object.traceId)
        : isSet(object.trace_id)
        ? globalThis.String(object.trace_id)
        : "",
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      tenantId: isSet(object.tenantId)
        ? globalThis.String(object.tenantId)
        : isSet(object.tenant_id)
        ? globalThis.String(object.tenant_id)
        : "",
      payloadJson: isSet(object.payloadJson)
        ? globalThis.String(object.payloadJson)
        : isSet(object.payload_json)
        ? globalThis.String(object.payload_json)
        : "",
    };
  },

  toJSON(message: GenericEvent): unknown {
    const obj: any = {};
    if (message.eventType !== "") {
      obj.eventType = message.eventType;
    }
    if (message.traceId !== "") {
      obj.traceId = message.traceId;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.tenantId !== "") {
      obj.tenantId = message.tenantId;
    }
    if (message.payloadJson !== "") {
      obj.payloadJson = message.payloadJson;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenericEvent>, I>>(base?: I): GenericEvent {
    return GenericEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenericEvent>, I>>(object: I): GenericEvent {
    const message = createBaseGenericEvent();
    message.eventType = object.eventType ?? "";
    message.traceId = object.traceId ?? "";
    message.timestamp = object.timestamp ?? undefined;
    message.tenantId = object.tenantId ?? "";
    message.payloadJson = object.payloadJson ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
