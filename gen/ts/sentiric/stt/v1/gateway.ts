// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/stt/v1/gateway.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.stt.v1";

/** File: proto/sentiric/stt/v1/gateway.proto */

/** Gateway request */
export interface TranscribeRequest {
  audioContent: Uint8Array;
  languageCode: string;
}

/** Gateway response */
export interface TranscribeResponse {
  transcription: string;
}

export interface TranscribeStreamRequest {
  audioChunk: Uint8Array;
}

export interface TranscribeStreamResponse {
  partialTranscription: string;
  isFinal: boolean;
  /** [YENİ - CRYSTALLINE VIZYONU] Duyuşsal Zeka Metrikleri */
  genderProxy: string;
  emotionProxy: string;
  arousal: number;
  valence: number;
}

function createBaseTranscribeRequest(): TranscribeRequest {
  return { audioContent: new Uint8Array(0), languageCode: "" };
}

export const TranscribeRequest: MessageFns<TranscribeRequest> = {
  encode(message: TranscribeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.audioContent.length !== 0) {
      writer.uint32(10).bytes(message.audioContent);
    }
    if (message.languageCode !== "") {
      writer.uint32(18).string(message.languageCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TranscribeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTranscribeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.audioContent = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.languageCode = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TranscribeRequest {
    return {
      audioContent: isSet(object.audioContent)
        ? bytesFromBase64(object.audioContent)
        : isSet(object.audio_content)
        ? bytesFromBase64(object.audio_content)
        : new Uint8Array(0),
      languageCode: isSet(object.languageCode)
        ? globalThis.String(object.languageCode)
        : isSet(object.language_code)
        ? globalThis.String(object.language_code)
        : "",
    };
  },

  toJSON(message: TranscribeRequest): unknown {
    const obj: any = {};
    if (message.audioContent.length !== 0) {
      obj.audioContent = base64FromBytes(message.audioContent);
    }
    if (message.languageCode !== "") {
      obj.languageCode = message.languageCode;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TranscribeRequest>, I>>(base?: I): TranscribeRequest {
    return TranscribeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TranscribeRequest>, I>>(object: I): TranscribeRequest {
    const message = createBaseTranscribeRequest();
    message.audioContent = object.audioContent ?? new Uint8Array(0);
    message.languageCode = object.languageCode ?? "";
    return message;
  },
};

function createBaseTranscribeResponse(): TranscribeResponse {
  return { transcription: "" };
}

export const TranscribeResponse: MessageFns<TranscribeResponse> = {
  encode(message: TranscribeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.transcription !== "") {
      writer.uint32(10).string(message.transcription);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TranscribeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTranscribeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.transcription = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TranscribeResponse {
    return { transcription: isSet(object.transcription) ? globalThis.String(object.transcription) : "" };
  },

  toJSON(message: TranscribeResponse): unknown {
    const obj: any = {};
    if (message.transcription !== "") {
      obj.transcription = message.transcription;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TranscribeResponse>, I>>(base?: I): TranscribeResponse {
    return TranscribeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TranscribeResponse>, I>>(object: I): TranscribeResponse {
    const message = createBaseTranscribeResponse();
    message.transcription = object.transcription ?? "";
    return message;
  },
};

function createBaseTranscribeStreamRequest(): TranscribeStreamRequest {
  return { audioChunk: new Uint8Array(0) };
}

export const TranscribeStreamRequest: MessageFns<TranscribeStreamRequest> = {
  encode(message: TranscribeStreamRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.audioChunk.length !== 0) {
      writer.uint32(10).bytes(message.audioChunk);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TranscribeStreamRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTranscribeStreamRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.audioChunk = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TranscribeStreamRequest {
    return {
      audioChunk: isSet(object.audioChunk)
        ? bytesFromBase64(object.audioChunk)
        : isSet(object.audio_chunk)
        ? bytesFromBase64(object.audio_chunk)
        : new Uint8Array(0),
    };
  },

  toJSON(message: TranscribeStreamRequest): unknown {
    const obj: any = {};
    if (message.audioChunk.length !== 0) {
      obj.audioChunk = base64FromBytes(message.audioChunk);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TranscribeStreamRequest>, I>>(base?: I): TranscribeStreamRequest {
    return TranscribeStreamRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TranscribeStreamRequest>, I>>(object: I): TranscribeStreamRequest {
    const message = createBaseTranscribeStreamRequest();
    message.audioChunk = object.audioChunk ?? new Uint8Array(0);
    return message;
  },
};

function createBaseTranscribeStreamResponse(): TranscribeStreamResponse {
  return { partialTranscription: "", isFinal: false, genderProxy: "", emotionProxy: "", arousal: 0, valence: 0 };
}

export const TranscribeStreamResponse: MessageFns<TranscribeStreamResponse> = {
  encode(message: TranscribeStreamResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.partialTranscription !== "") {
      writer.uint32(10).string(message.partialTranscription);
    }
    if (message.isFinal !== false) {
      writer.uint32(16).bool(message.isFinal);
    }
    if (message.genderProxy !== "") {
      writer.uint32(26).string(message.genderProxy);
    }
    if (message.emotionProxy !== "") {
      writer.uint32(34).string(message.emotionProxy);
    }
    if (message.arousal !== 0) {
      writer.uint32(45).float(message.arousal);
    }
    if (message.valence !== 0) {
      writer.uint32(53).float(message.valence);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TranscribeStreamResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTranscribeStreamResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.partialTranscription = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isFinal = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.genderProxy = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.emotionProxy = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 45) {
            break;
          }

          message.arousal = reader.float();
          continue;
        }
        case 6: {
          if (tag !== 53) {
            break;
          }

          message.valence = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TranscribeStreamResponse {
    return {
      partialTranscription: isSet(object.partialTranscription)
        ? globalThis.String(object.partialTranscription)
        : isSet(object.partial_transcription)
        ? globalThis.String(object.partial_transcription)
        : "",
      isFinal: isSet(object.isFinal)
        ? globalThis.Boolean(object.isFinal)
        : isSet(object.is_final)
        ? globalThis.Boolean(object.is_final)
        : false,
      genderProxy: isSet(object.genderProxy)
        ? globalThis.String(object.genderProxy)
        : isSet(object.gender_proxy)
        ? globalThis.String(object.gender_proxy)
        : "",
      emotionProxy: isSet(object.emotionProxy)
        ? globalThis.String(object.emotionProxy)
        : isSet(object.emotion_proxy)
        ? globalThis.String(object.emotion_proxy)
        : "",
      arousal: isSet(object.arousal) ? globalThis.Number(object.arousal) : 0,
      valence: isSet(object.valence) ? globalThis.Number(object.valence) : 0,
    };
  },

  toJSON(message: TranscribeStreamResponse): unknown {
    const obj: any = {};
    if (message.partialTranscription !== "") {
      obj.partialTranscription = message.partialTranscription;
    }
    if (message.isFinal !== false) {
      obj.isFinal = message.isFinal;
    }
    if (message.genderProxy !== "") {
      obj.genderProxy = message.genderProxy;
    }
    if (message.emotionProxy !== "") {
      obj.emotionProxy = message.emotionProxy;
    }
    if (message.arousal !== 0) {
      obj.arousal = message.arousal;
    }
    if (message.valence !== 0) {
      obj.valence = message.valence;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TranscribeStreamResponse>, I>>(base?: I): TranscribeStreamResponse {
    return TranscribeStreamResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TranscribeStreamResponse>, I>>(object: I): TranscribeStreamResponse {
    const message = createBaseTranscribeStreamResponse();
    message.partialTranscription = object.partialTranscription ?? "";
    message.isFinal = object.isFinal ?? false;
    message.genderProxy = object.genderProxy ?? "";
    message.emotionProxy = object.emotionProxy ?? "";
    message.arousal = object.arousal ?? 0;
    message.valence = object.valence ?? 0;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
