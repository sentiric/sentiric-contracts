// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/stream/v1/gateway.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.stream.v1";

/** ... Mesaj isimleri aynı kalabilir veya StreamSessionRequest olarak genelleyebilirsin ... */
export interface StreamSessionRequest {
  config?: SessionConfig | undefined;
  audioChunk?: Uint8Array | undefined;
  textMessage?: string | undefined;
  control?: SessionControl | undefined;
}

/** [ARCH-COMPLIANCE] StreamSessionResponse'a zengin veri yolları eklendi */
export interface StreamSessionResponse {
  audioResponse?: Uint8Array | undefined;
  textResponse?: string | undefined;
  statusUpdate?:
    | string
    | undefined;
  /** <--- YENİ: UI için zengin metin */
  transcript?:
    | TranscriptEvent
    | undefined;
  /** <--- YENİ: Barge-in kontrolü */
  clearAudioBuffer?: boolean | undefined;
}

export interface TranscriptEvent {
  text: string;
  isFinal: boolean;
  /** "USER" veya "AI" */
  sender: string;
  emotion: string;
  gender: string;
}

export interface SessionConfig {
  token: string;
  language: string;
  sampleRate: number;
  /** [YENİ] Edge cihazlar için Ghost Mode bayrağı. */
  edgeMode: boolean;
  /** [ARCH-COMPLIANCE FIX] Session Authority */
  traceId: string;
  sessionId: string;
}

export interface SessionControl {
  event: SessionControl_EventType;
}

export enum SessionControl_EventType {
  /** EVENT_TYPE_UNSPECIFIED - Linter Kuralı: 0 değeri _UNSPECIFIED ile bitmeli. */
  EVENT_TYPE_UNSPECIFIED = 0,
  /** EVENT_TYPE_INTERRUPT - Linter Kuralı: Değerler ENUM_NAME_ ile başlamalı. */
  EVENT_TYPE_INTERRUPT = 1,
  EVENT_TYPE_EOS = 2,
  EVENT_TYPE_HANGUP = 3,
  UNRECOGNIZED = -1,
}

export function sessionControl_EventTypeFromJSON(object: any): SessionControl_EventType {
  switch (object) {
    case 0:
    case "EVENT_TYPE_UNSPECIFIED":
      return SessionControl_EventType.EVENT_TYPE_UNSPECIFIED;
    case 1:
    case "EVENT_TYPE_INTERRUPT":
      return SessionControl_EventType.EVENT_TYPE_INTERRUPT;
    case 2:
    case "EVENT_TYPE_EOS":
      return SessionControl_EventType.EVENT_TYPE_EOS;
    case 3:
    case "EVENT_TYPE_HANGUP":
      return SessionControl_EventType.EVENT_TYPE_HANGUP;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SessionControl_EventType.UNRECOGNIZED;
  }
}

export function sessionControl_EventTypeToJSON(object: SessionControl_EventType): string {
  switch (object) {
    case SessionControl_EventType.EVENT_TYPE_UNSPECIFIED:
      return "EVENT_TYPE_UNSPECIFIED";
    case SessionControl_EventType.EVENT_TYPE_INTERRUPT:
      return "EVENT_TYPE_INTERRUPT";
    case SessionControl_EventType.EVENT_TYPE_EOS:
      return "EVENT_TYPE_EOS";
    case SessionControl_EventType.EVENT_TYPE_HANGUP:
      return "EVENT_TYPE_HANGUP";
    case SessionControl_EventType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

function createBaseStreamSessionRequest(): StreamSessionRequest {
  return { config: undefined, audioChunk: undefined, textMessage: undefined, control: undefined };
}

export const StreamSessionRequest: MessageFns<StreamSessionRequest> = {
  encode(message: StreamSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.config !== undefined) {
      SessionConfig.encode(message.config, writer.uint32(10).fork()).join();
    }
    if (message.audioChunk !== undefined) {
      writer.uint32(18).bytes(message.audioChunk);
    }
    if (message.textMessage !== undefined) {
      writer.uint32(26).string(message.textMessage);
    }
    if (message.control !== undefined) {
      SessionControl.encode(message.control, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.config = SessionConfig.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.audioChunk = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.textMessage = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.control = SessionControl.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamSessionRequest {
    return {
      config: isSet(object.config) ? SessionConfig.fromJSON(object.config) : undefined,
      audioChunk: isSet(object.audioChunk)
        ? bytesFromBase64(object.audioChunk)
        : isSet(object.audio_chunk)
        ? bytesFromBase64(object.audio_chunk)
        : undefined,
      textMessage: isSet(object.textMessage)
        ? globalThis.String(object.textMessage)
        : isSet(object.text_message)
        ? globalThis.String(object.text_message)
        : undefined,
      control: isSet(object.control) ? SessionControl.fromJSON(object.control) : undefined,
    };
  },

  toJSON(message: StreamSessionRequest): unknown {
    const obj: any = {};
    if (message.config !== undefined) {
      obj.config = SessionConfig.toJSON(message.config);
    }
    if (message.audioChunk !== undefined) {
      obj.audioChunk = base64FromBytes(message.audioChunk);
    }
    if (message.textMessage !== undefined) {
      obj.textMessage = message.textMessage;
    }
    if (message.control !== undefined) {
      obj.control = SessionControl.toJSON(message.control);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamSessionRequest>, I>>(base?: I): StreamSessionRequest {
    return StreamSessionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamSessionRequest>, I>>(object: I): StreamSessionRequest {
    const message = createBaseStreamSessionRequest();
    message.config = (object.config !== undefined && object.config !== null)
      ? SessionConfig.fromPartial(object.config)
      : undefined;
    message.audioChunk = object.audioChunk ?? undefined;
    message.textMessage = object.textMessage ?? undefined;
    message.control = (object.control !== undefined && object.control !== null)
      ? SessionControl.fromPartial(object.control)
      : undefined;
    return message;
  },
};

function createBaseStreamSessionResponse(): StreamSessionResponse {
  return {
    audioResponse: undefined,
    textResponse: undefined,
    statusUpdate: undefined,
    transcript: undefined,
    clearAudioBuffer: undefined,
  };
}

export const StreamSessionResponse: MessageFns<StreamSessionResponse> = {
  encode(message: StreamSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.audioResponse !== undefined) {
      writer.uint32(10).bytes(message.audioResponse);
    }
    if (message.textResponse !== undefined) {
      writer.uint32(18).string(message.textResponse);
    }
    if (message.statusUpdate !== undefined) {
      writer.uint32(26).string(message.statusUpdate);
    }
    if (message.transcript !== undefined) {
      TranscriptEvent.encode(message.transcript, writer.uint32(34).fork()).join();
    }
    if (message.clearAudioBuffer !== undefined) {
      writer.uint32(40).bool(message.clearAudioBuffer);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.audioResponse = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.textResponse = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.statusUpdate = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.transcript = TranscriptEvent.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.clearAudioBuffer = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamSessionResponse {
    return {
      audioResponse: isSet(object.audioResponse)
        ? bytesFromBase64(object.audioResponse)
        : isSet(object.audio_response)
        ? bytesFromBase64(object.audio_response)
        : undefined,
      textResponse: isSet(object.textResponse)
        ? globalThis.String(object.textResponse)
        : isSet(object.text_response)
        ? globalThis.String(object.text_response)
        : undefined,
      statusUpdate: isSet(object.statusUpdate)
        ? globalThis.String(object.statusUpdate)
        : isSet(object.status_update)
        ? globalThis.String(object.status_update)
        : undefined,
      transcript: isSet(object.transcript) ? TranscriptEvent.fromJSON(object.transcript) : undefined,
      clearAudioBuffer: isSet(object.clearAudioBuffer)
        ? globalThis.Boolean(object.clearAudioBuffer)
        : isSet(object.clear_audio_buffer)
        ? globalThis.Boolean(object.clear_audio_buffer)
        : undefined,
    };
  },

  toJSON(message: StreamSessionResponse): unknown {
    const obj: any = {};
    if (message.audioResponse !== undefined) {
      obj.audioResponse = base64FromBytes(message.audioResponse);
    }
    if (message.textResponse !== undefined) {
      obj.textResponse = message.textResponse;
    }
    if (message.statusUpdate !== undefined) {
      obj.statusUpdate = message.statusUpdate;
    }
    if (message.transcript !== undefined) {
      obj.transcript = TranscriptEvent.toJSON(message.transcript);
    }
    if (message.clearAudioBuffer !== undefined) {
      obj.clearAudioBuffer = message.clearAudioBuffer;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamSessionResponse>, I>>(base?: I): StreamSessionResponse {
    return StreamSessionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamSessionResponse>, I>>(object: I): StreamSessionResponse {
    const message = createBaseStreamSessionResponse();
    message.audioResponse = object.audioResponse ?? undefined;
    message.textResponse = object.textResponse ?? undefined;
    message.statusUpdate = object.statusUpdate ?? undefined;
    message.transcript = (object.transcript !== undefined && object.transcript !== null)
      ? TranscriptEvent.fromPartial(object.transcript)
      : undefined;
    message.clearAudioBuffer = object.clearAudioBuffer ?? undefined;
    return message;
  },
};

function createBaseTranscriptEvent(): TranscriptEvent {
  return { text: "", isFinal: false, sender: "", emotion: "", gender: "" };
}

export const TranscriptEvent: MessageFns<TranscriptEvent> = {
  encode(message: TranscriptEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.isFinal !== false) {
      writer.uint32(16).bool(message.isFinal);
    }
    if (message.sender !== "") {
      writer.uint32(26).string(message.sender);
    }
    if (message.emotion !== "") {
      writer.uint32(34).string(message.emotion);
    }
    if (message.gender !== "") {
      writer.uint32(42).string(message.gender);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TranscriptEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTranscriptEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isFinal = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.emotion = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.gender = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TranscriptEvent {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      isFinal: isSet(object.isFinal)
        ? globalThis.Boolean(object.isFinal)
        : isSet(object.is_final)
        ? globalThis.Boolean(object.is_final)
        : false,
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      emotion: isSet(object.emotion) ? globalThis.String(object.emotion) : "",
      gender: isSet(object.gender) ? globalThis.String(object.gender) : "",
    };
  },

  toJSON(message: TranscriptEvent): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.isFinal !== false) {
      obj.isFinal = message.isFinal;
    }
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.emotion !== "") {
      obj.emotion = message.emotion;
    }
    if (message.gender !== "") {
      obj.gender = message.gender;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TranscriptEvent>, I>>(base?: I): TranscriptEvent {
    return TranscriptEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TranscriptEvent>, I>>(object: I): TranscriptEvent {
    const message = createBaseTranscriptEvent();
    message.text = object.text ?? "";
    message.isFinal = object.isFinal ?? false;
    message.sender = object.sender ?? "";
    message.emotion = object.emotion ?? "";
    message.gender = object.gender ?? "";
    return message;
  },
};

function createBaseSessionConfig(): SessionConfig {
  return { token: "", language: "", sampleRate: 0, edgeMode: false, traceId: "", sessionId: "" };
}

export const SessionConfig: MessageFns<SessionConfig> = {
  encode(message: SessionConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    if (message.language !== "") {
      writer.uint32(18).string(message.language);
    }
    if (message.sampleRate !== 0) {
      writer.uint32(24).uint32(message.sampleRate);
    }
    if (message.edgeMode !== false) {
      writer.uint32(32).bool(message.edgeMode);
    }
    if (message.traceId !== "") {
      writer.uint32(42).string(message.traceId);
    }
    if (message.sessionId !== "") {
      writer.uint32(50).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.language = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sampleRate = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.edgeMode = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.traceId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionConfig {
    return {
      token: isSet(object.token) ? globalThis.String(object.token) : "",
      language: isSet(object.language) ? globalThis.String(object.language) : "",
      sampleRate: isSet(object.sampleRate)
        ? globalThis.Number(object.sampleRate)
        : isSet(object.sample_rate)
        ? globalThis.Number(object.sample_rate)
        : 0,
      edgeMode: isSet(object.edgeMode)
        ? globalThis.Boolean(object.edgeMode)
        : isSet(object.edge_mode)
        ? globalThis.Boolean(object.edge_mode)
        : false,
      traceId: isSet(object.traceId)
        ? globalThis.String(object.traceId)
        : isSet(object.trace_id)
        ? globalThis.String(object.trace_id)
        : "",
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
    };
  },

  toJSON(message: SessionConfig): unknown {
    const obj: any = {};
    if (message.token !== "") {
      obj.token = message.token;
    }
    if (message.language !== "") {
      obj.language = message.language;
    }
    if (message.sampleRate !== 0) {
      obj.sampleRate = Math.round(message.sampleRate);
    }
    if (message.edgeMode !== false) {
      obj.edgeMode = message.edgeMode;
    }
    if (message.traceId !== "") {
      obj.traceId = message.traceId;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SessionConfig>, I>>(base?: I): SessionConfig {
    return SessionConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SessionConfig>, I>>(object: I): SessionConfig {
    const message = createBaseSessionConfig();
    message.token = object.token ?? "";
    message.language = object.language ?? "";
    message.sampleRate = object.sampleRate ?? 0;
    message.edgeMode = object.edgeMode ?? false;
    message.traceId = object.traceId ?? "";
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseSessionControl(): SessionControl {
  return { event: 0 };
}

export const SessionControl: MessageFns<SessionControl> = {
  encode(message: SessionControl, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.event !== 0) {
      writer.uint32(8).int32(message.event);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionControl {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionControl();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.event = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionControl {
    return { event: isSet(object.event) ? sessionControl_EventTypeFromJSON(object.event) : 0 };
  },

  toJSON(message: SessionControl): unknown {
    const obj: any = {};
    if (message.event !== 0) {
      obj.event = sessionControl_EventTypeToJSON(message.event);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SessionControl>, I>>(base?: I): SessionControl {
    return SessionControl.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SessionControl>, I>>(object: I): SessionControl {
    const message = createBaseSessionControl();
    message.event = object.event ?? 0;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
