// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/external/v1/connector.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.external.v1";

/** ESKİ: ActionRequest */
export interface ExecuteActionRequest {
  connectorName: string;
  actionName: string;
  parameters: { [key: string]: string };
}

export interface ExecuteActionRequest_ParametersEntry {
  key: string;
  value: string;
}

/** ESKİ: ActionResult */
export interface ExecuteActionResponse {
  success: boolean;
  output: { [key: string]: string };
}

export interface ExecuteActionResponse_OutputEntry {
  key: string;
  value: string;
}

function createBaseExecuteActionRequest(): ExecuteActionRequest {
  return { connectorName: "", actionName: "", parameters: {} };
}

export const ExecuteActionRequest: MessageFns<ExecuteActionRequest> = {
  encode(message: ExecuteActionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.connectorName !== "") {
      writer.uint32(10).string(message.connectorName);
    }
    if (message.actionName !== "") {
      writer.uint32(18).string(message.actionName);
    }
    globalThis.Object.entries(message.parameters).forEach(([key, value]: [string, string]) => {
      ExecuteActionRequest_ParametersEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecuteActionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecuteActionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.connectorName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.actionName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = ExecuteActionRequest_ParametersEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.parameters[entry3.key] = entry3.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecuteActionRequest {
    return {
      connectorName: isSet(object.connectorName)
        ? globalThis.String(object.connectorName)
        : isSet(object.connector_name)
        ? globalThis.String(object.connector_name)
        : "",
      actionName: isSet(object.actionName)
        ? globalThis.String(object.actionName)
        : isSet(object.action_name)
        ? globalThis.String(object.action_name)
        : "",
      parameters: isObject(object.parameters)
        ? (globalThis.Object.entries(object.parameters) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: ExecuteActionRequest): unknown {
    const obj: any = {};
    if (message.connectorName !== "") {
      obj.connectorName = message.connectorName;
    }
    if (message.actionName !== "") {
      obj.actionName = message.actionName;
    }
    if (message.parameters) {
      const entries = globalThis.Object.entries(message.parameters) as [string, string][];
      if (entries.length > 0) {
        obj.parameters = {};
        entries.forEach(([k, v]) => {
          obj.parameters[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExecuteActionRequest>, I>>(base?: I): ExecuteActionRequest {
    return ExecuteActionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecuteActionRequest>, I>>(object: I): ExecuteActionRequest {
    const message = createBaseExecuteActionRequest();
    message.connectorName = object.connectorName ?? "";
    message.actionName = object.actionName ?? "";
    message.parameters = (globalThis.Object.entries(object.parameters ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseExecuteActionRequest_ParametersEntry(): ExecuteActionRequest_ParametersEntry {
  return { key: "", value: "" };
}

export const ExecuteActionRequest_ParametersEntry: MessageFns<ExecuteActionRequest_ParametersEntry> = {
  encode(message: ExecuteActionRequest_ParametersEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecuteActionRequest_ParametersEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecuteActionRequest_ParametersEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecuteActionRequest_ParametersEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ExecuteActionRequest_ParametersEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExecuteActionRequest_ParametersEntry>, I>>(
    base?: I,
  ): ExecuteActionRequest_ParametersEntry {
    return ExecuteActionRequest_ParametersEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecuteActionRequest_ParametersEntry>, I>>(
    object: I,
  ): ExecuteActionRequest_ParametersEntry {
    const message = createBaseExecuteActionRequest_ParametersEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseExecuteActionResponse(): ExecuteActionResponse {
  return { success: false, output: {} };
}

export const ExecuteActionResponse: MessageFns<ExecuteActionResponse> = {
  encode(message: ExecuteActionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    globalThis.Object.entries(message.output).forEach(([key, value]: [string, string]) => {
      ExecuteActionResponse_OutputEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecuteActionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecuteActionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = ExecuteActionResponse_OutputEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.output[entry2.key] = entry2.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecuteActionResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      output: isObject(object.output)
        ? (globalThis.Object.entries(object.output) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: ExecuteActionResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.output) {
      const entries = globalThis.Object.entries(message.output) as [string, string][];
      if (entries.length > 0) {
        obj.output = {};
        entries.forEach(([k, v]) => {
          obj.output[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExecuteActionResponse>, I>>(base?: I): ExecuteActionResponse {
    return ExecuteActionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecuteActionResponse>, I>>(object: I): ExecuteActionResponse {
    const message = createBaseExecuteActionResponse();
    message.success = object.success ?? false;
    message.output = (globalThis.Object.entries(object.output ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseExecuteActionResponse_OutputEntry(): ExecuteActionResponse_OutputEntry {
  return { key: "", value: "" };
}

export const ExecuteActionResponse_OutputEntry: MessageFns<ExecuteActionResponse_OutputEntry> = {
  encode(message: ExecuteActionResponse_OutputEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecuteActionResponse_OutputEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecuteActionResponse_OutputEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecuteActionResponse_OutputEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ExecuteActionResponse_OutputEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExecuteActionResponse_OutputEntry>, I>>(
    base?: I,
  ): ExecuteActionResponse_OutputEntry {
    return ExecuteActionResponse_OutputEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecuteActionResponse_OutputEntry>, I>>(
    object: I,
  ): ExecuteActionResponse_OutputEntry {
    const message = createBaseExecuteActionResponse_OutputEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
