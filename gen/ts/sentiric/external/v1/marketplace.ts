// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/external/v1/marketplace.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.external.v1";

/** ESKİ: ListRequest */
export interface ListRecipesRequest {
  category: string;
}

/** ESKİ: ListResponse */
export interface ListRecipesResponse {
  recipes: RecipeInfo[];
}

export interface RecipeInfo {
  recipeId: string;
  name: string;
  description: string;
}

/** ESKİ: InstallRequest */
export interface InstallRecipeRequest {
  recipeId: string;
  tenantId: string;
}

/** ESKİ: InstallResponse */
export interface InstallRecipeResponse {
  success: boolean;
  deploymentStatus: string;
}

function createBaseListRecipesRequest(): ListRecipesRequest {
  return { category: "" };
}

export const ListRecipesRequest: MessageFns<ListRecipesRequest> = {
  encode(message: ListRecipesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.category !== "") {
      writer.uint32(10).string(message.category);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListRecipesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListRecipesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.category = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListRecipesRequest {
    return { category: isSet(object.category) ? globalThis.String(object.category) : "" };
  },

  toJSON(message: ListRecipesRequest): unknown {
    const obj: any = {};
    if (message.category !== "") {
      obj.category = message.category;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListRecipesRequest>, I>>(base?: I): ListRecipesRequest {
    return ListRecipesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListRecipesRequest>, I>>(object: I): ListRecipesRequest {
    const message = createBaseListRecipesRequest();
    message.category = object.category ?? "";
    return message;
  },
};

function createBaseListRecipesResponse(): ListRecipesResponse {
  return { recipes: [] };
}

export const ListRecipesResponse: MessageFns<ListRecipesResponse> = {
  encode(message: ListRecipesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.recipes) {
      RecipeInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListRecipesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListRecipesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.recipes.push(RecipeInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListRecipesResponse {
    return {
      recipes: globalThis.Array.isArray(object?.recipes) ? object.recipes.map((e: any) => RecipeInfo.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListRecipesResponse): unknown {
    const obj: any = {};
    if (message.recipes?.length) {
      obj.recipes = message.recipes.map((e) => RecipeInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListRecipesResponse>, I>>(base?: I): ListRecipesResponse {
    return ListRecipesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListRecipesResponse>, I>>(object: I): ListRecipesResponse {
    const message = createBaseListRecipesResponse();
    message.recipes = object.recipes?.map((e) => RecipeInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRecipeInfo(): RecipeInfo {
  return { recipeId: "", name: "", description: "" };
}

export const RecipeInfo: MessageFns<RecipeInfo> = {
  encode(message: RecipeInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.recipeId !== "") {
      writer.uint32(10).string(message.recipeId);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RecipeInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecipeInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.recipeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RecipeInfo {
    return {
      recipeId: isSet(object.recipeId)
        ? globalThis.String(object.recipeId)
        : isSet(object.recipe_id)
        ? globalThis.String(object.recipe_id)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
    };
  },

  toJSON(message: RecipeInfo): unknown {
    const obj: any = {};
    if (message.recipeId !== "") {
      obj.recipeId = message.recipeId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RecipeInfo>, I>>(base?: I): RecipeInfo {
    return RecipeInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RecipeInfo>, I>>(object: I): RecipeInfo {
    const message = createBaseRecipeInfo();
    message.recipeId = object.recipeId ?? "";
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    return message;
  },
};

function createBaseInstallRecipeRequest(): InstallRecipeRequest {
  return { recipeId: "", tenantId: "" };
}

export const InstallRecipeRequest: MessageFns<InstallRecipeRequest> = {
  encode(message: InstallRecipeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.recipeId !== "") {
      writer.uint32(10).string(message.recipeId);
    }
    if (message.tenantId !== "") {
      writer.uint32(18).string(message.tenantId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InstallRecipeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInstallRecipeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.recipeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tenantId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InstallRecipeRequest {
    return {
      recipeId: isSet(object.recipeId)
        ? globalThis.String(object.recipeId)
        : isSet(object.recipe_id)
        ? globalThis.String(object.recipe_id)
        : "",
      tenantId: isSet(object.tenantId)
        ? globalThis.String(object.tenantId)
        : isSet(object.tenant_id)
        ? globalThis.String(object.tenant_id)
        : "",
    };
  },

  toJSON(message: InstallRecipeRequest): unknown {
    const obj: any = {};
    if (message.recipeId !== "") {
      obj.recipeId = message.recipeId;
    }
    if (message.tenantId !== "") {
      obj.tenantId = message.tenantId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InstallRecipeRequest>, I>>(base?: I): InstallRecipeRequest {
    return InstallRecipeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InstallRecipeRequest>, I>>(object: I): InstallRecipeRequest {
    const message = createBaseInstallRecipeRequest();
    message.recipeId = object.recipeId ?? "";
    message.tenantId = object.tenantId ?? "";
    return message;
  },
};

function createBaseInstallRecipeResponse(): InstallRecipeResponse {
  return { success: false, deploymentStatus: "" };
}

export const InstallRecipeResponse: MessageFns<InstallRecipeResponse> = {
  encode(message: InstallRecipeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.deploymentStatus !== "") {
      writer.uint32(18).string(message.deploymentStatus);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InstallRecipeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInstallRecipeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.deploymentStatus = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InstallRecipeResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      deploymentStatus: isSet(object.deploymentStatus)
        ? globalThis.String(object.deploymentStatus)
        : isSet(object.deployment_status)
        ? globalThis.String(object.deployment_status)
        : "",
    };
  },

  toJSON(message: InstallRecipeResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.deploymentStatus !== "") {
      obj.deploymentStatus = message.deploymentStatus;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InstallRecipeResponse>, I>>(base?: I): InstallRecipeResponse {
    return InstallRecipeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InstallRecipeResponse>, I>>(object: I): InstallRecipeResponse {
    const message = createBaseInstallRecipeResponse();
    message.success = object.success ?? false;
    message.deploymentStatus = object.deploymentStatus ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
