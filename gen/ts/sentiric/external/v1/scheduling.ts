// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/external/v1/scheduling.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.external.v1";

/** ESKİ: ApptRequest */
export interface CreateAppointmentRequest {
  userId: string;
  title: string;
  startTimeIso: string;
  endTimeIso: string;
}

/** ESKİ: ApptResponse */
export interface CreateAppointmentResponse {
  appointmentId: string;
  /** Başarı durumunu field 2'ye taşıdık */
  success: boolean;
}

function createBaseCreateAppointmentRequest(): CreateAppointmentRequest {
  return { userId: "", title: "", startTimeIso: "", endTimeIso: "" };
}

export const CreateAppointmentRequest: MessageFns<CreateAppointmentRequest> = {
  encode(message: CreateAppointmentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.startTimeIso !== "") {
      writer.uint32(26).string(message.startTimeIso);
    }
    if (message.endTimeIso !== "") {
      writer.uint32(34).string(message.endTimeIso);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateAppointmentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateAppointmentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.startTimeIso = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.endTimeIso = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateAppointmentRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      startTimeIso: isSet(object.startTimeIso)
        ? globalThis.String(object.startTimeIso)
        : isSet(object.start_time_iso)
        ? globalThis.String(object.start_time_iso)
        : "",
      endTimeIso: isSet(object.endTimeIso)
        ? globalThis.String(object.endTimeIso)
        : isSet(object.end_time_iso)
        ? globalThis.String(object.end_time_iso)
        : "",
    };
  },

  toJSON(message: CreateAppointmentRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.startTimeIso !== "") {
      obj.startTimeIso = message.startTimeIso;
    }
    if (message.endTimeIso !== "") {
      obj.endTimeIso = message.endTimeIso;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateAppointmentRequest>, I>>(base?: I): CreateAppointmentRequest {
    return CreateAppointmentRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateAppointmentRequest>, I>>(object: I): CreateAppointmentRequest {
    const message = createBaseCreateAppointmentRequest();
    message.userId = object.userId ?? "";
    message.title = object.title ?? "";
    message.startTimeIso = object.startTimeIso ?? "";
    message.endTimeIso = object.endTimeIso ?? "";
    return message;
  },
};

function createBaseCreateAppointmentResponse(): CreateAppointmentResponse {
  return { appointmentId: "", success: false };
}

export const CreateAppointmentResponse: MessageFns<CreateAppointmentResponse> = {
  encode(message: CreateAppointmentResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.appointmentId !== "") {
      writer.uint32(10).string(message.appointmentId);
    }
    if (message.success !== false) {
      writer.uint32(16).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateAppointmentResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateAppointmentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.appointmentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateAppointmentResponse {
    return {
      appointmentId: isSet(object.appointmentId)
        ? globalThis.String(object.appointmentId)
        : isSet(object.appointment_id)
        ? globalThis.String(object.appointment_id)
        : "",
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
    };
  },

  toJSON(message: CreateAppointmentResponse): unknown {
    const obj: any = {};
    if (message.appointmentId !== "") {
      obj.appointmentId = message.appointmentId;
    }
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateAppointmentResponse>, I>>(base?: I): CreateAppointmentResponse {
    return CreateAppointmentResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateAppointmentResponse>, I>>(object: I): CreateAppointmentResponse {
    const message = createBaseCreateAppointmentResponse();
    message.appointmentId = object.appointmentId ?? "";
    message.success = object.success ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
