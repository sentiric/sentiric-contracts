// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/data/v1/config.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.data.v1";

/** AppConfig, servislerin başlangıç yapılandırmasını (port, log seviyesi, sertifika yolları) içerir. */
export interface AppConfig {
  serviceName: string;
  environment: string;
  logLevel: number;
  tlsCertPath?: string | undefined;
}

function createBaseAppConfig(): AppConfig {
  return { serviceName: "", environment: "", logLevel: 0, tlsCertPath: undefined };
}

export const AppConfig: MessageFns<AppConfig> = {
  encode(message: AppConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serviceName !== "") {
      writer.uint32(10).string(message.serviceName);
    }
    if (message.environment !== "") {
      writer.uint32(18).string(message.environment);
    }
    if (message.logLevel !== 0) {
      writer.uint32(24).int32(message.logLevel);
    }
    if (message.tlsCertPath !== undefined) {
      writer.uint32(34).string(message.tlsCertPath);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.serviceName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.environment = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.logLevel = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.tlsCertPath = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppConfig {
    return {
      serviceName: isSet(object.serviceName)
        ? globalThis.String(object.serviceName)
        : isSet(object.service_name)
        ? globalThis.String(object.service_name)
        : "",
      environment: isSet(object.environment) ? globalThis.String(object.environment) : "",
      logLevel: isSet(object.logLevel)
        ? globalThis.Number(object.logLevel)
        : isSet(object.log_level)
        ? globalThis.Number(object.log_level)
        : 0,
      tlsCertPath: isSet(object.tlsCertPath)
        ? globalThis.String(object.tlsCertPath)
        : isSet(object.tls_cert_path)
        ? globalThis.String(object.tls_cert_path)
        : undefined,
    };
  },

  toJSON(message: AppConfig): unknown {
    const obj: any = {};
    if (message.serviceName !== "") {
      obj.serviceName = message.serviceName;
    }
    if (message.environment !== "") {
      obj.environment = message.environment;
    }
    if (message.logLevel !== 0) {
      obj.logLevel = Math.round(message.logLevel);
    }
    if (message.tlsCertPath !== undefined) {
      obj.tlsCertPath = message.tlsCertPath;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AppConfig>, I>>(base?: I): AppConfig {
    return AppConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AppConfig>, I>>(object: I): AppConfig {
    const message = createBaseAppConfig();
    message.serviceName = object.serviceName ?? "";
    message.environment = object.environment ?? "";
    message.logLevel = object.logLevel ?? 0;
    message.tlsCertPath = object.tlsCertPath ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
