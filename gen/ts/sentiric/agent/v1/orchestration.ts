// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: sentiric/agent/v1/orchestration.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../../google/protobuf/timestamp";
import { CallContext } from "../../data/v1/context";

export const protobufPackage = "sentiric.agent.v1";

/** [YENİ] Transcript Modeli */
export interface TranscriptEntry {
  /** USER, AI, AGENT */
  senderType: string;
  messageText: string;
  sentimentScore: number;
  createdAt?:
    | Date
    | undefined;
  /** Media Payload (JSON string olarak taşınır) */
  mediaPayloadJson: string;
}

export interface GetConversationTranscriptRequest {
  callId: string;
}

export interface GetConversationTranscriptResponse {
  entries: TranscriptEntry[];
  summary: string;
  overallSentiment: number;
}

/** --- Mevcut Mesajlar --- */
export interface ProcessManualDialRequest {
  destinationNumber: string;
  userId: string;
  tenantId: string;
}

export interface ProcessManualDialResponse {
  accepted: boolean;
  callId: string;
  errorMessage: string;
}

export interface ProcessCallStartRequest {
  callId: string;
  dialplanId: string;
  callerNumber: string;
}

export interface ProcessCallStartResponse {
  initiated: boolean;
}

export interface ProcessSagaStepRequest {
  sagaId: string;
  stepName: string;
  context?: CallContext | undefined;
}

export interface ProcessSagaStepResponse {
  completed: boolean;
}

function createBaseTranscriptEntry(): TranscriptEntry {
  return { senderType: "", messageText: "", sentimentScore: 0, createdAt: undefined, mediaPayloadJson: "" };
}

export const TranscriptEntry: MessageFns<TranscriptEntry> = {
  encode(message: TranscriptEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.senderType !== "") {
      writer.uint32(10).string(message.senderType);
    }
    if (message.messageText !== "") {
      writer.uint32(18).string(message.messageText);
    }
    if (message.sentimentScore !== 0) {
      writer.uint32(29).float(message.sentimentScore);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(34).fork()).join();
    }
    if (message.mediaPayloadJson !== "") {
      writer.uint32(42).string(message.mediaPayloadJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TranscriptEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTranscriptEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.senderType = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.messageText = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.sentimentScore = reader.float();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.mediaPayloadJson = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TranscriptEntry {
    return {
      senderType: isSet(object.senderType)
        ? globalThis.String(object.senderType)
        : isSet(object.sender_type)
        ? globalThis.String(object.sender_type)
        : "",
      messageText: isSet(object.messageText)
        ? globalThis.String(object.messageText)
        : isSet(object.message_text)
        ? globalThis.String(object.message_text)
        : "",
      sentimentScore: isSet(object.sentimentScore)
        ? globalThis.Number(object.sentimentScore)
        : isSet(object.sentiment_score)
        ? globalThis.Number(object.sentiment_score)
        : 0,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      mediaPayloadJson: isSet(object.mediaPayloadJson)
        ? globalThis.String(object.mediaPayloadJson)
        : isSet(object.media_payload_json)
        ? globalThis.String(object.media_payload_json)
        : "",
    };
  },

  toJSON(message: TranscriptEntry): unknown {
    const obj: any = {};
    if (message.senderType !== "") {
      obj.senderType = message.senderType;
    }
    if (message.messageText !== "") {
      obj.messageText = message.messageText;
    }
    if (message.sentimentScore !== 0) {
      obj.sentimentScore = message.sentimentScore;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.mediaPayloadJson !== "") {
      obj.mediaPayloadJson = message.mediaPayloadJson;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TranscriptEntry>, I>>(base?: I): TranscriptEntry {
    return TranscriptEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TranscriptEntry>, I>>(object: I): TranscriptEntry {
    const message = createBaseTranscriptEntry();
    message.senderType = object.senderType ?? "";
    message.messageText = object.messageText ?? "";
    message.sentimentScore = object.sentimentScore ?? 0;
    message.createdAt = object.createdAt ?? undefined;
    message.mediaPayloadJson = object.mediaPayloadJson ?? "";
    return message;
  },
};

function createBaseGetConversationTranscriptRequest(): GetConversationTranscriptRequest {
  return { callId: "" };
}

export const GetConversationTranscriptRequest: MessageFns<GetConversationTranscriptRequest> = {
  encode(message: GetConversationTranscriptRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.callId !== "") {
      writer.uint32(10).string(message.callId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetConversationTranscriptRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetConversationTranscriptRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.callId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetConversationTranscriptRequest {
    return {
      callId: isSet(object.callId)
        ? globalThis.String(object.callId)
        : isSet(object.call_id)
        ? globalThis.String(object.call_id)
        : "",
    };
  },

  toJSON(message: GetConversationTranscriptRequest): unknown {
    const obj: any = {};
    if (message.callId !== "") {
      obj.callId = message.callId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetConversationTranscriptRequest>, I>>(
    base?: I,
  ): GetConversationTranscriptRequest {
    return GetConversationTranscriptRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetConversationTranscriptRequest>, I>>(
    object: I,
  ): GetConversationTranscriptRequest {
    const message = createBaseGetConversationTranscriptRequest();
    message.callId = object.callId ?? "";
    return message;
  },
};

function createBaseGetConversationTranscriptResponse(): GetConversationTranscriptResponse {
  return { entries: [], summary: "", overallSentiment: 0 };
}

export const GetConversationTranscriptResponse: MessageFns<GetConversationTranscriptResponse> = {
  encode(message: GetConversationTranscriptResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.entries) {
      TranscriptEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.summary !== "") {
      writer.uint32(18).string(message.summary);
    }
    if (message.overallSentiment !== 0) {
      writer.uint32(29).float(message.overallSentiment);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetConversationTranscriptResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetConversationTranscriptResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entries.push(TranscriptEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.summary = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.overallSentiment = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetConversationTranscriptResponse {
    return {
      entries: globalThis.Array.isArray(object?.entries)
        ? object.entries.map((e: any) => TranscriptEntry.fromJSON(e))
        : [],
      summary: isSet(object.summary) ? globalThis.String(object.summary) : "",
      overallSentiment: isSet(object.overallSentiment)
        ? globalThis.Number(object.overallSentiment)
        : isSet(object.overall_sentiment)
        ? globalThis.Number(object.overall_sentiment)
        : 0,
    };
  },

  toJSON(message: GetConversationTranscriptResponse): unknown {
    const obj: any = {};
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => TranscriptEntry.toJSON(e));
    }
    if (message.summary !== "") {
      obj.summary = message.summary;
    }
    if (message.overallSentiment !== 0) {
      obj.overallSentiment = message.overallSentiment;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetConversationTranscriptResponse>, I>>(
    base?: I,
  ): GetConversationTranscriptResponse {
    return GetConversationTranscriptResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetConversationTranscriptResponse>, I>>(
    object: I,
  ): GetConversationTranscriptResponse {
    const message = createBaseGetConversationTranscriptResponse();
    message.entries = object.entries?.map((e) => TranscriptEntry.fromPartial(e)) || [];
    message.summary = object.summary ?? "";
    message.overallSentiment = object.overallSentiment ?? 0;
    return message;
  },
};

function createBaseProcessManualDialRequest(): ProcessManualDialRequest {
  return { destinationNumber: "", userId: "", tenantId: "" };
}

export const ProcessManualDialRequest: MessageFns<ProcessManualDialRequest> = {
  encode(message: ProcessManualDialRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.destinationNumber !== "") {
      writer.uint32(10).string(message.destinationNumber);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    if (message.tenantId !== "") {
      writer.uint32(26).string(message.tenantId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProcessManualDialRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProcessManualDialRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.destinationNumber = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tenantId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProcessManualDialRequest {
    return {
      destinationNumber: isSet(object.destinationNumber)
        ? globalThis.String(object.destinationNumber)
        : isSet(object.destination_number)
        ? globalThis.String(object.destination_number)
        : "",
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      tenantId: isSet(object.tenantId)
        ? globalThis.String(object.tenantId)
        : isSet(object.tenant_id)
        ? globalThis.String(object.tenant_id)
        : "",
    };
  },

  toJSON(message: ProcessManualDialRequest): unknown {
    const obj: any = {};
    if (message.destinationNumber !== "") {
      obj.destinationNumber = message.destinationNumber;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.tenantId !== "") {
      obj.tenantId = message.tenantId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProcessManualDialRequest>, I>>(base?: I): ProcessManualDialRequest {
    return ProcessManualDialRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProcessManualDialRequest>, I>>(object: I): ProcessManualDialRequest {
    const message = createBaseProcessManualDialRequest();
    message.destinationNumber = object.destinationNumber ?? "";
    message.userId = object.userId ?? "";
    message.tenantId = object.tenantId ?? "";
    return message;
  },
};

function createBaseProcessManualDialResponse(): ProcessManualDialResponse {
  return { accepted: false, callId: "", errorMessage: "" };
}

export const ProcessManualDialResponse: MessageFns<ProcessManualDialResponse> = {
  encode(message: ProcessManualDialResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accepted !== false) {
      writer.uint32(8).bool(message.accepted);
    }
    if (message.callId !== "") {
      writer.uint32(18).string(message.callId);
    }
    if (message.errorMessage !== "") {
      writer.uint32(26).string(message.errorMessage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProcessManualDialResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProcessManualDialResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.accepted = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.callId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProcessManualDialResponse {
    return {
      accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false,
      callId: isSet(object.callId)
        ? globalThis.String(object.callId)
        : isSet(object.call_id)
        ? globalThis.String(object.call_id)
        : "",
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : "",
    };
  },

  toJSON(message: ProcessManualDialResponse): unknown {
    const obj: any = {};
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    if (message.callId !== "") {
      obj.callId = message.callId;
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProcessManualDialResponse>, I>>(base?: I): ProcessManualDialResponse {
    return ProcessManualDialResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProcessManualDialResponse>, I>>(object: I): ProcessManualDialResponse {
    const message = createBaseProcessManualDialResponse();
    message.accepted = object.accepted ?? false;
    message.callId = object.callId ?? "";
    message.errorMessage = object.errorMessage ?? "";
    return message;
  },
};

function createBaseProcessCallStartRequest(): ProcessCallStartRequest {
  return { callId: "", dialplanId: "", callerNumber: "" };
}

export const ProcessCallStartRequest: MessageFns<ProcessCallStartRequest> = {
  encode(message: ProcessCallStartRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.callId !== "") {
      writer.uint32(10).string(message.callId);
    }
    if (message.dialplanId !== "") {
      writer.uint32(18).string(message.dialplanId);
    }
    if (message.callerNumber !== "") {
      writer.uint32(26).string(message.callerNumber);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProcessCallStartRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProcessCallStartRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.callId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.dialplanId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.callerNumber = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProcessCallStartRequest {
    return {
      callId: isSet(object.callId)
        ? globalThis.String(object.callId)
        : isSet(object.call_id)
        ? globalThis.String(object.call_id)
        : "",
      dialplanId: isSet(object.dialplanId)
        ? globalThis.String(object.dialplanId)
        : isSet(object.dialplan_id)
        ? globalThis.String(object.dialplan_id)
        : "",
      callerNumber: isSet(object.callerNumber)
        ? globalThis.String(object.callerNumber)
        : isSet(object.caller_number)
        ? globalThis.String(object.caller_number)
        : "",
    };
  },

  toJSON(message: ProcessCallStartRequest): unknown {
    const obj: any = {};
    if (message.callId !== "") {
      obj.callId = message.callId;
    }
    if (message.dialplanId !== "") {
      obj.dialplanId = message.dialplanId;
    }
    if (message.callerNumber !== "") {
      obj.callerNumber = message.callerNumber;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProcessCallStartRequest>, I>>(base?: I): ProcessCallStartRequest {
    return ProcessCallStartRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProcessCallStartRequest>, I>>(object: I): ProcessCallStartRequest {
    const message = createBaseProcessCallStartRequest();
    message.callId = object.callId ?? "";
    message.dialplanId = object.dialplanId ?? "";
    message.callerNumber = object.callerNumber ?? "";
    return message;
  },
};

function createBaseProcessCallStartResponse(): ProcessCallStartResponse {
  return { initiated: false };
}

export const ProcessCallStartResponse: MessageFns<ProcessCallStartResponse> = {
  encode(message: ProcessCallStartResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.initiated !== false) {
      writer.uint32(8).bool(message.initiated);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProcessCallStartResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProcessCallStartResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.initiated = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProcessCallStartResponse {
    return { initiated: isSet(object.initiated) ? globalThis.Boolean(object.initiated) : false };
  },

  toJSON(message: ProcessCallStartResponse): unknown {
    const obj: any = {};
    if (message.initiated !== false) {
      obj.initiated = message.initiated;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProcessCallStartResponse>, I>>(base?: I): ProcessCallStartResponse {
    return ProcessCallStartResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProcessCallStartResponse>, I>>(object: I): ProcessCallStartResponse {
    const message = createBaseProcessCallStartResponse();
    message.initiated = object.initiated ?? false;
    return message;
  },
};

function createBaseProcessSagaStepRequest(): ProcessSagaStepRequest {
  return { sagaId: "", stepName: "", context: undefined };
}

export const ProcessSagaStepRequest: MessageFns<ProcessSagaStepRequest> = {
  encode(message: ProcessSagaStepRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sagaId !== "") {
      writer.uint32(10).string(message.sagaId);
    }
    if (message.stepName !== "") {
      writer.uint32(18).string(message.stepName);
    }
    if (message.context !== undefined) {
      CallContext.encode(message.context, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProcessSagaStepRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProcessSagaStepRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sagaId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.stepName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.context = CallContext.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProcessSagaStepRequest {
    return {
      sagaId: isSet(object.sagaId)
        ? globalThis.String(object.sagaId)
        : isSet(object.saga_id)
        ? globalThis.String(object.saga_id)
        : "",
      stepName: isSet(object.stepName)
        ? globalThis.String(object.stepName)
        : isSet(object.step_name)
        ? globalThis.String(object.step_name)
        : "",
      context: isSet(object.context) ? CallContext.fromJSON(object.context) : undefined,
    };
  },

  toJSON(message: ProcessSagaStepRequest): unknown {
    const obj: any = {};
    if (message.sagaId !== "") {
      obj.sagaId = message.sagaId;
    }
    if (message.stepName !== "") {
      obj.stepName = message.stepName;
    }
    if (message.context !== undefined) {
      obj.context = CallContext.toJSON(message.context);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProcessSagaStepRequest>, I>>(base?: I): ProcessSagaStepRequest {
    return ProcessSagaStepRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProcessSagaStepRequest>, I>>(object: I): ProcessSagaStepRequest {
    const message = createBaseProcessSagaStepRequest();
    message.sagaId = object.sagaId ?? "";
    message.stepName = object.stepName ?? "";
    message.context = (object.context !== undefined && object.context !== null)
      ? CallContext.fromPartial(object.context)
      : undefined;
    return message;
  },
};

function createBaseProcessSagaStepResponse(): ProcessSagaStepResponse {
  return { completed: false };
}

export const ProcessSagaStepResponse: MessageFns<ProcessSagaStepResponse> = {
  encode(message: ProcessSagaStepResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.completed !== false) {
      writer.uint32(8).bool(message.completed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProcessSagaStepResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProcessSagaStepResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.completed = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProcessSagaStepResponse {
    return { completed: isSet(object.completed) ? globalThis.Boolean(object.completed) : false };
  },

  toJSON(message: ProcessSagaStepResponse): unknown {
    const obj: any = {};
    if (message.completed !== false) {
      obj.completed = message.completed;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProcessSagaStepResponse>, I>>(base?: I): ProcessSagaStepResponse {
    return ProcessSagaStepResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProcessSagaStepResponse>, I>>(object: I): ProcessSagaStepResponse {
    const message = createBaseProcessSagaStepResponse();
    message.completed = object.completed ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
