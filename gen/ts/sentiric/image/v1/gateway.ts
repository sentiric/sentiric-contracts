// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: sentiric/image/v1/gateway.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentiric.image.v1";

export interface GenerateImageRequest {
  tenantId: string;
  traceId: string;
  prompt: string;
  preferredModel: string;
  aspectRatio: string;
}

export interface GenerateImageResponse {
  success: boolean;
  imageUri: string;
  errorMessage: string;
}

function createBaseGenerateImageRequest(): GenerateImageRequest {
  return { tenantId: "", traceId: "", prompt: "", preferredModel: "", aspectRatio: "" };
}

export const GenerateImageRequest: MessageFns<GenerateImageRequest> = {
  encode(message: GenerateImageRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tenantId !== "") {
      writer.uint32(10).string(message.tenantId);
    }
    if (message.traceId !== "") {
      writer.uint32(18).string(message.traceId);
    }
    if (message.prompt !== "") {
      writer.uint32(26).string(message.prompt);
    }
    if (message.preferredModel !== "") {
      writer.uint32(34).string(message.preferredModel);
    }
    if (message.aspectRatio !== "") {
      writer.uint32(42).string(message.aspectRatio);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateImageRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateImageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tenantId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.traceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.prompt = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.preferredModel = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.aspectRatio = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateImageRequest {
    return {
      tenantId: isSet(object.tenantId)
        ? globalThis.String(object.tenantId)
        : isSet(object.tenant_id)
        ? globalThis.String(object.tenant_id)
        : "",
      traceId: isSet(object.traceId)
        ? globalThis.String(object.traceId)
        : isSet(object.trace_id)
        ? globalThis.String(object.trace_id)
        : "",
      prompt: isSet(object.prompt) ? globalThis.String(object.prompt) : "",
      preferredModel: isSet(object.preferredModel)
        ? globalThis.String(object.preferredModel)
        : isSet(object.preferred_model)
        ? globalThis.String(object.preferred_model)
        : "",
      aspectRatio: isSet(object.aspectRatio)
        ? globalThis.String(object.aspectRatio)
        : isSet(object.aspect_ratio)
        ? globalThis.String(object.aspect_ratio)
        : "",
    };
  },

  toJSON(message: GenerateImageRequest): unknown {
    const obj: any = {};
    if (message.tenantId !== "") {
      obj.tenantId = message.tenantId;
    }
    if (message.traceId !== "") {
      obj.traceId = message.traceId;
    }
    if (message.prompt !== "") {
      obj.prompt = message.prompt;
    }
    if (message.preferredModel !== "") {
      obj.preferredModel = message.preferredModel;
    }
    if (message.aspectRatio !== "") {
      obj.aspectRatio = message.aspectRatio;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenerateImageRequest>, I>>(base?: I): GenerateImageRequest {
    return GenerateImageRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenerateImageRequest>, I>>(object: I): GenerateImageRequest {
    const message = createBaseGenerateImageRequest();
    message.tenantId = object.tenantId ?? "";
    message.traceId = object.traceId ?? "";
    message.prompt = object.prompt ?? "";
    message.preferredModel = object.preferredModel ?? "";
    message.aspectRatio = object.aspectRatio ?? "";
    return message;
  },
};

function createBaseGenerateImageResponse(): GenerateImageResponse {
  return { success: false, imageUri: "", errorMessage: "" };
}

export const GenerateImageResponse: MessageFns<GenerateImageResponse> = {
  encode(message: GenerateImageResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.imageUri !== "") {
      writer.uint32(18).string(message.imageUri);
    }
    if (message.errorMessage !== "") {
      writer.uint32(26).string(message.errorMessage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateImageResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateImageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.imageUri = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateImageResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      imageUri: isSet(object.imageUri)
        ? globalThis.String(object.imageUri)
        : isSet(object.image_uri)
        ? globalThis.String(object.image_uri)
        : "",
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : "",
    };
  },

  toJSON(message: GenerateImageResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.imageUri !== "") {
      obj.imageUri = message.imageUri;
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenerateImageResponse>, I>>(base?: I): GenerateImageResponse {
    return GenerateImageResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenerateImageResponse>, I>>(object: I): GenerateImageResponse {
    const message = createBaseGenerateImageResponse();
    message.success = object.success ?? false;
    message.imageUri = object.imageUri ?? "";
    message.errorMessage = object.errorMessage ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
